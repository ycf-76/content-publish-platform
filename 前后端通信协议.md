# 多智能体小红书发布平台 - 前后端通信协议

| 字段 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 创建日期 | 2026-07-24 |
| 关联文档 | PRD.md / 技术架构设计文档.md |
| 用途 | 前端组件设计的前置依赖，定义前后端数据通信契约 |

---

## 1. 通信架构总览

### 1.1 双通道模型

采用 **SSE（Server-Sent Events）+ HTTP POST** 双通道架构，而非 WebSocket。理由：

| 维度 | SSE + POST | WebSocket |
|---|---|---|
| 通信方向 | SSE单向推送（后端→前端）+ POST操作（前端→后端） | 双向 |
| 复杂度 | 低，浏览器原生支持EventSource | 高，需维护连接状态、心跳、重连 |
| 断线重连 | 浏览器原生自动重连 + `Last-Event-ID` 续传 | 需自实现 |
| 协议层 | HTTP，穿透代理简单 | 升级为WS，部分代理不友好 |
| 适配场景 | 本项目以"后端推状态"为主，前端操作稀疏 | 高频双向交互 |

**结论**：本场景后端推送是绝对主流（Agent每步都推状态），前端操作稀疏（审核/暂停/回退偶尔触发），SSE+POST 比 WebSocket 更合适、更简单。

```
┌──────────────────────────────────────────────────────────┐
│                        前端 (Vue3)                        │
│                                                          │
│  ┌─────────────────┐         ┌──────────────────────┐   │
│  │  EventSource    │◄────────│  SSE 通道 (单向推送)  │   │
│  │  (状态订阅)     │         │  /api/sse/workflow/:id│   │
│  └────────┬────────┘         └──────────────────────┘   │
│           │                                              │
│           ▼ Pinia store 更新                              │
│  ┌─────────────────┐                                    │
│  │  流程图/待办/审核│                                    │
│  │  组件实时渲染    │                                    │
│  └────────┬────────┘                                    │
│           │                                              │
│           ▼ 用户操作                                      │
│  ┌─────────────────┐         ┌──────────────────────┐   │
│  │  Axios POST     │────────►│  HTTP 通道 (操作)     │   │
│  │  (审核/暂停/回退)│         │  /api/workflow/...    │   │
│  └─────────────────┘         └──────────────────────┘   │
└──────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────┐
│                    后端 (FastAPI)                         │
│                                                          │
│  SSE Endpoint: 接收订阅，推送事件流                       │
│  POST Endpoint: 接收操作，返回同步响应                    │
│                                                          │
│  事件源：LangGraph节点状态变更 / Recovery / 审核待办 /    │
│         软语义建议 / 挂起 / 流式输出                      │
└──────────────────────────────────────────────────────────┘
```

### 1.2 通道职责划分

| 通道 | 方向 | 职责 | 端点 |
|---|---|---|---|
| SSE | 后端→前端 | 推送所有状态变更、事件、流式输出 | `GET /api/sse/workflow/{workflow_id}` |
| HTTP POST | 前端→后端 | 提交审核结果、暂停、回退、手动恢复、编辑内容 | 各业务端点（见第8章） |

---

## 2. SSE 通道规范

### 2.1 建立连接

```
GET /api/sse/workflow/{workflow_id}
Accept: text/event-stream
Authorization: Bearer {jwt_token}
```

后端响应：
```
Content-Type: text/event-stream
Cache-Control: no-cache
Connection: keep-alive
X-Accel-Buffering: no   # 禁用Nginx缓冲，保证实时性
```

### 2.2 消息格式

SSE 每条消息由若干字段组成，本项目采用以下字段：

```
id: {event_id}                    # 事件唯一ID，用于断线续传
event: {event_type}               # 事件类型（见第3章）
data: {json_payload}              # 事件数据，JSON字符串

```

> 注意：data 字段是 JSON 字符串，前端需 `JSON.parse()`。

### 2.3 通用事件信封

所有事件 data 字段遵循统一信封：

```typescript
interface SSEEvent<T> {
  event_id: string;           // 事件唯一ID
  event_type: string;         // 事件类型
  workflow_id: string;        // 工作流ID
  node_id: string | null;     // 关联节点ID（null=工作流级事件）
  timestamp: string;          // ISO8601时间戳
  payload: T;                 // 事件特定数据
}
```

### 2.4 断线重连

浏览器 EventSource 原生支持断线重连。重连时自动携带 `Last-Event-ID` 请求头，值为最后收到的事件ID。后端据此续传未发送的事件。

```
# 重连请求
GET /api/sse/workflow/{workflow_id}
Last-Event-ID: evt_018f3a2b...
```

后端逻辑：
- 收到 `Last-Event-ID` → 从该ID之后的事件开始重放（从内存事件缓冲区或DB读取）
- 未收到 → 从当前状态开始推送（先推一个 `workflow_snapshot` 全量快照，再推增量）

### 2.5 心跳

SSE 每隔 15 秒推送一个心跳注释（不是事件），防止代理超时断开：

```
: heartbeat

```

> 这是 SSE 注释行，前端 EventSource 不会触发 onmessage，仅用于保活。

---

## 3. 事件清单总表

### 3.1 事件分类

| 类别 | 事件类型 | 触发时机 | 优先级 |
|---|---|---|---|
| 工作流级 | `workflow_started` | 工作流启动 | 中 |
| 工作流级 | `workflow_snapshot` | SSE连接建立时的全量状态快照 | 高 |
| 工作流级 | `workflow_completed` | 全流程完成 | 高 |
| 工作流级 | `workflow_error` | 工作流级错误终止 | 高 |
| 工作流级 | `workflow_suspended` | 30分钟超时挂起 | 高 |
| 工作流级 | `workflow_resumed` | 挂起后被拉起 | 高 |
| 工作流级 | `workflow_paused` | 用户软暂停 | 中 |
| 节点级 | `node_status_changed` | 节点状态变更 | 高 |
| 节点级 | `node_started` | 节点开始执行 | 中 |
| 节点级 | `node_completed` | 节点执行完成 | 高 |
| 节点级 | `node_error` | 节点执行失败 | 高 |
| 流式 | `stream_chunk` | 文案逐字流式输出 | 中 |
| 流式 | `image_generated` | 单张图片生成完成 | 中 |
| 审核 | `review_required` | 需要人工审核 | 高 |
| 审核 | `review_processed` | 审核结果已处理 | 中 |
| 监督 | `supervisor_suggestion` | 软语义/Recovery建议回退 | 高 |
| 监督 | `recovery_attempt` | Recovery Loop某次尝试 | 低 |
| 监督 | `recovery_exhausted` | Recovery Loop三次全失败 | 高 |
| 监督 | `circuit_open` | 熔断器打开 | 中 |
| 检查点 | `checkpoint_saved` | 检查点已保存（供时光机） | 低 |
| Agent过程 | `tool_call_start` | 工具开始调用（L1必推） | 中 |
| Agent过程 | `tool_call_end` | 工具返回（L1必推，含摘要） | 中 |
| Agent过程 | `progress_update` | 进度更新（如VL分析3/10） | 中 |
| Agent过程 | `model_switched` | 模型切换（如R1→V3降级） | 中 |
| Agent过程 | `agent_thinking` | LLM思考流式chunk（R1 reasoning） | 中 |
| Agent过程 | `decision_made` | Agent关键决策点 | 低 |
| 系统 | `search_degraded` | 搜索降级到服务端模式（插件不可用） | 中 |

### 3.2 事件优先级与前端处理

| 优先级 | 含义 | 前端处理 |
|---|---|---|
| 高 | 必须立即渲染，需用户感知 | 视觉强反馈（高亮/弹窗/声音可选） |
| 中 | 实时渲染，不需特殊提示 | 静默更新对应组件 |
| 低 | 仅记录，不强制渲染 | 写入日志抽屉，不更新主视觉 |

---

## 4. 节点状态码定义

### 4.1 节点状态枚举

```typescript
type NodeStatus =
  | "pending"            // 待执行
  | "running"            // 执行中
  | "awaiting_review"    // 等待人工审核
  | "passed"             // 通过，进入下一步
  | "rejected"           // 被打回
  | "error"              // 异常
  | "suspended"          // 工作流挂起导致节点暂停
  | "completed"          // 全流程完成（仅终节点）
  | "terminated";        // 终止
```

### 4.2 状态流转规则

```
pending ──► running ──► passed ──► (下一节点 pending)
              │            │
              │            └──► awaiting_review ──► passed
              │                                  └──► rejected ──► (回退)
              │
              ├──► error ──► running (Recovery/监督重试)
              │           └──► terminated
              │
              └──► suspended ──► running (拉起恢复)
                              └──► terminated
```

### 4.3 节点类型枚举

```typescript
type NodeType =
  | "search"          // 搜索Agent
  | "analyze"         // 分析Agent
  | "image_gen"       // 图片生成Agent
  | "image_review"    // 图片人工审核
  | "copywrite"       // 文案Agent
  | "audit"           // 审核Agent
  | "final_review"    // 人工终审
  | "publish";        // 发布Agent
```

---

## 5. 事件详细 Schema

### 5.1 工作流级事件

#### 5.1.1 workflow_started

```typescript
interface WorkflowStartedPayload {
  workflow_id: string;
  theme: string;              // 主题，如"科技"
  keyword: string | null;     // 细分关键词
  account_nickname: string;   // 小红书账号昵称
  started_at: string;         // ISO8601
  nodes: NodeSnapshot[];      // 所有节点初始状态
}

interface NodeSnapshot {
  node_id: string;
  node_type: NodeType;
  node_label: string;         // 中文标签，如"搜索爆款"
  status: NodeStatus;
  order: number;              // 在流程图中的顺序
}
```

#### 5.1.2 workflow_snapshot（SSE连接时的全量快照）

```typescript
interface WorkflowSnapshotPayload {
  workflow_id: string;
  theme: string;
  keyword: string | null;
  status: WorkflowStatus;     // running / awaiting_review / suspended / completed / terminated
  current_node_id: string | null;
  started_at: string;
  progress: number;           // 0-100，已完成节点占比
  nodes: NodeDetail[];        // 所有节点的完整状态（含产出）
  pending_reviews: PendingReview[];      // 当前待办列表
  pending_suggestions: PendingSuggestion[];  // 未处理的监督建议
  checkpoints: CheckpointMeta[];         // 检查点元数据（供时光机）
}

interface NodeDetail extends NodeSnapshot {
  input: any | null;
  output: any | null;
  model: string | null;
  started_at: string | null;
  completed_at: string | null;
  duration_ms: number | null;
  token_cost: number | null;
  quality_report: QualityReport | null;
  error: string | null;
}

interface QualityReport {
  quality_pass: boolean;
  issues: string[];
  severity: "low" | "medium" | "high";
}
```

#### 5.1.3 workflow_completed

```typescript
interface WorkflowCompletedPayload {
  workflow_id: string;
  completed_at: string;
  total_duration_ms: number;
  total_token_cost: number;
  post_url: string;           // 发布成功的小红书笔记链接
}
```

#### 5.1.4 workflow_error

```typescript
interface WorkflowErrorPayload {
  workflow_id: string;
  error_code: string;         // 见第9章错误码表
  error_message: string;
  failed_node_id: string | null;
  recoverable: boolean;       // 是否可恢复（用户可手动重试）
}
```

#### 5.1.5 workflow_suspended

```typescript
interface WorkflowSuspendedPayload {
  workflow_id: string;
  reason: "structural_recovery_timeout";   // 目前仅此一种
  pending_node_id: string;                 // 待用户决策的节点
  pending_action: PendingSuggestion;       // 待确认的结构性恢复建议
  suspended_at: string;
  can_resume_manually: boolean;            // 是否可手动恢复（通常true）
  next_auto_check_at: string;              // 下次定时拉起检查时间
}
```

#### 5.1.6 workflow_resumed

```typescript
interface WorkflowResumedPayload {
  workflow_id: string;
  resume_reason: "manual" | "auto_dependency_recovered" | "auto_timer";
  resumed_at: string;
  pending_node_id: string;     // 重新进入待确认的节点
}
```

#### 5.1.7 workflow_paused

```typescript
interface WorkflowPausedPayload {
  workflow_id: string;
  paused_at: string;
  current_node_id: string;     // 当前正在跑的节点（软暂停：该节点跑完后停）
  will_stop_after_node: string; // 将在此节点后停止
  note: string;                // "当前节点完成后自动暂停"
}
```

---

### 5.2 节点级事件

#### 5.2.1 node_started

```typescript
interface NodeStartedPayload {
  node_id: string;
  node_type: NodeType;
  model: string | null;        // 使用的模型
  attempt: number;             // 第几次尝试（Recovery Loop内，0=首次）
  strategy: string | null;     // 当前使用的Recovery策略（null=首次执行）
}
```

#### 5.2.2 node_status_changed（通用状态变更）

```typescript
interface NodeStatusChangedPayload {
  node_id: string;
  node_type: NodeType;
  old_status: NodeStatus;
  new_status: NodeStatus;
  changed_at: string;
  // 视new_status而定的附加字段：
  output?: any;                // new_status=passed 时
  error?: string;              // new_status=error 时
  quality_report?: QualityReport;  // 软语义判断后
}
```

#### 5.2.3 node_completed

```typescript
interface NodeCompletedPayload {
  node_id: string;
  node_type: NodeType;
  status: "passed" | "error";
  output: any | null;
  model: string | null;
  duration_ms: number;
  token_cost: number;
  quality_report: QualityReport | null;  // 若经过软语义
  error: string | null;
  next_node_id: string | null;  // 下一个将执行的节点（null=工作流结束）
}
```

#### 5.2.4 node_error

```typescript
interface NodeErrorPayload {
  node_id: string;
  node_type: NodeType;
  error_code: string;
  error_message: string;
  crash_reason: string;        // 见6.4节Recovery决策的crash_reason枚举
  recovery_attempts: number;   // 已尝试次数
  escalate_to_supervisor: boolean;  // 是否已上报监督Agent
}
```

---

### 5.3 流式输出事件

#### 5.3.1 stream_chunk（文案逐字流式）

文案Agent使用DeepSeek-R1流式输出。后端每收到一个token块即推送。

```typescript
interface StreamChunkPayload {
  node_id: string;             // copywrite节点ID
  field: "title" | "body" | "tags";  // 当前流式的字段
  chunk: string;               // 本次增量文本
  index: number;               // 该字段的第几个chunk（从0递增）
  is_final: boolean;           // 该字段是否流式完成
}

// 前端处理：拼接chunk到对应字段，逐字渲染
// title流式完 → body流式 → tags流式 → is_final=true 整体完成
```

**协议约定**：
- 流式顺序固定：`title` → `body` → `tags`
- 每个 field 的最后一个 chunk `is_final=true`
- 整个文案节点流式完成后，仍会推送 `node_completed` 事件（含完整output）

#### 5.3.2 image_generated（图片分张推送）

图片生成Agent每完成一张图即推送，不等全部完成。

```typescript
interface ImageGeneratedPayload {
  node_id: string;             // image_gen节点ID
  image_index: number;         // 第几张（从0递增）
  image_url: string;
  prompt_used: string;
  model: string;
  total_expected: number;      // 预期总张数
  is_final: boolean;           // 是否最后一张
}

// 前端处理：渐进式展示，每收到一张就渲染一张
// 全部完成后，节点进入 awaiting_review
```

---

### 5.4 审核事件

#### 5.4.1 review_required（需用户审核）

```typescript
interface ReviewRequiredPayload {
  review_id: string;           // 审核任务ID
  node_id: string;
  node_type: "image_review" | "final_review";
  review_type: "image" | "final";
  created_at: string;
  timeout_seconds: number;     // 审核超时（图片审核无超时；结构性回退30分钟）

  // image类型特有
  images?: ReviewImage[];

  // final类型特有
  content?: ReviewContent;
}

interface ReviewImage {
  image_index: number;
  image_url: string;
  prompt_used: string;
  model: string;
}

interface ReviewContent {
  title: string;
  body: string;
  tags: string[];
  images: string[];            // 图片URL列表
  audit_report: QualityReport | null;  // 审核Agent的报告
}
```

**前端处理**（B+A混合方案）：
1. 推送此事件 → 流程图对应节点变 `awaiting_review`，显示小铃铛
2. 同时更新顶部待办计数器 `pending_reviews.length + 1`
3. 节点自动展开为审核卡片（B方案：就地审核）
4. 顶部待办列表新增一项（A方案：全局入口）

#### 5.4.2 review_processed（审核结果已处理）

```typescript
interface ReviewProcessedPayload {
  review_id: string;
  node_id: string;
  action: "pass" | "reject" | "edit" | "upload_image" | "regenerate";
  processed_at: string;
  next_node_id: string | null;  // 审核后的下一节点
  // 若action=reject，附带回退目标
  rollback_target_node_id?: string;
}
```

---

### 5.5 监督Agent事件

#### 5.5.1 supervisor_suggestion（建议回退，建议权）

软语义不通过或 Recovery 决策层判定需结构性恢复时推送。

```typescript
interface SupervisorSuggestionPayload {
  suggestion_id: string;
  node_id: string;             // 问题节点
  source: "soft_semantic" | "recovery_decision";
  issue_summary: string;       // 问题摘要（前端展示用）
  issues: string[];            // 详细问题列表
  severity: "low" | "medium" | "high";
  suggested_action: "rollback" | "terminate";
  target_node_id: string | null;   // 建议回退到的目标节点
  reason: string;              // 建议理由
  require_user_confirm: boolean;   // true=用户拍板
  timeout_seconds: number;     // 结构性恢复30分钟超时
  created_at: string;

  // 溯源链（你方案里要求的"附上溯源链"）
  trace: SuggestionTrace[];    // 导致此建议的上游节点链路
}

interface SuggestionTrace {
  node_id: string;
  node_type: NodeType;
  node_label: string;
  contribution: string;        // 该节点对问题的贡献描述
}
```

**前端处理**（B+A混合）：
1. 推送此事件 → 流程图对应节点旁显示"AI建议"气泡（含issue_summary + 溯源链）
2. 同时更新顶部待办计数器
3. 气泡内提供按钮：`确认回退` / `忽略建议`
4. 启动30分钟倒计时显示

#### 5.5.2 recovery_attempt（Recovery Loop某次尝试）

```typescript
interface RecoveryAttemptPayload {
  node_id: string;
  attempt: number;             // 第几次（0/1/2）
  max_attempts: number;        // 总次数（3）
  strategy: string;            // 使用的策略名
  strategy_description: string;// 策略中文描述，如"拓宽搜索关键词"
  error: string;               // 本次失败原因
  next_delay_seconds: number;  // 下次重试前等待秒数（退避）
}
```

**前端处理**：低优先级，仅写入节点详情抽屉的"恢复日志"，不弹窗。但节点上可显示"重试中(2/3)"角标。

#### 5.5.3 recovery_exhausted

```typescript
interface RecoveryExhaustedPayload {
  node_id: string;
  attempts: number;            // 已尝试次数（=max_attempts）
  last_error: string;
  escalated_to_supervisor: boolean;  // true=已上报，等待supervisor_suggestion
  // 若escalated=false，说明监督决策为"技术性恢复"，正在自动处理
  auto_recovery_action?: {
    type: "switch_model" | "refresh_token";
    description: string;
  };
}
```

#### 5.5.4 circuit_open

```typescript
interface CircuitOpenPayload {
  node_id: string;
  agent_id: string;
  failure_count: number;
  threshold: number;
  recovery_timeout_seconds: number;  // 多少秒后半开试探
  message: string;              // "该智能体近期失败过多，已熔断，60秒后试探恢复"
}
```

---

### 5.6 检查点事件

#### 5.6.1 checkpoint_saved（供时光机）

```typescript
interface CheckpointSavedPayload {
  checkpoint_id: string;
  node_id: string;
  node_label: string;
  order: number;               // 第几个检查点
  langgraph_checkpoint_id: string;
  saved_at: string;
  summary: string;             // 该检查点的摘要（时光机滑块上显示）
}
```

**前端处理**：写入时光机时间轴，作为滑块的一个刻度。

---

### 5.7 Agent过程事件（D16 过程透明化）

本类事件实现"Agent内部过程透明化"，让用户实时看到Agent在思考什么、调用什么工具、进度如何。**这是产品体验从"能用"到"惊艳"的关键设计**。

#### 5.7.1 设计原理：分层推送

为避免刷屏和成本爆炸，Agent过程事件采用三层分级：

| Layer | 推送策略 | 内容 | 默认是否推前端 |
|---|---|---|---|
| **L1 必推** | 实时推SSE | 工具调用、进度、模型切换 | 是（所有粒度模式都推） |
| **L2 可推** | 流式推SSE | R1 reasoning思考过程、关键决策 | 仅"详细模式"展开可见 |
| **L3 不推** | 仅存DB | 原始prompt、完整工具返回、完整token | 用户主动点击"查看原始数据"时HTTP加载 |

**粒度模式**（用户可在设置中切换）：
- `concise` 简洁模式（默认）：仅显示L1事件
- `detailed` 详细模式：L1+L2（思考过程可展开折叠）

**推送频率**：不节流。Recovery Loop最多3次attempt，VL多图分析靠 `progress_update` 天然限频，无需额外节流逻辑。

#### 5.7.2 tool_call_start（L1 工具调用开始）

```typescript
interface ToolCallStartPayload {
  node_id: string;
  trace_id: string;              // 本次工具调用的唯一ID（用于关联tool_call_end）
  tool_name: string;             // 技术名，如 "xhs_search"
  tool_display_name: string;     // 中文显示名，如 "小红书搜索"
  inputs: Record<string, any>;   // 可读化的输入参数（已是人类可读，非原始JSON）
  started_at: string;
}
```

**inputs 可读化规则**（Harness内格式化，不调LLM）：
- `xhs_search`: `{ keyword: "AI编程", limit: 10 }`
- `vl_analyze`: `{ image_index: 1, total: 10 }`
- `generate_image`: `{ prompt: "暖色调俯拍手冲咖啡", model: "wanx" }`

**前端处理**：在节点详情面板时间线新增一条，图标🔍蓝色，文字如"调用小红书搜索：关键词=AI编程"。

#### 5.7.3 tool_call_end（L1 工具调用返回）

```typescript
interface ToolCallEndPayload {
  node_id: string;
  trace_id: string;              // 关联tool_call_start
  tool_name: string;
  success: boolean;
  result_summary: string;        // ★ 人类可读摘要（Harness格式化，非原始返回）
  duration_ms: number;
  // 原始返回不推SSE，存DB（L3），用户主动查看时通过HTTP加载
}
```

**result_summary 生成规则**（Harness内 `summarize_result()` 方法，不调LLM）：
- `xhs_search`: `"找到10条笔记，平均点赞2.3k，最高5.8k"`
- `vl_analyze`: `"图片标签：暖色调、俯拍、手冲咖啡、极简背景"`
- `generate_image`: `"生成成功，URL已保存"`

**前端处理**：时间线新增一条，图标✓绿色，文字为result_summary。

#### 5.7.4 progress_update（L1 进度更新）

用于多步骤工具的进度反馈，如VL分析10张图、生成3张图。

```typescript
interface ProgressUpdatePayload {
  node_id: string;
  current: number;               // 当前第几步
  total: number;                 // 总步数
  label: string;                 // 进度标签，如"分析图片" / "生成图片"
  percentage: number;            // 0-100，前端可直接用
}
```

**前端处理**：
- 节点详情面板时间线新增进度条
- 节点本身可显示简化进度角标（如"3/10"）
- 不阻塞主流程，仅可视化

#### 5.7.5 model_switched（L1 模型切换）

Recovery Loop切换模型、技术性恢复自动切模型时推送。

```typescript
interface ModelSwitchedPayload {
  node_id: string;
  from_model: string;            // 原模型，如 "deepseek-r1"
  to_model: string;              // 新模型，如 "deepseek-v3"
  reason: string;                // 切换原因，如 "R1过载降级" / "Recovery策略"
  auto: boolean;                 // true=自动切换，false=用户手动
}
```

**前端处理**：时间线新增一条，图标🔄蓝色，文字如"模型切换：R1 → V3（R1过载降级）"。

#### 5.7.6 agent_thinking（L2 LLM思考流式）

DeepSeek-R1的 `reasoning_content` 流式推送。**零额外LLM成本**（reasoning本身就是R1流式输出的副产品）。

```typescript
interface AgentThinkingPayload {
  node_id: string;
  thinking_id: string;           // 本次思考会话ID（一个节点可能有多次LLM调用）
  chunk: string;                 // 思考内容增量
  index: number;                 // 第几个chunk（从0递增）
  is_final: boolean;             // 本次思考会话是否结束
}
```

**关键约定**：
- 原始reasoning直接流式推送，**不做摘要**（避免额外LLM成本和失真）
- 前端默认折叠在"思考过程"折叠区，用户主动展开才看
- `concise`模式下：事件仍推SSE（前端缓存），但UI不渲染展开区
- `detailed`模式下：展开区可见，逐字流式渲染
- 思考内容可能较长（2-5k token），前端用虚拟滚动避免DOM爆炸

**前端处理**：
- 时间线新增"💭 思考中..."条目，可展开
- 展开后逐字流式渲染（类似ChatGPT o1的thinking展开）
- 完成后折叠区保留完整内容，可再次展开查看

#### 5.7.7 decision_made（L2 关键决策点）

Agent在执行中做出的关键决策，让用户看到"Agent决定了什么"。

```typescript
interface DecisionMadePayload {
  node_id: string;
  decision_type: string;         // 决策类型枚举
  decision_summary: string;      // 决策摘要，如"决定采用清单体结构"
  rationale: string;             // 简短理由，如"3条爆款中2条用了清单体"
  alternatives_considered: string[];  // 考虑过的其他选项
}
```

**decision_type 枚举**：
- `structure_choice`: 结构选择（清单体/故事体/对比体）
- `tone_choice`: 调性选择（专业/轻松/激情）
- `image_style_choice`: 图片风格选择
- `keyword_refinement`: 关键词优化
- `content_focus`: 内容焦点选择

**前端处理**：时间线新增一条，图标🎯紫色，文字为decision_summary，悬浮显示rationale。

#### 5.7.8 L3原始数据获取（HTTP接口）

L3不通过SSE推送，用户主动点击"查看原始数据"时通过HTTP加载。

```
GET /api/node/{node_id}/trace
Authorization: Bearer {jwt}

# 响应
{
  "node_id": "...",
  "traces": [
    {
      "trace_id": "...",
      "type": "tool_call",
      "tool_name": "xhs_search",
      "raw_inputs": {...},        // 完整原始输入
      "raw_output": {...},        // 完整原始返回
      "started_at": "...",
      "duration_ms": 1234
    },
    {
      "type": "llm_call",
      "model": "deepseek-r1",
      "raw_prompt": "...",        // 完整prompt
      "raw_response": "...",      // 完整响应
      "reasoning_content": "...", // 完整reasoning
      "token_input": 1234,
      "token_output": 567
    }
  ]
}
```

**前端处理**：在节点详情面板底部提供"展开原始数据"按钮，点击后调此接口，以折叠JSON树形结构展示。

---

### 5.8 系统事件（D17 MCP降级）

#### 5.8.1 search_degraded（搜索降级到服务端模式）

当浏览器插件MCP不可用，系统降级到uv run本地MCP时推送。提示用户当前风控较高。

```typescript
interface SearchDegradedPayload {
  reason: string;                    // 降级原因，如 "plugin_offline" / "plugin_error"
  message: string;                   // 人类可读消息
  current_mode: "plugin" | "local";  // 当前模式
  suggest_install_plugin: boolean;   // 是否建议安装插件
  risk_level: "low" | "medium" | "high";  // 当前风控等级
}
```

**前端处理**：
- 顶部显示黄色提示横幅（非阻塞，可关闭）
- 文案："浏览器插件未运行，已降级到服务端搜索，风控较高。建议安装插件以获得更稳定体验。"
- 提供"安装插件指南"链接
- 流程图搜索节点照常执行，不中断

**触发时机**：
- 后端MCP Client Manager健康检查发现插件离线
- 插件调用失败触发降级

---

## 6. Recovery 决策相关协议

### 6.1 crash_reason 枚举

监督Agent根据 crash_reason 决定恢复类型：

```typescript
type CrashReason =
  | "timeout"              // 超时 → 技术性（重试/切模型）
  | "empty_output"         // 空返回 → 技术性（重试）
  | "schema_error"         // 格式错误 → 技术性（重试/简化prompt）
  | "http_500"             // 服务端错误 → 技术性（切模型）
  | "http_429"             // 限流 → 技术性（退避+切模型）
  | "model_overload"       // 模型过载 → 技术性（切V3）
  | "rate_limit"           // 限流 → 技术性（退避）
  | "token_expired"        // Token过期 → 技术性（刷新Token）
  | "input_insufficient"   // 输入不足 → 结构性（回退上游）
  | "image_gen_failed"     // 图片生成失败 → 结构性（回退分析）
  | "content_mismatch"     // 内容不匹配 → 结构性（回退创作/搜索）
  | "unknown";             // 未知 → 终止
```

### 6.2 恢复类型判定规则

| crash_reason | 恢复类型 | 决策权限 |
|---|---|---|
| timeout / empty_output / schema_error | 技术性 | 自动 |
| http_500 / http_429 / model_overload / rate_limit | 技术性（切模型） | 自动 |
| token_expired | 技术性（刷新Token） | 自动 |
| input_insufficient | 结构性（回退搜索） | 用户拍板 |
| image_gen_failed | 结构性（回退分析） | 用户拍板 |
| content_mismatch | 结构性（回退创作） | 用户拍板 |
| unknown | 终止 | 用户拍板 |

### 6.3 技术性恢复的协议流程

```
1. node_error        (crash_reason=model_overload)
2. recovery_exhausted (escalated=false, auto_recovery_action=switch_model)
   → 前端静默，节点显示"自动恢复中"
3. node_started      (attempt=0, strategy=switch_model)  ← 注意attempt重置
4. node_completed    (成功) 或 再次进入Recovery
```

### 6.4 结构性恢复的协议流程

```
1. node_error          (crash_reason=input_insufficient)
2. recovery_exhausted  (escalated=true)
3. supervisor_suggestion (suggested_action=rollback, target_node_id=search,
                          require_user_confirm=true, timeout_seconds=1800,
                          trace=[...])
   → 前端：节点旁气泡 + 待办中心新增项 + 30分钟倒计时
4. 用户操作（POST /api/workflow/{id}/confirm-rollback 或 /dismiss-suggestion）
5a. 确认回退 → review_processed (action=reject, rollback_target_node_id=search)
   → 后端执行 rollback_to_node
   → node_status_changed (search节点 running)
6a. 30分钟无响应 → workflow_suspended
   → 定时拉起 → workflow_resumed → 回到步骤3状态
```

---

## 7. 流式输出协议细节

### 7.1 文案流式（DeepSeek-R1）

```
后端                                    前端
  │                                       │
  │  stream_chunk (field=title, chunk="今")│
  │ ────────────────────────────────────► │  渲染"今"
  │  stream_chunk (field=title, chunk="天")│
  │ ────────────────────────────────────► │  渲染"今天"
  │  stream_chunk (field=title, is_final)  │
  │ ────────────────────────────────────► │  title完成
  │  stream_chunk (field=body, chunk="...")│
  │ ────────────────────────────────────► │  body开始流式
  │  ...                                   │
  │  stream_chunk (field=tags, is_final)   │
  │ ────────────────────────────────────► │  整体流式完成
  │  node_completed                        │
  │ ────────────────────────────────────► │  节点完成（含完整output校验）
```

**断线续传约定**：若SSE断线重连，流式不续传（已渲染的文本保留），重连后若节点已完成则推 `node_completed`（含完整output，前端用完整output覆盖已渲染文本，保证一致性）。

### 7.2 图片分张推送

```
后端                                    前端
  │                                       │
  │  image_generated (index=0, url=...)   │
  │ ────────────────────────────────────► │  渲染第1张
  │  image_generated (index=1, url=...)   │
  │ ────────────────────────────────────► │  渲染第2张
  │  image_generated (index=2, is_final)  │
  │ ────────────────────────────────────► │  全部完成
  │  node_status_changed (awaiting_review)│
  │ ────────────────────────────────────► │  进入图片审核
```

---

## 8. HTTP POST 操作端点

### 8.1 审核操作

#### 提交审核结果

```
POST /api/node/{node_id}/review
Authorization: Bearer {jwt}
Content-Type: application/json

// 图片审核 - 通过
{
  "action": "pass"
}

// 图片审核 - 重新生成
{
  "action": "regenerate",
  "modified_prompt": "更暖的色调，俯拍角度"  // 可选，不改则用原prompt
}

// 图片审核 - 上传自有图片
{
  "action": "upload_image",
  "image_index": 0,                    // 替换第几张
  "image_base64": "..." 或 "image_url": "..."
}

// 终审 - 通过并发布
{
  "action": "pass"
}

// 终审 - 打回到指定节点
{
  "action": "reject",
  "rollback_target_node_id": "copywrite",
  "reason": "文案调性不对"
}

// 终审 - 手动编辑后发布
{
  "action": "edit",
  "edited_content": {
    "title": "...",
    "body": "...",
    "tags": [...]
  }
}
```

响应：
```json
{
  "review_id": "...",
  "accepted": true,
  "next_node_id": "publish",
  "message": "审核已处理，正在发布"
}
```

### 8.2 监督建议操作

```
POST /api/workflow/{workflow_id}/suggestion/{suggestion_id}/confirm
// 确认回退
{
  "action": "confirm_rollback"
}

POST /api/workflow/{workflow_id}/suggestion/{suggestion_id}/dismiss
// 忽略建议，继续
{
  "action": "dismiss",
  "reason": "我觉得没问题"  // 可选
}
```

### 8.3 暂停与恢复

```
POST /api/workflow/{workflow_id}/pause
// 软暂停：当前节点跑完后停
// 响应：
{
  "paused": true,
  "will_stop_after_node": "analyze",
  "message": "将在分析节点完成后暂停"
}

POST /api/workflow/{workflow_id}/resume
// 从暂停恢复
{
  "resumed": true,
  "current_node_id": "image_gen"
}

POST /api/workflow/{workflow_id}/terminate
// 终止工作流
{
  "terminated": true
}
```

### 8.4 回退操作（用户主动）

```
POST /api/workflow/{workflow_id}/rollback
// 用户在时光机滑块上选择某检查点，点击"从此处继续"
{
  "target_checkpoint_id": "ckpt_018f...",
  "target_node_id": "analyze"
}

// 响应：
{
  "rollback_accepted": true,
  "rollback_to_node": "analyze",
  "downstream_will_rerun": ["image_gen", "image_review", "copywrite", ...],
  "message": "已回退到分析节点，下游将重新执行"
}
```

### 8.5 挂起任务的手动恢复

```
POST /api/workflow/{workflow_id}/manual-resume
// 用户手动拉起挂起的任务
{
  "resumed": true,
  "pending_node_id": "copywrite",
  "message": "任务已拉起，等待您确认结构性恢复方案"
}
```

### 8.6 节点内容编辑

```
PATCH /api/node/{node_id}/content
// 手动编辑节点产出（如终审前编辑文案）
{
  "field": "body",
  "value": "编辑后的正文内容..."
}
```

---

## 9. 错误码表

### 9.1 工作流级错误码

| 错误码 | 含义 | 可恢复 | 用户提示 |
|---|---|---|---|
| WF_001 | 工作流初始化失败 | 否 | "工作流启动失败，请重试" |
| WF_002 | 账号未绑定或Session失效 | 是 | "小红书登录已失效，请重新扫码" |
| WF_003 | 工作流已被终止（不可恢复） | 否 | "工作流已终止" |
| WF_004 | 工作流挂起超时自动terminated | 否 | "任务长时间未响应已自动终止，可重新发起" |
| WF_005 | LangGraph checkpoint损坏 | 否 | "工作流状态异常，请联系管理员" |

### 9.2 节点级错误码

| 错误码 | 含义 | 可恢复 | 前端处理 |
|---|---|---|---|
| NODE_001 | 节点超时 | 是 | Recovery自动处理 |
| NODE_002 | 节点空返回 | 是 | Recovery自动处理 |
| NODE_003 | 输出Schema校验失败 | 是 | Recovery自动处理 |
| NODE_004 | LLM服务不可用（5xx） | 是 | 切备用模型 |
| NODE_005 | LLM限流（429） | 是 | 退避重试 |
| NODE_006 | MCP服务不可用 | 是 | 重试，仍失败则报错 |
| NODE_007 | 发布失败（小红书侧拒绝） | 否 | "发布被小红书拒绝，请检查内容合规性" |
| NODE_008 | Recovery耗尽-技术性恢复中 | 是 | 静默，显示"自动恢复中" |
| NODE_009 | Recovery耗尽-待用户决策 | 是 | 推送supervisor_suggestion |
| NODE_010 | Recovery耗尽-无法恢复 | 否 | "该步骤多次失败，建议重新发起工作流" |
| NODE_011 | 熔断器打开 | 是 | "智能体冷却中，稍后自动重试" |

### 9.3 操作错误码（HTTP响应）

| HTTP状态 | 错误码 | 含义 |
|---|---|---|
| 400 | INVALID_ACTION | 不支持的操作类型 |
| 409 | NODE_NOT_REVIEWABLE | 该节点不在待审核状态 |
| 409 | WORKFLOW_NOT_PAUSABLE | 工作流不在可暂停状态 |
| 409 | SUGGESTION_ALREADY_HANDLED | 该建议已被处理 |
| 410 | CHECKPOINT_EXPIRED | 检查点已过期或不存在 |
| 403 | NO_PERMISSION | 无权操作此工作流 |

---

## 10. 前端订阅规范

### 10.1 EventSource 封装（React 18 + TypeScript + Zustand）

```typescript
// stores/useSSEStore.ts
import { create } from 'zustand'
import { useWorkflowStore } from './useWorkflowStore'
import { useNodeStore } from './useNodeStore'
import { useReviewStore } from './useReviewStore'
import { useSuggestionStore } from './useSuggestionStore'
import { useCheckpointStore } from './useCheckpointStore'
import { useStreamStore } from './useStreamStore'
import { useTraceStore } from './useTraceStore'

type ConnectionStatus = 'connecting' | 'open' | 'closed'

interface SSEState {
  eventSource: EventSource | null
  events: SSEEvent<any>[]
  lastEventId: string | null
  connectionStatus: ConnectionStatus
  connect: (workflowId: string) => void
  disconnect: () => void
}

const EVENT_TYPES = [
  'workflow_started', 'workflow_snapshot', 'workflow_completed',
  'workflow_error', 'workflow_suspended', 'workflow_resumed',
  'workflow_paused',
  'node_started', 'node_status_changed', 'node_completed', 'node_error',
  'stream_chunk', 'image_generated',
  'review_required', 'review_processed',
  'supervisor_suggestion', 'recovery_attempt',
  'recovery_exhausted', 'circuit_open',
  'checkpoint_saved',
  // D16 过程透明化事件
  'tool_call_start', 'tool_call_end', 'progress_update',
  'model_switched', 'agent_thinking', 'decision_made',
  // D17 系统事件
  'search_degraded'
] as const

export const useSSEStore = create<SSEState>((set, get) => ({
  eventSource: null,
  events: [],
  lastEventId: null,
  connectionStatus: 'closed',

  connect: (workflowId: string) => {
    set({ connectionStatus: 'connecting' })
    const es = new EventSource(
      `/api/sse/workflow/${workflowId}`,
      { withCredentials: true }
    )

    es.onopen = () => set({ connectionStatus: 'open' })

    EVENT_TYPES.forEach(type => {
      es.addEventListener(type, (e: MessageEvent) => {
        const event: SSEEvent<any> = JSON.parse(e.data)
        set(state => ({
          lastEventId: event.event_id,
          events: [...state.events, event]
        }))
        dispatchEvent(type, event.payload)
      })
    })

    es.onerror = () => {
      set({ connectionStatus: 'closed' })
      // EventSource会自动重连，无需手动处理
      // 重连时浏览器自动携带Last-Event-ID
    }

    set({ eventSource: es })
  },

  disconnect: () => {
    const { eventSource } = get()
    eventSource?.close()
    set({ eventSource: null, connectionStatus: 'closed' })
  }
}))
```

### 10.2 事件分发到各Store

```typescript
// utils/dispatchEvent.ts
// Zustand的store通过 getState() 调用action，无需provider
import { useWorkflowStore } from '../stores/useWorkflowStore'
import { useNodeStore } from '../stores/useNodeStore'
import { useReviewStore } from '../stores/useReviewStore'
import { useSuggestionStore } from '../stores/useSuggestionStore'
import { useCheckpointStore } from '../stores/useCheckpointStore'
import { useStreamStore } from '../stores/useStreamStore'
import { useTraceStore } from '../stores/useTraceStore'

export function dispatchEvent(type: string, payload: any) {
  switch (type) {
    // 工作流级 → workflowStore
    case 'workflow_snapshot':
      useWorkflowStore.getState().applySnapshot(payload)
      break
    case 'workflow_suspended':
      useWorkflowStore.getState().markSuspended(payload)
      break

    // 节点级 → nodeStore
    case 'node_status_changed':
      useNodeStore.getState().updateNodeStatus(payload)
      break
    case 'node_error':
      useNodeStore.getState().markNodeError(payload)
      break

    // 流式 → streamStore
    case 'stream_chunk':
      useStreamStore.getState().appendChunk(payload)
      break
    case 'image_generated':
      useStreamStore.getState().addImage(payload)
      break

    // 审核 → reviewStore（同时更新待办计数器）
    case 'review_required':
      useReviewStore.getState().addPendingReview(payload)
      break
    case 'review_processed':
      useReviewStore.getState().removePendingReview(payload)
      break

    // 监督建议 → suggestionStore（同时更新待办计数器）
    case 'supervisor_suggestion':
      useSuggestionStore.getState().addPendingSuggestion(payload)
      break

    // 检查点 → checkpointStore（时光机）
    case 'checkpoint_saved':
      useCheckpointStore.getState().addCheckpoint(payload)
      break

    // D16 Agent过程 → traceStore（执行详情面板时间线）
    case 'tool_call_start':
      useTraceStore.getState().addToolCallStart(payload)
      break
    case 'tool_call_end':
      useTraceStore.getState().addToolCallEnd(payload)
      break
    case 'progress_update':
      useTraceStore.getState().updateProgress(payload)
      break
    case 'model_switched':
      useTraceStore.getState().addModelSwitch(payload)
      break
    case 'agent_thinking':
      // L2事件：始终接收缓存，是否渲染取决于粒度模式
      useTraceStore.getState().appendThinkingChunk(payload)
      break
    case 'decision_made':
      useTraceStore.getState().addDecision(payload)
      break

    // D17 系统事件 → workflowStore（显示降级横幅）
    case 'search_degraded':
      useWorkflowStore.getState().showDegradedBanner(payload)
      break
  }
}
```

### 10.3 React组件中使用

```tsx
// hooks/useWorkflowSSE.ts
import { useEffect } from 'react'
import { useSSEStore } from '../stores/useSSEStore'

export function useWorkflowSSE(workflowId: string | null) {
  const connect = useSSEStore(s => s.connect)
  const disconnect = useSSEStore(s => s.disconnect)
  const connectionStatus = useSSEStore(s => s.connectionStatus)

  useEffect(() => {
    if (!workflowId) return
    connect(workflowId)
    return () => disconnect()
  }, [workflowId, connect, disconnect])

  return { connectionStatus }
}

// 组件示例
function WorkflowCanvas({ workflowId }: { workflowId: string }) {
  const { connectionStatus } = useWorkflowSSE(workflowId)
  const nodes = useNodeStore(s => s.nodes)

  return (
    <div>
      {connectionStatus === 'connecting' && <ConnectionBanner status="connecting" />}
      <ReactFlow nodes={nodes} edges={edges} />
    </div>
  )
}
```

### 10.4 待办计数器（B+A混合方案的关键）

```typescript
// stores/useTodoStore.ts
import { create } from 'zustand'
import { useReviewStore } from './useReviewStore'
import { useSuggestionStore } from './useSuggestionStore'

interface TodoState {
  // 派生值：通过subscribe同步，避免每次渲染都计算
  todoCount: number
  recalc: () => void
}

export const useTodoStore = create<TodoState>((set) => ({
  todoCount: 0,
  recalc: () => {
    const reviews = useReviewStore.getState().pendingReviews
    const suggestions = useSuggestionStore.getState().pendingSuggestions
    set({ todoCount: reviews.length + suggestions.length })
  }
}))

// 在reviewStore和suggestionStore的action末尾调用 useTodoStore.getState().recalc()
// 组件中订阅：
function TodoBadge() {
  const count = useTodoStore(s => s.todoCount)
  return count > 0 ? <span className="badge">{count}</span> : null
}
```

---

## 11. 完整事件时序示例

### 11.1 正常流程时序

```
后端                                          前端
  │
  │  workflow_started                          │
  │ ─────────────────────────────────────────►│  渲染流程图全节点pending
  │
  │  node_started (search)                     │
  │ ─────────────────────────────────────────►│  search节点running+呼吸灯
  │
  │  checkpoint_saved (search)                 │
  │ ─────────────────────────────────────────►│  时光机新增刻度1
  │
  │  node_completed (search, passed)           │
  │ ─────────────────────────────────────────►│  search节点绿色对勾
  │
  │  node_started (analyze)                    │
  │ ─────────────────────────────────────────►│  analyze节点running
  │
  │  ...（分析含Qwen-VL调用，可能较慢）         │
  │
  │  node_completed (analyze, quality_pass=true)│
  │ ─────────────────────────────────────────►│  analyze绿色对勾
  │
  │  node_started (image_gen)                  │
  │ ─────────────────────────────────────────►│  image_gen running
  │
  │  image_generated (index=0)                 │
  │ ─────────────────────────────────────────►│  渲染第1张缩略图
  │  image_generated (index=1)                 │
  │ ─────────────────────────────────────────►│  渲染第2张
  │
  │  review_required (image_review)            │
  │ ─────────────────────────────────────────►│  节点变awaiting_review+铃铛
  │                                            │  +待办计数器+1
  │                                            │  +节点自动展开审核卡片
  │
  │  ◄─────────────────────────────────────────│  POST /review (action=pass)
  │
  │  review_processed                          │
  │ ─────────────────────────────────────────►│  待办计数器-1
  │
  │  node_started (copywrite)                  │
  │ ─────────────────────────────────────────►│  copywrite running
  │
  │  stream_chunk (title, "今")                │
  │ ─────────────────────────────────────────►│  逐字渲染title
  │  stream_chunk (title, "天")                │
  │ ─────────────────────────────────────────►│
  │  ...                                       │
  │  stream_chunk (body, is_final)             │
  │ ─────────────────────────────────────────►│  body完成
  │
  │  node_completed (copywrite)                │
  │ ─────────────────────────────────────────►│  copywrite对勾
  │
  │  ...（audit → final_review → publish）     │
  │
  │  workflow_completed                        │
  │ ─────────────────────────────────────────►│  全流程完成+发布链接
```

### 11.2 崩溃-结构性恢复时序

```
后端                                          前端
  │
  │  node_started (analyze, attempt=0)         │
  │ ─────────────────────────────────────────►│
  │
  │  node_error (crash_reason=input_insufficient)│
  │ ─────────────────────────────────────────►│  analyze变红
  │
  │  recovery_attempt (attempt=1, retry)       │
  │ ─────────────────────────────────────────►│  节点显示"重试中(1/3)"
  │  node_started (attempt=1)                  │
  │ ─────────────────────────────────────────►│
  │  node_error (again)                        │
  │ ─────────────────────────────────────────►│
  │
  │  recovery_attempt (attempt=2, reduce_input)│
  │ ─────────────────────────────────────────►│  "重试中(2/3) 减少输入"
  │  node_started (attempt=2)                  │
  │ ─────────────────────────────────────────►│
  │  node_error (again)                        │
  │ ─────────────────────────────────────────►│
  │
  │  recovery_attempt (attempt=3, switch_model)│
  │ ─────────────────────────────────────────►│  "重试中(3/3) 切模型"
  │  node_error (again)                        │
  │ ─────────────────────────────────────────►│
  │
  │  recovery_exhausted (escalated=true)       │
  │ ─────────────────────────────────────────►│  节点显示"已上报监督"
  │
  │  supervisor_suggestion                     │
  │    (rollback to search,                    │
  │     trace=[search, analyze],               │
  │     timeout=1800)                          │
  │ ─────────────────────────────────────────►│  节点旁气泡+溯源链
  │                                            │  +待办计数器+1
  │                                            │  +30分钟倒计时
  │
  │  ◄─────────────────────────────────────────│  POST /confirm-rollback
  │
  │  review_processed (reject, rollback)       │
  │ ─────────────────────────────────────────►│  待办-1
  │
  │  node_status_changed (search, running)     │
  │ ─────────────────────────────────────────►│  search重新running
  │  ...（正常流程继续）                         │
```

### 11.3 30分钟超时挂起时序

```
后端                                          前端
  │
  │  supervisor_suggestion (timeout=1800)      │
  │ ─────────────────────────────────────────►│  30分钟倒计时
  │
  │  （30分钟无用户操作）                         │
  │
  │  workflow_suspended                        │
  │ ─────────────────────────────────────────►│  全流程变灰
  │                                            │  顶部横幅"任务已挂起"
  │                                            │  +手动恢复按钮
  │
  │  （1小时后定时检查）                          │
  │
  │  workflow_resumed (auto_dependency_recovered)│
  │ ─────────────────────────────────────────►│  重新进入待确认
  │                                            │  +新的30分钟倒计时
  │
  │  ◄─────────────────────────────────────────│  POST /confirm-rollback
  │  ...（继续）                                 │
```

---

## 12. 协议约定汇总

### 12.1 时间单位
- 所有时间戳：ISO8601字符串（UTC）
- 所有时长：秒（整数）或毫秒（duration_ms字段）

### 12.2 ID规范
- workflow_id: `wf_` + ULID
- node_id: `node_` + ULID
- event_id: `evt_` + ULID
- review_id: `rev_` + ULID
- suggestion_id: `sug_` + ULID
- checkpoint_id: `ckpt_` + ULID

### 12.3 SSE连接生命周期
- 工作流启动时前端建立SSE连接
- 工作流完成/终止后，后端推送最后事件后保持连接30秒再关闭（防止前端丢失最后事件）
- 前端离开工作流页面时主动close

### 12.4 幂等性
- 所有POST操作支持幂等（通过request_id去重）
- 前端每次POST携带 `X-Request-Id` header
- 后端对相同request_id的请求返回首次结果

### 12.5 事件顺序保证
- 单个workflow的SSE连接内，事件严格有序（FIFO）
- 后端按事件产生顺序推送，不重排
- 断线重连时，从 `Last-Event-ID` 之后严格续传，不漏不重

---

**文档结束。本协议是前端组件设计的契约前置，前端开发严格按此实现。**
