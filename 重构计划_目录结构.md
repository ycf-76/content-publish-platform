# 重构计划：后端目录结构整理

> 目标：解开 `agents/` 巨型包，按职责分层，消除循环依赖
> 原则：每阶段独立可验证，每阶段完 git commit，可逐阶段回滚
> 日期：2026-08-21

---

## 一、现状问题

### 1.1 目录结构

```
app/
  agents/           ← 巨型上帝包，塞了所有东西
    adapters/       ← LLM 适配（基础设施，不是编排）
    configs/        ← YAML 配置（数据）
    core/           ← harness/executor/recovery（运行时基础设施）
    harnesses/      ← 和 core/harness 重复
    nodes/          ← DAG 节点（编排逻辑 ✅）
    prompts/        ← 提示词（数据）
    schemas/        ← 数据结构
    skills/         ← 工具层（不是编排！）
      mcp/          ← 外部协议客户端
      sources/      ← 数据源
  api/              ← 路由层 ✅
  cache/            ← 基础设施 ✅
  core/             ← 又一个 core？和 agents/core 重复 ❌
  crypto/           ← 基础设施 ✅
  db/               ← 基础设施 ✅
  ...
```

### 1.2 五个核心问题

1. **`agents/` 是巨型包** — 编排、基础设施、工具、数据、配置全塞一起
2. **`agents/skills/` 层级错** — 工具层不该从属于编排层
3. **两个 `core/`** — `agents/core/` 和 `app/core/` 职责重叠
4. **`agents/harnesses/` vs `agents/core/harness/`** — 同一个东西拆两个地方
5. **数据文件混在代码里** — configs/、prompts/、schemas/ 散落 agents 下

### 1.3 循环依赖（最致命）

```
core/harness/runtime.py       ──→  skills/base.py
core/harness/runtime.py       ──→  skills/permissions.py
core/harness/executor/loop.py ──→  skills/permissions.py     ← 顶层 import
core/harness/recovery/supervisor.py ──→  adapters/llm_base.py
skills/permissions.py         ──→  core/schemas.py            ← 反向依赖
```

**core ↔ skills 是双向循环**，core 里有 2 处（runtime + loop）依赖 skills。

---

## 二、目标结构

```
app/
  api/                ← 接口层（路由、schema、deps）
  services/           ← 业务服务（workflow、chat_session、sse...）
  agents/             ← 编排层（只放编排逻辑）
    __init__.py
    graph.py            DAG 定义
    registry.py         Agent 注册
    chat_agent.py       对话 Agent
    intent_parser.py    意图解析
    input_rules.py      输入规则
    prompt_assembler.py
    context.py
    state.py
    nodes/              DAG 节点
    configs/            YAML 配置
    prompts/            提示词模板
    schemas/            节点输出 schema
  tools/              ← 工具层（原 agents/skills/，平级提升）
    __init__.py
    base.py
    registry.py
    permissions.py
    copywrite_builder.py
    trending_search.py
    viral_analyzer.py
    analyze_layer.py
    analyze_skill.py
    audit_skill.py
    blueprint_skill.py
    dev_tools.py
    xhs_search.py
    xhs_publish.py
    vl_analyze.py
    mcp/              ← 外部协议客户端
    sources/          ← 数据源
  adapters/           ← 适配层（原 agents/adapters/，平级提升）
    __init__.py
    llm_base.py
    deepseek.py
    qwen_vl.py
    image_gen.py
    llm/              ← LLM 子包
  engine/             ← 运行时引擎（原 agents/core/ + agents/harnesses/ 合并）
    __init__.py
    schemas.py          WorkflowContext, LLMProtocol, Permission, PermissionDeniedError 等
    harness/
      runtime.py
      executor/
        base.py
        loop.py
        single_shot.py
      memory/
        memory.py
      observer/
        observer.py
      recovery/
        backoff.py
        circuit_breaker.py
        loop.py
        strategies.py
        supervisor.py
    factory.py          ← 原 agents/harnesses/factory.py
  core/               ← 全局基础设施（event_bus、node_registry、plugin_manager）
  db/                 ← 数据层
  cache/              ← 缓存
  crypto/             ← 加密
  account/            ← 账户模块
  pool_monitor/       ← 池监控
  rpa/                ← RPA
  config.py
  security.py
  main.py
```

### 依赖方向（单向，不回头）

```
api → services → agents → tools → adapters
                      → engine → (tools, adapters)  ← 阶段1解环后 engine 不再依赖 tools
```

---

## 三、阶段 1：解开循环依赖

> 不改目录，只改 import 逻辑。目标：让 `agents/core` 不再反向依赖 `agents/skills` 和 `agents/adapters`。

### 3.1 把 Permission 和 PermissionDeniedError 提升到 core/schemas.py

**原因**：`Permission`（枚举）和 `PermissionDeniedError`（异常类）是零依赖的纯定义，但当前放在 `skills/permissions.py` 里，导致 `core/executor/loop.py` 必须顶层 import skills 才能拿到异常类。

**操作**：

| 步骤 | 文件 | 改动 |
|------|------|------|
| 1 | `agents/core/schemas.py` | 加入 `Permission` 枚举和 `PermissionDeniedError` 异常类（从 permissions.py 搬过来） |
| 2 | `agents/skills/permissions.py` | 删除 `Permission` 和 `PermissionDeniedError` 定义，改为 `from app.agents.core.schemas import Permission, PermissionDeniedError`；`permission_gate` 里的 WorkflowContext 保持延迟 import |
| 3 | `agents/skills/base.py` | `from app.agents.skills.permissions import Permission` → `from app.agents.core.schemas import Permission` |
| 4 | `agents/core/harness/executor/loop.py` | 顶层 `from app.agents.skills.permissions import PermissionDeniedError, permission_gate` → `from app.agents.core.schemas import PermissionDeniedError`；`permission_gate` 改为函数内延迟 import |
| 5 | `agents/core/harness/runtime.py` | `from app.agents.skills.base import Skill` 和 `from app.agents.skills.permissions import ...` → 全部改为函数内延迟 import |
| 6 | 所有其他引用 `Permission` / `PermissionDeniedError` 的文件 | 改为从 `app.agents.core.schemas` 导入 |

**涉及文件清单**（需逐一确认）：

```
agents/core/schemas.py                    ← 加入定义
agents/skills/permissions.py              ← 删定义，改 import
agents/skills/base.py                     ← 改 import 来源
agents/skills/dev_tools.py                ← 改 import
agents/skills/xhs_search.py               ← 改 import
agents/skills/xhs_publish.py              ← 改 import
agents/skills/trending_search.py          ← 改 import
agents/core/harness/executor/loop.py      ← 顶层改延迟
agents/core/harness/runtime.py            ← 改延迟
agents/harnesses/factory.py               ← 确认是否引用 Permission
tests/test_permissions.py                 ← 改 import
tests/test_skill_execution_policy.py      ← 改 import
```

### 3.2 解开 runtime.py → skills/base.Skill

**操作**：`runtime.py` 第 28 行 `from app.agents.skills.base import Skill` 改为函数内延迟 import。

**风险**：低。Skill 只在 `AgentHarness.__init__` 的类型标注和运行时使用，延迟 import 不影响功能。

### 3.3 解开 supervisor.py → adapters/llm_base.BaseLLM

**操作**：`supervisor.py` 第 25 行已经是延迟 import（在函数内），确认无需改动。

### 3.4 阶段 1 验证

```bash
pytest tests/ -q                    # 全绿
python -m app.main                  # 后端启动无 import 错误
curl http://127.0.0.1:8000/api/skills  # 接口正常
```

### 3.5 阶段 1 commit

```bash
git add -A && git commit -m "refactor(phase1): 解开 core ↔ skills 循环依赖，Permission/PermissionDeniedError 提升到 schemas"
```

---

## 四、阶段 2：移动目录

> 循环解开后，依赖方向单向，可以安全移动目录。

### 4.1 移动操作

| 序号 | 操作 | 命令 |
|------|------|------|
| 1 | skills → tools | `git mv app/agents/skills app/tools` |
| 2 | adapters 提升 | `git mv app/agents/adapters app/adapters` |
| 3 | core → engine | `git mv app/agents/core app/engine` |
| 4 | harnesses 合并到 engine | `git mv app/agents/harnesses/factory.py app/engine/factory.py`；删除 `app/agents/harnesses/` |
| 5 | 更新所有 `__init__.py` | 修改 re-export 路径 |

### 4.2 import 路径替换表

| 旧路径 | 新路径 | 预估影响文件数 |
|--------|--------|---------------|
| `app.agents.skills.` | `app.tools.` | ~30 文件 |
| `app.agents.adapters.` | `app.adapters.` | ~15 文件 |
| `app.agents.core.` | `app.engine.` | ~20 文件 |
| `app.agents.harnesses.factory` | `app.engine.factory` | ~8 文件 |
| `app.agents.harnesses.` | `app.engine.` | ~5 文件 |

### 4.3 特殊替换（字符串形式模块路径）

| 文件 | 内容 | 替换 |
|------|------|------|
| `app/tools/registry.py` 第 48-55 行 | `BUILTIN_SKILL_MODULES` 元组 7 个字符串 | `app.agents.skills.X` → `app.tools.X` |
| `app/agents/nodes/audit.py` 第 38 行 | `import app.agents.skills.audit_skill` | `import app.tools.audit_skill` |
| `app/agents/nodes/copywrite.py` 第 124 行 | `import app.agents.skills.copywrite_builder` | `import app.tools.copywrite_builder` |
| `app/agents/nodes/analyze.py` 第 21 行 | `import app.agents.skills.analyze_skill` | `import app.tools.analyze_skill` |
| `tests/test_chat_agent.py` 4 处 | `patch("app.agents.skills.X")` | `patch("app.tools.X")` |
| `skills/example_third_party_skills.py` | `from app.agents.skills.` | `from app.tools.` |
| `frontend/.../SkillsManager.vue` 第 26-27 行 | 开发指南示例代码 | `app.agents.skills.` → `app.tools.` |

### 4.4 `__init__.py` 更新

| 文件 | 改动 |
|------|------|
| `app/tools/__init__.py` | 原 `agents/skills/__init__.py` 内容，import 路径更新 |
| `app/adapters/__init__.py` | 原 `agents/adapters/__init__.py` 内容，import 路径更新 |
| `app/engine/__init__.py` | 原 `agents/core/__init__.py` 内容，import 路径更新 |
| `app/agents/__init__.py` | 删除 skills/adapters/core 的 re-export |
| `app/agents/nodes/__init__.py` | 更新内部引用 |

### 4.5 阶段 2 验证

```bash
pytest tests/ -q                    # 全绿
python -m app.main                  # 后端启动无 import 错误
curl http://127.0.0.1:8000/api/skills  # 接口正常
# 手动跑一次搜索→分析→文案 链路
```

### 4.6 阶段 2 commit

```bash
git add -A && git commit -m "refactor(phase2): 目录结构整理 - skills→tools, adapters提升, core→engine, harnesses合并"
```

---

## 五、阶段 3：收尾清理

### 5.1 清理残留

| 操作 | 说明 |
|------|------|
| 删除 `app/agents/harnesses/` 目录 | 已合并到 engine |
| 清理 `__pycache__` | 所有移动过的目录 |
| 检查 `agents/__init__.py` | 确保不再 re-export 已移走的模块 |
| 检查 `agents/graph.py` 注释 | 更新文档字符串里的模块路径引用 |

### 5.2 更新文档/注释

- `agents/graph.py` 第 10 行注释：`app.agents.nodes` → 不变（nodes 留在 agents 下）
- `agents/nodes/_base.py` 第 219 行注释：`app.agents.harnesses.factory` → `app.engine.factory`
- `agents/nodes/__init__.py` 第 4 行注释：更新向后兼容说明

### 5.3 阶段 3 验证

```bash
pytest tests/ -q                    # 全绿
python -m app.main                  # 后端启动
# 完整 E2E：搜索 → 分析 → 文案 → 图片 → 审核 → 发布
# 前端设置页 Skills 技能列表正常加载
# 第三方 Skill 注册/注销正常
```

### 5.4 阶段 3 commit + push

```bash
git add -A && git commit -m "refactor(phase3): 收尾清理 - 删除残留目录、更新注释、E2E验证通过"
git push
```

---

## 六、风险控制

| 风险 | 应对 |
|------|------|
| 循环依赖解不开 | 阶段 1 不改目录，只改 import，可单独回滚 |
| 移动目录后 import 漏改 | 用 grep 全量扫描 `app.agents.skills` / `app.agents.core` / `app.agents.adapters`，确保零残留 |
| 字符串模块路径漏改 | 专门列出字符串引用清单（4.3 节），逐一替换 |
| `__init__.py` re-export 链断裂 | 每次移动后立即 `python -c "import app.tools"` 验证 |
| pytest 失败 | 每阶段 commit 前必须全绿，失败则回滚到上一 commit |
| 后端启动失败 | 每阶段 commit 后重启后端验证 |

---

## 七、预估工作量

| 阶段 | 改动文件数 | 风险 | 预估时间 |
|------|-----------|------|---------|
| 阶段 1：解循环 | ~12 文件 | 中（改 import 逻辑） | 20 分钟 |
| 阶段 2：移目录 | ~50 文件 | 低（机械替换） | 30 分钟 |
| 阶段 3：收尾 | ~5 文件 | 低 | 10 分钟 |
| **合计** | **~55 文件** | | **~60 分钟** |

---

## 八、执行顺序确认

- [ ] 用户确认计划
- [ ] 阶段 1：解开循环依赖
- [ ] 阶段 1 验证 + commit
- [ ] 阶段 2：移动目录
- [ ] 阶段 2 验证 + commit
- [ ] 阶段 3：收尾清理
- [ ] 阶段 3 验证 + commit + push