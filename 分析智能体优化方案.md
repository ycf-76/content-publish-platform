# 分析智能体优化方案

> 创建时间：2026-07-29
> 关联文档：搜索节点调试与增强记录.md
> 核心主线：三层分层分析架构 + 发布回看反馈闭环

---

## 一、背景与问题

### 1.1 用户洞察

爆款内容有三种截然不同的来源，当前实现无法区分：

| 爆款类型 | 特征 | 对内容生产的参考价值 | 误判后果 |
|---------|------|---------------------|---------|
| **粉丝型爆款** | 大V发布，粉丝惯性流量推高 | 价值低——学不会他的粉丝量 | 把大V日常水文当热点选题，抄了个寂寞 |
| **内容型爆款** | 小博主发布，内容本身引爆 | 价值极高——可复制的选题/钩子/结构 | 错过真正的可复制爆款 |
| **话题型爆款** | 多个不同量级作者同时爆款 | 价值最高——真趋势信号 | 混在流量噪音里被忽略 |

### 1.2 当前实现的三层盲区

1. **analyze 节点是空壳**：[graph.py:230-250](file:///d:\My_Project\多智能体小红书发布平台\backend\app\agents\graph.py#L230-L250) 的 `analyze_node` 只发 `node_completed` 事件，`output = {"factors": {}}`，根本没接 harness
2. **search 数据缺关键字段**：[qr_http_worker.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\account\qr_http_worker.py) 的搜索 JS 只抓了 `author` 名字，没抓 `author_fans`，LLM 没有判断爆款来源的依据
3. **只按 likes 绝对值排序**：9374 赞的首条可能是百万粉大V的日常水文，参考价值不如 2000 赞但作者只有 500 粉的内容型爆款

### 1.3 核心设计原则

- **承认 LLM 和规则各有擅长**：规则擅长算指标、打标签、保证字段完整；LLM 擅长模式识别、归因、跨笔记发现趋势
- **分层调用控成本**：不是每条笔记都值得 LLM 深度分析
- **反馈闭环让模型进化**：发布后数据回看，校准分析权重，越用越准

---

## 二、核心架构：三层分层分析 + 反馈闭环

### 2.1 架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│  search 节点输出：top 20 笔记（含 author_fans 等字段）           │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1: 规则层（在 XhsSearchSkill 内，0 LLM 成本）             │
│                                                                  │
│  ① 计算派生指标                                                  │
│     - 互动率 interaction_rate = likes / max(fans, 1)             │
│     - 爆点系数 viral_coefficient = likes / (fans^0.5 + 1)        │
│     - 互动质量 quality_score = comments / max(likes, 1)          │
│                                                                  │
│  ② 基于分位数的相对分类                                          │
│     - 用样本 P75 分位数划分 粉丝型/内容型/双重型/普通             │
│     - 自适应不同类目（穿搭 vs 知识 vs 美食）                     │
│                                                                  │
│  ③ 综合爆点分排序                                                │
│     - viral_score = 0.5*rate_percentile + 0.3*viral + 0.2*likes  │
│     - 内容型爆款优先，粉丝型噪音下沉                             │
│                                                                  │
│  输出：带 viral_type 和 viral_score 的 top 20 笔记               │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 2: LLM 粗分析（只对 top 5，1 次 LLM 调用）                │
│                                                                  │
│  分析 top 5 笔记的：                                             │
│  ① 标题钩子模式（数字型/反差型/痛点型/好奇型）                   │
│  ② 内容结构骨架（场景→痛点→方案→证据→召唤）                      │
│  ③ 情绪触发点（焦虑/认同/好奇/对比）                             │
│                                                                  │
│  输出：可复制的模式总结（不碰 results 字段，只产出分析结论）      │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 3: LLM 深度归因（只对 top 2，1 次 LLM 调用）              │
│                                                                  │
│  ① 跨笔记趋势信号发现                                            │
│     - 多个小博主同时用相似结构爆款 = 话题型趋势                  │
│  ② 选题方向 + 内容骨架建议                                       │
│     - 带可解释证据链（为什么推荐这个方向）                       │
│  ③ 排除粉丝型噪音                                                │
│     - 明确标注哪些是"大V日常，不参考"                            │
│                                                                  │
│  输出：analyze_node 的最终 output                                │
│     {                                                            │
│       "results": <Layer 1 的 top 20, 字段完整>,                  │
│       "patterns": <Layer 2 的模式总结>,                          │
│       "insights": <Layer 3 的趋势归因 + 选题建议>,               │
│       "filter_stats": {...}                                      │
│     }                                                            │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
                    下游节点消费
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  反馈闭环：发布回看（T+7 天）                                    │
│                                                                  │
│  本次产出内容 → 发布 → 7天后采集数据 → 回填分析模型              │
│     ↓                                                            │
│  - 哪些"内容型爆款"特征真的复制成功了？→ 强化该特征权重          │
│  - 哪些是噪音？→ 降低该特征权重                                  │
│  - 校准 viral_score 的三个系数（0.5/0.3/0.2）                    │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 三层分工的核心逻辑

| 层 | 执行者 | 输入 | 输出 | LLM 调用 | 延迟 |
|----|-------|------|------|---------|------|
| Layer 1 | 规则（Python） | top 20 原始笔记 | 带标签和分数的 top 20 | 0 | <100ms |
| Layer 2 | LLM（DeepSeek） | top 5 的 title+summary | 模式总结 | 1 次 | ~3s |
| Layer 3 | LLM（DeepSeek） | top 2 + Layer 2 结论 | 趋势归因 + 选题建议 | 1 次 | ~5s |
| **合计** | | | | **2 次** | **~8s** |

对比旧方案（对 20 条每条都 LLM 分析）：20 次调用，延迟 60s+，**成本降 90%**。

---

## 三、Layer 1：规则层（0 LLM 成本）

### 3.1 最小数据集（前置依赖）

Layer 1 的指标计算需要 search 节点返回以下字段：

| 字段 | 来源 | 抓取代价 | 用途 |
|------|------|---------|------|
| `author_fans` | 搜索列表页作者卡片 | 低 | 互动率、爆点系数的核心分母 |
| `published_at` | 搜索列表页时间戳 | 低 | 时间衰减、热度斜率 |
| `collects` | 详情页 | 中 | 收藏率（内容实用性的核心信号） |
| `note_type` | 列表页图标 | 低 | 图文/视频分流分析 |
| `tags` | 列表页标签 | 低 | 跨笔记标签重合度=话题趋势信号 |

**实施优先级**：`author_fans` 和 `published_at` 是 P0（必须），其余 P1（可后续补）。

### 3.2 派生指标计算

```python
import math

def compute_metrics(note: dict) -> dict:
    """计算单条笔记的派生指标。所有指标在 skill 层算好，不依赖 LLM。"""
    likes = max(note.get("likes", 0), 0)
    comments = max(note.get("comments", 0), 0)
    fans = max(note.get("author_fans", 0), 1)  # 防除零
    collects = max(note.get("collects", 0), 0)
    
    # 互动率：内容本身引爆的核心信号
    # 大V日常：100万粉 / 5000赞 → 0.5%
    # 小博主爆款：500粉 / 2000赞 → 400%
    interaction_rate = likes / fans
    
    # 爆点系数：幂律校正，消除粉丝量杠杆
    # 让 100粉 2000赞 和 100万粉 5万赞 的爆点系数可比
    viral_coefficient = likes / (math.sqrt(fans) + 1)
    
    # 互动质量分：评论/点赞比，衡量内容深度
    # 高评论率=引发思考/争议，低评论率=单纯情绪转发
    quality_score = comments / max(likes, 1)
    
    # 收藏率：内容实用性的核心信号（有 collects 时才算）
    # 点赞容易，收藏需要内容真的有用
    collect_rate = collects / max(likes, 1) if collects else None
    
    return {
        **note,
        "interaction_rate": round(interaction_rate, 4),
        "viral_coefficient": round(viral_coefficient, 2),
        "quality_score": round(quality_score, 4),
        "collect_rate": round(collect_rate, 4) if collect_rate else None,
    }
```

### 3.3 基于分位数的相对分类

**核心改进**：不依赖硬阈值（50000/5000），用样本分布的分位数自适应分类。

```python
def classify_viral_type(notes: list[dict]) -> list[dict]:
    """基于样本分布的相对分类，自适应不同类目。
    
    穿搭类的"大V"和知识类的"大V"标准不同，
    分位数会根据本次搜索的实际样本自动校准。
    """
    if not notes:
        return notes
    
    # 提取样本
    fans_list = [n.get("author_fans", 0) for n in notes]
    rate_list = [n["interaction_rate"] for n in notes]
    
    # 计算分位数
    fans_p75 = _percentile(fans_list, 75)
    rate_p75 = _percentile(rate_list, 75)
    
    # 分类
    for n in notes:
        is_big_author = n.get("author_fans", 0) > fans_p75
        is_high_rate = n["interaction_rate"] > rate_p75
        
        if is_big_author and not is_high_rate:
            n["viral_type"] = "粉丝型"  # 大V但互动率低于样本P75
        elif not is_big_author and is_high_rate:
            n["viral_type"] = "内容型"  # 小作者但互动率高于样本P75
        elif is_big_author and is_high_rate:
            n["viral_type"] = "双重型"  # 大V + 内容也好
        else:
            n["viral_type"] = "普通"
    
    return notes


def _percentile(data: list[float], p: int) -> float:
    """简单分位数计算（不依赖 numpy）。"""
    if not data:
        return 0.0
    sorted_data = sorted(data)
    k = (len(sorted_data) - 1) * p / 100
    f = int(k)
    c = k - f
    if f + 1 < len(sorted_data):
        return sorted_data[f] + c * (sorted_data[f + 1] - sorted_data[f])
    return sorted_data[f]
```

**优势**：
- 穿搭类样本粉丝量普遍高，P75 自动升高，不会误判
- 知识类样本粉丝量普遍低，P75 自动降低，小博主也能被识别为"内容型"
- **完全自适应，零硬编码阈值**

### 3.4 综合爆点分排序

```python
def compute_viral_score(notes: list[dict]) -> list[dict]:
    """综合爆点分，优先内容型爆款，粉丝型噪音下沉。"""
    if not notes:
        return notes
    
    # 计算各指标的分位数（用于归一化）
    rates = [n["interaction_rate"] for n in notes]
    virals = [n["viral_coefficient"] for n in notes]
    likes_list = [n.get("likes", 0) for n in notes]
    
    for n in notes:
        # 归一化到 0-1（用分位数，避免极值拉偏）
        rate_p = _percentile_rank(rates, n["interaction_rate"])
        viral_p = _percentile_rank(virals, n["viral_coefficient"])
        likes_p = _percentile_rank(likes_list, n.get("likes", 0))
        
        # 加权：互动率权重最高（内容引爆信号）
        # 爆点系数次之（消除粉丝杠杆）
        # 绝对点赞数兜底（不漏真大爆款）
        n["viral_score"] = round(
            rate_p * 0.5 + viral_p * 0.3 + likes_p * 0.2,
            4
        )
    
    # 按 viral_score 降序
    notes.sort(key=lambda n: n["viral_score"], reverse=True)
    return notes


def _percentile_rank(data: list[float], value: float) -> float:
    """返回 value 在 data 中的分位数排名（0-1）。"""
    if not data:
        return 0.0
    below = sum(1 for x in data if x <= value)
    return below / len(data)
```

---

## 四、Layer 2：LLM 粗分析（top 5，1 次调用）

### 4.1 职责边界

**LLM 做什么**：模式识别——从 top 5 的 title+summary 里识别可复制的模式
**LLM 不做什么**：不碰 results 字段，不搬数据，只产出分析结论

### 4.2 三个分析任务

```python
_LAYER2_PROMPT = """你需要分析以下 5 条小红书爆款笔记，找出可复制的模式。

笔记数据（已按爆点分排序，含分类标签）：
{top5_json}

请分析以下三个方面，输出 JSON：

1. title_patterns：标题钩子模式
   - 识别每条笔记的标题类型（数字型/反差型/痛点型/好奇型/利益型/故事型）
   - 总结最常见的 2-3 种钩子模式
   - 每种模式给出 1 个可复制的标题模板

2. content_structures：内容结构骨架
   - 从 summary 推断内容结构（场景引入→痛点→解决方案→证据→行动召唤）
   - 总结最有效的 1-2 种结构

3. emotion_triggers：情绪触发点
   - 识别每条笔记的核心情绪（焦虑/认同/好奇/对比/归属/成就）
   - 总结哪种情绪在该主题下最容易引爆

输出格式：
{{
  "title_patterns": [
    {{"type": "数字型", "template": "X个方法让你...", "examples": ["..."]}},
    ...
  ],
  "content_structures": [
    {{"structure": "场景→痛点→方案→证据→召唤", "description": "..."}},
    ...
  ],
  "emotion_triggers": [
    {{"emotion": "焦虑", "reason": "..."}},
    ...
  ]
}}

重要：只输出分析结论，不要复述笔记原文。
"""
```

### 4.3 与 Layer 1 的衔接

```python
# 在 analyze_node 或 AnalyzeHarness 里
top5 = state["node_outputs"]["search"]["results"][:5]
layer2_output = await llm.call(_LAYER2_PROMPT.format(top5_json=json.dumps(top5, ensure_ascii=False)))
```

---

## 五、Layer 3：LLM 深度归因（top 2，1 次调用）

### 5.1 职责边界

**LLM 做什么**：跨笔记趋势发现 + 选题方向建议（带证据链）
**LLM 不做什么**：不重新分析标题/结构（Layer 2 已做），只做归因和建议

### 5.2 两个核心任务

```python
_LAYER3_PROMPT = """基于以下分析结果，做深度归因和选题建议。

主题：{topic}

Layer 1 规则层输出（top 20，含 viral_type 分类）：
{layer1_summary}

Layer 2 LLM 粗分析输出（模式总结）：
{layer2_output}

请完成以下两个任务，输出 JSON：

1. trend_signals：跨笔记趋势信号
   - 检查是否有"多个小博主（粉丝型=false）同时用相似标题结构或内容结构爆款"
   - 如果有，说明这是话题型趋势（真趋势信号）
   - 明确标注哪些笔记是"大V日常，不参考"（粉丝型）
   - 给出趋势强度评估（强/中/弱）和依据

2. recommendations：选题方向 + 内容骨架建议
   - 基于 Layer 2 的模式总结 + Layer 3 的趋势信号
   - 给出 2-3 个可复制的选题方向
   - 每个方向附带：推荐标题模板 + 内容结构 + 情绪钩子 + 参考笔记 note_id
   - 必须带证据链：为什么推荐这个方向？哪些笔记是证据？

输出格式：
{{
  "trend_signals": {{
    "is_topic_trend": true/false,
    "trend_strength": "强/中/弱",
    "trend_basis": "依据说明",
    "noise_notes": ["note_id1", "note_id2"]  // 粉丝型噪音，不参考
  }},
  "recommendations": [
    {{
      "topic_direction": "...",
      "title_template": "...",
      "content_structure": "...",
      "emotion_hook": "...",
      "evidence_note_ids": ["note_id1", "note_id2"],
      "reason": "为什么推荐这个方向"
    }}
  ]
}}

重要：recommendations 必须带证据链（evidence_note_ids + reason），不要给无依据的建议。
"""
```

### 4.3 analyze_node 最终输出组装

```python
async def analyze_node(state: WorkflowState) -> dict:
    """分析节点：三层分层分析。"""
    workflow_id = state["workflow_id"]
    node_id = "analyze"
    
    await emit_node_event(workflow_id, node_id, "node_started")
    await emit_node_event(workflow_id, node_id, "node_status_changed", {"status": "running"})
    
    search_output = state["node_outputs"].get("search", {})
    raw_results = search_output.get("results", [])
    
    try:
        # Layer 1: 规则层（0 LLM 成本）
        with_metrics = [compute_metrics(n) for n in raw_results]
        with_metrics = classify_viral_type(with_metrics)
        with_metrics = compute_viral_score(with_metrics)
        
        # Layer 2: LLM 粗分析（top 5）
        top5 = with_metrics[:5]
        layer2_output = await _run_layer2_analysis(top5, state["topic"])
        
        # Layer 3: LLM 深度归因（top 2 + Layer 2 结论）
        top2 = with_metrics[:2]
        layer3_output = await _run_layer3_attribution(
            top2, with_metrics, layer2_output, state["topic"]
        )
        
        output = {
            "results": with_metrics,           # Layer 1 完整数据（字段完整）
            "patterns": layer2_output,          # Layer 2 模式总结
            "insights": layer3_output,          # Layer 3 趋势归因 + 选题建议
            "filter_stats": search_output.get("filter_stats", {}),
        }
        await emit_node_event(workflow_id, node_id, "node_completed", output)
        
    except Exception as e:
        logger.exception(f"[{workflow_id}] analyze_node failed: {e}")
        output = {"results": raw_results, "patterns": {}, "insights": {}, "error": str(e)}
        await emit_node_event(workflow_id, node_id, "node_completed", output)
    
    return {
        "current_node": node_id,
        "node_statuses": {node_id: NodeStatus.COMPLETED.value},
        "node_outputs": {node_id: output},
    }
```

---

## 六、反馈闭环：发布回看机制

### 6.1 核心逻辑

```
本次分析 → 产出内容 → 发布 → 7天后采集数据 → 回填分析模型
                                      ↓
                              校准 viral_score 的权重（0.5/0.3/0.2）
                              - 哪些"内容型爆款"特征真的复制成功了？→ 强化
                              - 哪些是噪音？→ 降低
```

### 6.2 数据表设计

```sql
-- 发布内容的表现记录（T+7 回采）
CREATE TABLE published_content_performance (
    id BIGSERIAL PRIMARY KEY,
    workflow_id VARCHAR(64) NOT NULL,           -- 关联工作流
    published_note_id VARCHAR(64),              -- 发布后的小红书 note_id
    topic VARCHAR(255),                         -- 选题主题
    selected_pattern JSONB,                     -- 采用了哪个 Layer 2 模式
    selected_direction JSONB,                   -- 采用了哪个 Layer 3 选题方向
    published_at TIMESTAMP,                     -- 发布时间
    
    -- T+7 回采的数据
    collected_likes INT DEFAULT 0,
    collected_collects INT DEFAULT 0,
    collected_comments INT DEFAULT 0,
    collected_shares INT DEFAULT 0,
    collected_at TIMESTAMP,                     -- 回采时间
    
    -- 派生指标
    performance_score FLOAT,                    -- 综合表现分（用同样公式算）
    is_replicated BOOLEAN,                      -- 是否成功复制了爆款特征
    
    created_at TIMESTAMP DEFAULT NOW()
);

-- 分析权重的历史记录（用于追踪权重演变）
CREATE TABLE analysis_weight_history (
    id BIGSERIAL PRIMARY KEY,
    weight_name VARCHAR(64),                    -- rate_weight / viral_weight / likes_weight
    old_value FLOAT,
    new_value FLOAT,
    adjustment_reason TEXT,                     -- 调整依据
    workflow_id VARCHAR(64),                    -- 触发调整的工作流
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 6.3 T+7 回采流程

```python
# backend/app/services/performance_collector.py
async def collect_performance_7d():
    """定时任务：回采 7 天前发布内容的表现数据。
    
    每天 03:00 执行，扫描 published_at 在 7 天前 ±12 小时的记录。
    """
    target_date = datetime.now() - timedelta(days=7)
    records = await db.fetch_all(
        """
        SELECT * FROM published_content_performance
        WHERE published_at BETWEEN $1 AND $2
          AND collected_at IS NULL
        """,
        target_date - timedelta(hours=12),
        target_date + timedelta(hours=12),
    )
    
    for record in records:
        try:
            # 通过 MCP 抓取发布笔记的当前数据
            stats = await mcp_client.fetch_note_stats(record["published_note_id"])
            
            # 计算表现分（用 Layer 1 同样的公式）
            performance_score = compute_viral_score_single({
                "likes": stats["likes"],
                "author_fans": stats["author_fans"],  # 自己账号的粉丝数
                "comments": stats["comments"],
            })
            
            # 判断是否成功复制了爆款特征
            is_replicated = performance_score > 0.3  # 阈值待数据校准
            
            await db.execute(
                """
                UPDATE published_content_performance
                SET collected_likes = $1, collected_collects = $2,
                    collected_comments = $3, collected_shares = $4,
                    collected_at = NOW(), performance_score = $5,
                    is_replicated = $6
                WHERE id = $7
                """,
                stats["likes"], stats["collects"], stats["comments"], stats["shares"],
                performance_score, is_replicated, record["id"],
            )
            
        except Exception as e:
            logger.warning(f"collect performance failed for {record['id']}: {e}")


async def calibrate_weights():
    """基于历史表现数据校准 viral_score 的三个权重。
    
    每周执行一次，分析过去 4 周的 is_replicated 数据。
    """
    # 拉取过去 4 周已回采的记录
    records = await db.fetch_all(
        """
        SELECT selected_pattern, selected_direction, 
               performance_score, is_replicated
        FROM published_content_performance
        WHERE collected_at > NOW() - INTERVAL '28 days'
        """
    )
    
    if len(records) < 10:
        logger.info("Not enough data for calibration")
        return
    
    # 分析：哪些特征预测了成功复制
    # 如果"内容型爆款"特征对应的 is_replicated=true 比例高
    # → 提高 interaction_rate 的权重
    # （具体校准算法待数据积累后设计，初版可用简单统计）
    
    replicated = [r for r in records if r["is_replicated"]]
    if len(replicated) < 3:
        return
    
    # 统计成功复制的内容，采用了哪些 viral_type 的特征
    type_counts = {"内容型": 0, "双重型": 0, "粉丝型": 0, "普通": 0}
    for r in replicated:
        pattern = r["selected_pattern"] or {}
        viral_type = pattern.get("source_viral_type", "普通")
        type_counts[viral_type] = type_counts.get(viral_type, 0) + 1
    
    # 如果内容型占比 > 60%，说明 interaction_rate 权重应该更高
    total = sum(type_counts.values())
    content_ratio = type_counts.get("内容型", 0) / total if total > 0 else 0
    
    if content_ratio > 0.6:
        new_rate_weight = 0.6  # 从 0.5 提到 0.6
        # ... 记录到 analysis_weight_history
```

### 6.4 定时任务配置

```python
# backend/app/main.py 或 scheduler.py
from apscheduler.schedulers.asyncio import AsyncIOScheduler

scheduler = AsyncIOScheduler()

# 每天 03:00 回采 7 天前发布的表现
scheduler.add_job(
    collect_performance_7d,
    "cron", hour=3, minute=0,
    id="collect_performance_7d",
)

# 每周一 04:00 校准权重
scheduler.add_job(
    calibrate_weights,
    "cron", day_of_week="mon", hour=4, minute=0,
    id="calibrate_weights",
)

scheduler.start()
```

---

## 七、工程实现路径

### 7.1 分阶段实施

| 阶段 | 内容 | 依赖 | 验收标准 |
|------|------|------|---------|
| **阶段 1** | 数据层补全 | 修改搜索 JS + `_normalize()` | search 返回包含 author_fans/published_at |
| **阶段 2** | Layer 1 规则层 | 阶段 1 | top 20 带 viral_type 和 viral_score |
| **阶段 3** | Layer 2 + Layer 3 LLM 层 | 阶段 2 | analyze_node 输出 patterns + insights |
| **阶段 4** | 反馈闭环 | 阶段 3 + 发布功能 | T+7 回采 + 权重校准 |

### 7.2 阶段 1：数据层补全

**修改文件**：
- [qr_http_worker.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\account\qr_http_worker.py)：搜索 JS 增加 `author_fans`、`published_at` 抓取
- [xhs_search.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\agents\skills\xhs_search.py)：`_normalize()` 增加新字段

**风险**：抓粉丝数可能需要进入作者主页，增加请求次数和风控风险。建议先只抓列表页能看到的"作者卡片"粉丝数（粗略值），不进详情页。

### 7.3 阶段 2：Layer 1 规则层

**新增文件**：
- `backend/app/agents/skills/viral_analyzer.py`：独立的爆款分析模块（`compute_metrics` / `classify_viral_type` / `compute_viral_score`）

**修改文件**：
- [xhs_search.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\agents\skills\xhs_search.py)：在 `execute()` 末尾调用 `viral_analyzer`，把带标签和分数的 results 返回

### 7.4 阶段 3：Layer 2 + Layer 3 LLM 层

**新增文件**：
- `backend/app/agents/harnesses/prompts/analyze_layer2.md`：Layer 2 提示词
- `backend/app/agents/harnesses/prompts/analyze_layer3.md`：Layer 3 提示词

**修改文件**：
- [graph.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\agents\graph.py)：`analyze_node` 从空壳改为调用三层分析
- [factory.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\agents\harnesses\factory.py)：新增 `build_analyze_harness`

### 7.5 阶段 4：反馈闭环

**新增文件**：
- `backend/app/services/performance_collector.py`：T+7 回采 + 权重校准
- `backend/app/models/published_performance.py`：ORM 模型
- 数据库迁移：`published_content_performance` + `analysis_weight_history` 表

**修改文件**：
- [main.py](file:///d:\My_Project\多智能体小红书发布平台\backend\app\main.py)：注册 APScheduler 定时任务

---

## 八、成本与延迟对比

| 方案 | LLM 调用次数 | 延迟 | 成本（相对值） | 分析深度 |
|------|------------|------|--------------|---------|
| 旧方案（analyze 空壳） | 0 | 0ms | 0 | 无分析 |
| 朴素方案（20条全分析） | 20 | 60s+ | 100% | 深但贵 |
| **本方案（三层分层）** | **2** | **~8s** | **~10%** | **深且省** |

---

## 九、风险与对策

### 9.1 数据抓取风险

**风险**：抓 `author_fans` 需要额外请求，增加风控触发概率
**对策**：
- 优先用搜索列表页的"作者卡片"粉丝数（粗略值，不进详情页）
- 列表页没有时，用 `author_fans=None` 标记，Layer 1 降级为只按 `likes` 排序
- 不为了精确粉丝数而牺牲搜索成功率

### 9.2 LLM 输出不可信

**风险**：Layer 2/3 的 LLM 可能输出不合规的 JSON 或编造证据
**对策**：
- Layer 3 的 `evidence_note_ids` 必须在 Layer 1 的 top 20 里校验，不在则丢弃
- LLM 输出 JSON 解析失败时，降级为只返回 Layer 1 的规则分析结果
- 不让 LLM 碰 `results` 字段，只产出 `patterns` 和 `insights`

### 9.3 反馈闭环冷启动

**风险**：前期没有发布数据，权重校准无法运行
**对策**：
- 初始权重 0.5/0.3/0.2 是经验值，前期固定不变
- 积累 10 条以上发布记录后才触发权重校准
- 校准结果记录到 `analysis_weight_history`，可追溯权重演变

### 9.4 分位数计算的样本偏差

**风险**：单次搜索的 20 条样本量偏小，分位数可能不稳定
**对策**：
- 样本量 < 5 时，降级为硬阈值分类（兜底）
- 样本量 5-20 时，用分位数但结果标注"置信度中等"
- 后续可引入"近 7 天同关键词的合并样本"提升稳定性

---

## 十、核心结论

### 10.1 方案的核心价值

1. **区分爆款来源**：从"只看 likes 绝对值"升级到"区分粉丝型/内容型/话题型"，真正识别可复制的爆款
2. **分层控成本**：2 次 LLM 调用替代 20 次，成本降 90%，延迟从 60s 降到 8s
3. **反馈闭环进化**：发布后 T+7 回采，校准权重，模型越用越准
4. **规则与 LLM 各司其职**：规则算指标保证数据完整，LLM 做模式识别发挥归因优势

### 10.2 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 分类方式 | 分位数相对分类 | 自适应不同类目，零硬编码阈值 |
| LLM 职责 | 模式识别 + 归因 | LLM 擅长模式识别，不擅长数据搬运 |
| 分层粒度 | 规则→top5→top2 | 平衡成本与分析深度 |
| 反馈周期 | T+7 | 小红书内容热度周期通常 7 天达稳态 |
| 权重校准 | 周度 + 10 条门槛 | 避免小样本过拟合 |

### 10.3 预期效果

- **短期**（阶段 1-3）：analyze 节点从空壳变为三层分析，能区分爆款来源，给出带证据链的选题建议
- **长期**（阶段 4）：发布回看数据积累后，权重自动校准，分析模型持续进化
