# 前端缺失功能实现规划

> 基于「后端 API vs 前端实现」完整对比，后端 76 个端点中前端已实现 63 个（83%），未实现 13 个（17%）。

---

## 现有前端架构

```
WorkbenchView.vue（主壳）
├── SidebarNav.vue（左侧导航）
│   ├── 工作流        → currentPage='workflow'
│   ├── 选题池        → 路由 /topic-pool
│   ├── 审核中心      → currentPage='reviews'  → ReviewsPage.vue
│   ├── 工作流历史    → currentPage='history'  → HistoryPage.vue
│   ├── 账号管理      → currentPage='accounts' → AccountsPage.vue
│   ├── 配置管理      → currentPage='config'   → ConfigPage.vue
│   ├── Esther工厂    → 路由 /esther-factory
│   └── 系统设置      → 空壳（无页面）
├── RightPanel.vue（右侧配置中心：模型/风格/Skill 选择）
└── 各节点卡片组件

独立路由页面：
├── /topic-pool       → TopicPoolView.vue
├── /topic-pool/:id   → TopicPoolDetailView.vue
├── /esther-factory   → EstherFactoryView.vue
├── /mcp-bridge       → McpBridgeView.vue
└── /card-editor      → CardEditorView.vue

前端 API 层：
├── api/auth.ts           → auth 模块 7/8
├── api/account.ts        → account 模块 9/9
├── api/workflow.ts       → workflow 模块 11/12 + SSE + skills
├── api/search.ts         → search 模块 2/2
├── api/topic_pool.ts     → topic_pool 模块 9/9
├── api/esther_factory.ts → esther_factory 模块 12/13
├── api/client.ts         → HTTP 客户端 + token 刷新
└── 缺失：api/memory.ts、api/mcp_bridge.ts
```

---

## 缺失端点清单

### 🔴 高优先级

| # | 后端端点 | 模块 | 缺失内容 |
|---|---|---|---|
| 1 | `GET /api/memory` | memory | 用户记忆概览（偏好/统计/最近选题/最近发布） |
| 2 | `PUT /api/memory/preferences` | memory | 用户偏好设置（文风/图片风格/偏好主题/避免主题） |
| 3 | `POST /workflows/{id}/images/render-template` | workflow | 结构化图表模板渲染为 base64 PNG |

### 🟡 中优先级

| # | 后端端点 | 模块 | 缺失内容 |
|---|---|---|---|
| 4 | `GET /workflows/{id}/review/pending` | review | 轮询获取待审核状态（SSE 兜底） |
| 5 | `GET /esther-factory/brand-config/avatar/{user_id}` | esther_factory | 通过 URL 加载头像（替代 base64） |
| 6 | `GET /mcp/bridge/health` | mcp_bridge | 桥接后端健康检测 |
| 7 | `GET /mcp/bridge/check-account` | mcp_bridge | 小红书账号状态检测 |
| 8 | `GET /mcp/bridge/pending` | mcp_bridge | 拉取待处理 MCP 请求 |

### 🟢 低优先级（后端也是空壳）

| # | 后端端点 | 模块 | 缺失内容 |
|---|---|---|---|
| 9 | `POST /workflows/{id}/suggestions/{sid}/confirm` | rollback | 确认 supervisor 建议 |
| 10 | `POST /workflows/{id}/suggestions/{sid}/reject` | rollback | 拒绝 supervisor 建议 |
| 11 | `POST /workflows/{id}/resume-from-suspension` | recovery | 恢复挂起的工作流 |
| 12 | `GET /agents/configs` | config | Agent 配置列表（后端返回空数组） |
| 13 | `PUT /agents/configs/{agent_id}` | config | 更新 Agent 配置（后端空壳） |

---

## Phase 1：高优先级实现方案

### 1.1 用户记忆与偏好（memory 模块）

**API 层**：新建 `frontend/src/api/memory.ts`

```
GET  /api/memory             → getOverview()     记忆概览
PUT  /api/memory/preferences → updatePreferences() 设置偏好
```

**页面位置**：SidebarNav 的「系统设置」导航项

- 当前「系统设置」是空壳，点击无反应
- 记忆/偏好天然属于用户级设置，与右侧面板的工作流级配置区分开
- 激活后 `currentPage='settings'`，在 WorkbenchView 中渲染

**组件**：新建 `frontend/src/components/workbench/SettingsPage.vue`

```
SettingsPage.vue
├── 偏好概览卡片
│   ├── 文风偏好（当前值 + 修改入口）
│   ├── 图片风格偏好
│   ├── 偏好主题列表
│   └── 避免主题列表
├── 偏好编辑表单
│   ├── 文风选择（下拉/标签选择）
│   ├── 图片风格选择
│   ├── 偏好主题（标签输入）
│   └── 避免主题（标签输入）
└── 记忆统计
    ├── 累计选题数
    ├── 累计发布数
    └── 最近选题/发布记录
```

**改动文件清单**：

| 文件 | 改动 |
|---|---|
| `api/memory.ts` | 新建，封装 2 个 API |
| `components/workbench/SettingsPage.vue` | 新建，偏好设置页面 |
| `components/workbench/SidebarNav.vue` | 激活「系统设置」导航项，emit `nav-click` 事件 |
| `views/WorkbenchView.vue` | 添加 `page-settings` 容器 + import SettingsPage |

### 1.2 结构化图表模板渲染（render-template）

**API 层**：在 `frontend/src/api/workflow.ts` 中新增

```
POST /workflows/{id}/images/render-template → renderTemplateImage(workflowId, params)
```

**调用位置**：`ImagePlanCard.vue`

- 当 image_plan 节点输出包含 `template_type`（flow/list/timeline）时
- 调用接口获取渲染后的 base64 PNG
- 在卡片中显示预览图

**改动文件清单**：

| 文件 | 改动 |
|---|---|
| `api/workflow.ts` | 新增 `renderTemplateImage` 方法 |
| `components/workbench/ImagePlanCard.vue` | 模板类型检测 + 预览渲染调用 |

---

## Phase 2：中优先级实现方案

### 2.1 审核状态轮询（review/pending）

**API 层**：在 `frontend/src/api/workflow.ts` 中新增

```
GET /workflows/{id}/review/pending → getPendingReview(workflowId)
```

**调用位置**：`stores/workflow.ts`

- SSE 断线重连后，轮询此接口获取当前审核状态
- 替代纯 SSE 依赖，提升可靠性

**改动文件清单**：

| 文件 | 改动 |
|---|---|
| `api/workflow.ts` | 新增 `getPendingReview` 方法 |
| `stores/workflow.ts` | SSE 断线后轮询逻辑 |

### 2.2 头像 URL 加载（brand-config/avatar/{user_id}）

**API 层**：在 `frontend/src/api/esther_factory.ts` 中新增

```
GET /esther-factory/brand-config/avatar/{user_id} → getAvatarUrl(userId)
```

- 返回 URL 字符串（非 base64），供 `<img :src>` 直接使用
- 支持浏览器缓存，减少重复传输

**改动文件清单**：

| 文件 | 改动 |
|---|---|
| `api/esther_factory.ts` | 新增 `getAvatarUrl` 方法 |
| `views/EstherFactoryView.vue` | 头像显示逻辑改用 URL |

### 2.3 MCP 桥接状态检测

**API 层**：新建 `frontend/src/api/mcp_bridge.ts`

```
GET /mcp/bridge/health        → checkHealth()       后端健康状态
GET /mcp/bridge/check-account → checkAccount()      小红书账号状态
GET /mcp/bridge/pending       → getPendingRequests() 待处理请求列表
```

**调用位置**：`McpBridgeView.vue`

- 页面加载时自动检测后端健康状态
- 显示小红书账号在线/离线状态指示器
- 待处理请求列表展示（当前只走 SSE stream，缺轮询兜底）

**改动文件清单**：

| 文件 | 改动 |
|---|---|
| `api/mcp_bridge.ts` | 新建，封装 3 个 API |
| `views/McpBridgeView.vue` | 状态指示器 + 自动检测 + 待处理列表 |

---

## Phase 3：低优先级（等后端完善后再实现）

### 3.1 Supervisor 建议系统

- 后端 `rollback.py` 的 confirm/reject 都是空壳
- 前端在 `api/workflow.ts` 预留方法签名即可
- 后端实现后，在 WorkbenchView 工作流节点中添加建议卡片

### 3.2 挂起恢复

- 后端 `recovery.py` 也是空壳
- 前端在 `api/workflow.ts` 预留 `resumeFromSuspension` 方法签名
- 后端实现后，在工作流状态栏添加「恢复」按钮

### 3.3 Agent 配置

- 后端返回空数组
- 前端可在 `ConfigPage.vue` 中预留一个「Agent 配置」卡片
- 后端实现后填充内容

---

## 实施顺序总览

```
Phase 1（高优先级）
  1.1 新建 api/memory.ts
  1.2 新建 SettingsPage.vue（记忆概览 + 偏好编辑）
  1.3 SidebarNav 激活「系统设置」→ WorkbenchView 添加 page-settings
  1.4 workflow.ts 新增 renderTemplateImage → ImagePlanCard 调用

Phase 2（中优先级）
  2.1 workflow.ts 新增 getPendingReview → store SSE 兜底轮询
  2.2 esther_factory.ts 新增 getAvatarUrl → EstherFactoryView 头像优化
  2.3 新建 api/mcp_bridge.ts → McpBridgeView 状态检测

Phase 3（低优先级，等后端完善）
  3.1 workflow.ts 预留 suggestions API
  3.2 workflow.ts 预留 resumeFromSuspension API
  3.3 ConfigPage 预留 agent 配置卡片
```