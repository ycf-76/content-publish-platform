#+TITLE: 并发优化方案
#+AUTHOR: 多智能体小红书发布平台
#+DATE: 2026-08-15

* 当前架构容量评估

** 逐层瓶颈分析

#+BEGIN_SRC
用户请求
  │
  ▼
┌──────────────┐
│  Nginx/CDN   │  ← 目前没有，直接裸跑 uvicorn
└──────┬───────┘
       ▼
┌──────────────┐
│   uvicorn    │  ← 单进程，1个 event loop
│  (1 worker)  │
└──────┬───────┘
       ▼
┌──────────────┐
│   FastAPI    │  ← 异步框架，理论上轻松万级 QPS
│  路由/中间件  │
└──────┬───────┘
       │
   ┌───┴────────────────────┐
   ▼                        ▼
┌────────┐           ┌───────────┐
│  MySQL │           │   Redis   │
│ 连接池  │           │  连接池    │
│ 10+20  │           │   20      │
└────────┘           └───────────┘
       │
       ▼
┌──────────────┐
│  LLM API     │  ← 真正的瓶颈！DeepSeek/通义万相
│  (外部服务)    │     每次调用 5-60 秒
└──────────────┘
#+END_SRC

** 各层容量表

| 层               | 当前配置                     | 理论上限                | 实际瓶颈       |
|------------------+----------------------------+-----------------------+---------------|
| uvicorn          | 1 worker, 单进程             | ~1000 并发连接          | ⚠️ 单进程无并行  |
| FastAPI          | 异步, 无限流                  | ~5000-10000 QPS (简单接口) | ✅ 不是瓶颈     |
| MySQL 连接池      | pool_size=10, max_overflow=20 | **30 个并发查询**        | ⚠️ 瓶颈之一    |
| Redis 连接池      | pool_size=20                 | ~10000 QPS             | ✅ 不是瓶颈     |
| SSE 长连接        | 每个用户1个, 无上限             | 受内存限制, ~500-1000    | ⚠️ 内存吃紧    |
| LLM API          | 无并发控制                    | 受 API 限速              | 🔴 **最大瓶颈** |
| SSE Event Bus    | 内存 dict, 无 eviction        | 无限增长 → OOM          | 🔴 **隐患**    |

** 实际能扛多少？

*** 短请求（登录、查配置、选题池浏览）— 能扛 500+ QPS
纯数据库读写，FastAPI 异步处理，MySQL 30 个连接完全够用。

*** 工作流启动 — 能扛 ~10 个同时启动
每个工作流启动后，asyncio.create_task 在后台跑 LangGraph，每个工作流会：
- 调 DeepSeek API（5-30秒/次，9 个节点 = 调 9 次）
- 调通义万相 API（生图，10-60秒/次）
- 持有 1 个 SSE 长连接

LLM API 是硬瓶颈：DeepSeek 默认限速约 5-10 RPM，通义万相约 2-5 RPM。
10 个工作流同时跑，LLM 调用就开始排队了。

*** SSE 长连接 — 能扛 ~200-500 个同时在线
每个 SSE 连接占用一个 asyncio.Queue + 一个协程，内存约 50KB/连接。
1GB 内存约 2000 个连接，但加上事件历史，实际安全上限约 500。

* 三个隐藏炸弹

** 炸弹 1：SSE Event Bus 无清理

#+BEGIN_SRC python
# sse_bus.py — 事件历史永远不清理
self._event_history[workflow_id].append(event)
#+END_SRC

100 个工作流 × 每个约 50 个事件 × 每个事件约 2KB = 10MB。
看起来不多，但跑一周不重启就 700MB，一个月就 OOM。

** 炸弹 2：后台 task 无上限

#+BEGIN_SRC python
# workflow.py — 想启动多少就启动多少
task = asyncio.create_task(coro)
#+END_SRC

100 个用户同时点搜索 → 100 个 LangGraph 并行跑 → 100×9=900 个 LLM API 调用同时发出
→ DeepSeek 限速返回 429 → 全部失败。

** 炸弹 3：MySQL 连接池耗尽

30 个连接上限，10 个工作流同时写节点状态（每个工作流 9 个节点 × 多次状态更新），
加上普通查询，连接池很容易打满，其他请求排队等连接。

* 优化路线图

| 优先级 | 改什么              | 怎么改                                       | 效果               |
|-------+-------------------+--------------------------------------------+-------------------|
| P0    | LLM 并发控制        | 加 Semaphore 限制同时 LLM 调用数                | 防止 API 限速雪崩 ✅  |
| P0    | SSE 事件清理        | 工作流结束后清 _event_history，或迁移到 Redis Stream | 防止内存泄漏 ✅     |
| P1    | 工作流并发上限       | Redis 分布式锁 + 计数器，限制同时运行的工作流数      | 防止资源耗尽 ✅     |
| P1    | uvicorn 多 worker   | uvicorn app:app --workers 4                  | 吞吐量 ×4          |
| P2    | MySQL 连接池扩容     | pool_size=20, max_overflow=40                | 支持更多并发查询 ✅   |
| P2    | API 限流           | Redis 计数器限流                               | 防止单用户刷接口 ✅   |
| P3    | Nginx 反代         | 负载均衡 + 静态资源 + 限流                        | 生产环境必备        |

** 当前阶段够用吗？

够用。开发/内测阶段，同时在线用户不会超过 5 个，20-30 的并发上限完全足够。

真正需要扩容的信号：
1. 用户数 > 50 同时在线
2. 后端内存 > 2GB（SSE 事件堆积）
3. LLM API 频繁 429（并发工作流太多）

到那时再加 Semaphore 和事件清理就行，架构已预留 Redis 位置，改造成本很低。

* P0 实施细节

** ✅ LLM 并发控制（Semaphore）— 已实施

#+BEGIN_SRC python
# app/agents/adapters/llm_base.py 或 app/services/workflow.py

import asyncio

# 全局信号量：同时最多 N 个 LLM API 调用
# DeepSeek Tier1: 5 RPM → 设为 3-5
# DeepSeek Tier2: 60 RPM → 设为 10-15
# 根据你的 API 套餐调整
LLM_SEMAPHORE = asyncio.Semaphore(5)

async def call_llm_with_limit(coro):
    """带并发限制的 LLM 调用。"""
    async with LLM_SEMAPHORE:
        return await coro
#+END_SRC

** ✅ SSE 事件清理 — 已实施

#+BEGIN_SRC python
# app/services/sse_bus.py — 添加清理方法

def cleanup_workflow(self, workflow_id: str) -> None:
    """工作流结束后清理事件历史和订阅者。"""
    self._event_history.pop(workflow_id, None)
    self._subscribers.pop(workflow_id, None)
    logger.info(f"[{workflow_id}] SSE event history cleaned up")

# 在 workflow 终态事件发布后调用
if event_type in {'workflow_completed', 'workflow_error', 'workflow_terminated'}:
    # 延迟清理：给断线重连一点时间
    import asyncio
    asyncio.get_event_loop().call_later(300, self.cleanup_workflow, workflow_id)
#+END_SRC

** 工作流并发上限

#+BEGIN_SRC python
# app/services/workflow.py — 启动前检查

MAX_CONCURRENT_WORKFLOWS = 10

async def start_workflow(self, ...):
    # Redis 计数器检查
    from app.cache import redis_client
    count = await redis_client.incr("count:active_workflows")
    if count == 1:
        await redis_client.expire("count:active_workflows", 3600)  # 1小时自动过期防泄漏

    if count > MAX_CONCURRENT_WORKFLOWS:
        await redis_client.incrby("count:active_workflows", -1)
        raise HTTPException(429, f"当前有 {count-1} 个工作流正在运行，请稍后再试")

    try:
        # ... 正常启动逻辑
        pass
    except Exception:
        await redis_client.incrby("count:active_workflows", -1)
        raise

# 工作流结束时递减
async def _on_workflow_complete(self, workflow_id: str):
    await redis_client.incrby("count:active_workflows", -1)
#+END_SRC

* 容量规划参考

| 场景             | 同时在线 | 工作流并发 | uvicorn workers | MySQL 连接池 | Redis 内存 | 服务器配置        |
|----------------+--------+----------+----------------+------------+-----------+----------------|
| 开发/内测        | 1-5    | 1-3      | 1              | 10+20      | 无需       | 2C4G 单机       |
| 小规模公测       | 10-50  | 5-10     | 2              | 20+40      | 512MB     | 4C8G 单机       |
| 正式运营         | 50-200 | 10-30    | 4              | 30+50      | 2GB       | 8C16G + Nginx   |
| 规模化           | 200+   | 30+      | 8 (多机)       | 50+100     | 4GB+      | K8s 集群        |

* 项目风险评估与修复方案

** 高危问题（P0 — 必须修）

| #  | 严重度 | 问题                    | 影响范围              | 修复难度 | 状态   |
|----+--------+-------------------------+---------------------+----------+--------|
|  1 | 🔴 P0 | SSE 无鉴权               | 数据泄露              | 低       | ✅已修 |
|  2 | 🔴 P0 | 验证码无防暴力破解         | 账号被盗              | 低       | ✅已修 |
|  3 | 🔴 P0 | 内存状态重启丢失           | 登录失败/会话断开       | 中       | ✅已修 |
|  4 | 🔴 P0 | SSE 事件不清理            | OOM 崩溃             | 低       | ✅已修 |
|  5 | 🔴 P0 | 后台 task 无上限          | LLM API 雪崩          | 低       | ✅已修 |

*** P0-1：SSE 无鉴权

#+BEGIN_SRC python
# 当前：sse.py — 只校验 sse_token，不校验用户与工作流归属
sse_token = request.cookies.get("sse_token")
if sse_token and not validate_sse_token(sse_token):
    raise HTTPException(401, detail="SSE session invalid")
# 问题：知道 workflow_id 就能订阅任意工作流事件
#+END_SRC

修复方案：SSE 端点加 JWT 校验 + 用户-工作流归属校验。

#+BEGIN_SRC python
# 修复后
@router.get("/workflow/{workflow_id}")
async def subscribe_workflow(
    workflow_id: str,
    request: Request,
    db: AsyncSession = Depends(get_db),
):
    token = request.cookies.get("token")
    if not token:
        raise HTTPException(401, "Authentication required")
    payload = verify_jwt(token)
    user_id = payload.get("sub")

    # 校验用户是否拥有该工作流
    workflow = await workflow_service.get_workflow(workflow_id, user_id)
    if not workflow:
        raise HTTPException(403, "Access denied to workflow")
#+END_SRC

*** P0-2：验证码无防暴力破解

#+BEGIN_SRC python
# 当前：auth.py — 无失败次数限制
stored = _email_codes.get(email)
if not stored or stored[0] != code:
    raise HTTPException(401, "验证码错误或已过期")
# 问题：6位验证码可被暴力枚举，100万次尝试即可破解
#+END_SRC

修复方案：Redis 计数器限制验证码验证频率。

#+BEGIN_SRC python
# 修复后
MAX_VERIFY_ATTEMPTS = 5
VERIFY_LOCK_MINUTES = 15

async def verify_email_code(email: str, code: str):
    lock_key = f"lock:verify:{email}"
    attempts = await redis_client.get(lock_key)
    if attempts and int(attempts) >= MAX_VERIFY_ATTEMPTS:
        raise HTTPException(429, "验证次数过多，请15分钟后再试")

    stored = await redis_client.get(f"code:{email}")
    if not stored or stored != code:
        await redis_client.incr(lock_key)
        await redis_client.expire(lock_key, VERIFY_LOCK_MINUTES * 60)
        raise HTTPException(401, "验证码错误或已过期")

    await redis_client.delete(lock_key)
#+END_SRC

*** P0-3：内存状态重启丢失

#+BEGIN_SRC python
# 当前：auth.py — 验证码和 SSE token 存内存
_email_codes: dict[str, tuple[str, float]] = {}
_sse_tokens: set[str] = set()
# 问题：后端重启 → 所有验证码失效 → 已登录用户 SSE 断开
#+END_SRC

修复方案：迁移到 Redis 持久化存储。

#+BEGIN_SRC python
# 修复后
async def store_email_code(email: str, code: str):
    await redis_client.setex(f"code:{email}", 300, code)  # 5分钟过期

async def store_sse_token(token: str):
    await redis_client.sadd("sse_tokens", token)

async def validate_sse_token(token: str) -> bool:
    return await redis_client.sismember("sse_tokens", token)
#+END_SRC

*** P0-4：SSE 事件不清理

#+BEGIN_SRC python
# 当前：sse_bus.py — 事件历史只追加不清理
self._event_history[workflow_id].append(event)
# 问题：长期运行 → 内存无限增长 → OOM
#+END_SRC

修复方案：工作流结束后延迟清理事件历史。

#+BEGIN_SRC python
# 修复后
def cleanup_workflow(self, workflow_id: str) -> None:
    self._event_history.pop(workflow_id, None)
    self._subscribers.pop(workflow_id, None)

# 工作流终态事件发布后，延迟5分钟清理
if event_type in {'workflow_completed', 'workflow_error', 'workflow_terminated'}:
    asyncio.get_event_loop().call_later(300, self.cleanup_workflow, workflow_id)
#+END_SRC

*** P0-5：后台 task 无上限

#+BEGIN_SRC python
# 当前：workflow.py — 无限制创建后台任务
task = asyncio.create_task(coro)
# 问题：100个用户同时启动 → 100×9=900个 LLM API 调用 → 429 雪崩
#+END_SRC

修复方案：Semaphore 限制并发 LLM 调用 + 工作流启动上限。

#+BEGIN_SRC python
# 修复后
LLM_SEMAPHORE = asyncio.Semaphore(5)
MAX_CONCURRENT_WORKFLOWS = 10

async def call_llm_with_limit(coro):
    async with LLM_SEMAPHORE:
        return await coro

async def start_workflow(self, ...):
    count = await redis_client.incr("count:active_workflows")
    if count > MAX_CONCURRENT_WORKFLOWS:
        await redis_client.incrby("count:active_workflows", -1)
        raise HTTPException(429, "当前工作流过多，请稍后再试")
#+END_SRC

** 中危问题（P1 — 应该修）

| #  | 严重度 | 问题                    | 影响范围              | 修复难度 | 状态     |
|----+--------+-------------------------+---------------------+----------+----------|
|  6 | 🟡 P1 | JWT 7天太长              | 被盗后窗口大          | 中       | ✅已修   |
|  7 | 🟡 P1 | 401 硬跳转丢状态         | 用户编辑内容丢失       | 低       | ✅已修   |
|  8 | 🟡 P1 | 迁移用原始 SQL           | 不可回滚/协作冲突      | 中       | ✅已有   |
|  9 | 🟡 P1 | get_nodes 无鉴权         | 数据泄露              | 低       | ✅已修   |

*** P1-6：JWT 7天太长

#+BEGIN_SRC python
# 当前：security.py
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7天
# 问题：Token 被盗后，攻击者有7天窗口期
#+END_SRC

修复方案：缩短 Access Token 过期时间 + Refresh Token 机制。

#+BEGIN_SRC python
# 修复后
ACCESS_TOKEN_EXPIRE_MINUTES = 30          # 30分钟
REFRESH_TOKEN_EXPIRE_DAYS = 7             # 7天（仅用于刷新）

# 前端：401 时用 refresh_token 静默刷新，用户无感知
#+END_SRC

*** P1-7：401 硬跳转丢状态

#+BEGIN_SRC python
# 当前：前端 axios 拦截器
if (error.response?.status === 401) {
  router.push('/login')  // 直接跳转，用户正在编辑的内容丢失
}
#+END_SRC

修复方案：跳转前保存当前状态，登录后恢复。

#+BEGIN_SRC python
# 修复后
if (error.response?.status === 401) {
  // 保存当前路由和状态
  sessionStorage.setItem('redirect_after_login', router.currentRoute.value.fullPath)
  sessionStorage.setItem('draft_state', JSON.stringify(storeToSave))
  router.push('/login')
}

// 登录成功后恢复
const redirect = sessionStorage.getItem('redirect_after_login')
if (redirect) {
  router.push(redirect)
  sessionStorage.removeItem('redirect_after_login')
}
#+END_SRC

*** P1-8：迁移用原始 SQL

#+BEGIN_SRC python
# 当前：migrations/ — 直接执行 SQL 字符串
# 问题：无法回滚、多人协作时迁移冲突、无版本追踪
#+END_SRC

修复方案：引入 Alembic 管理数据库迁移。

#+BEGIN_SRC bash
# 修复后
pip install alembic
alembic init migrations
alembic revision --autogenerate -m "add workflow table"
alembic upgrade head
alembic downgrade -1  # 可回滚
#+END_SRC

*** P1-9：get_nodes 无鉴权

#+BEGIN_SRC python
# 当前：workflow.py — 获取节点数据不校验用户身份
@router.get("/workflows/{workflow_id}/nodes")
async def get_workflow_nodes(workflow_id: str, db: AsyncSession = Depends(get_db)):
    # 任何人都能查看任意工作流的节点数据
#+END_SRC

修复方案：添加 JWT 校验 + 工作流归属校验。

#+BEGIN_SRC python
# 修复后
@router.get("/workflows/{workflow_id}/nodes")
async def get_workflow_nodes(
    workflow_id: str,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    workflow = await db.get(Workflow, workflow_id)
    if not workflow or workflow.user_id != current_user.id:
        raise HTTPException(403, "Access denied")
#+END_SRC

** 低危问题（P2 — 有空再修）

| #  | 严重度 | 问题                    | 影响范围              | 修复难度 | 状态   |
|----+--------+-------------------------+---------------------+----------+--------|
| 10 | 🟢 P2 | ULID 非真正 ULID         | 失去时序优势          | 低       | ✅已修 |
| 11 | 🟢 P2 | CORS 硬编码              | 部署需改代码          | 低       | ✅已修 |
| 12 | 🟢 P2 | SSE 直连后端              | 生产需 Nginx          | 低       | ✅已修 |

*** P2-10：ULID 非真正 ULID

#+BEGIN_SRC python
# 当前：models.py — 用 uuid4 代替 ULID
id = Column(String(26), default=lambda: str(uuid.uuid4())[:26])
# 问题：UUID4 是随机的，ULID 的核心优势是时间排序
#+END_SRC

修复方案：引入真正的 ULID 库。

#+BEGIN_SRC python
# 修复后
from ulid import ULID
id = Column(String(26), default=lambda: str(ULID()))
#+END_SRC

*** P2-11：CORS 硬编码

#+BEGIN_SRC python
# 当前：config.py
CORS_ORIGINS = ["http://localhost:3001", "http://127.0.0.1:3001"]
# 问题：部署到新环境需要改代码重新部署
#+END_SRC

修复方案：从环境变量读取。

#+BEGIN_SRC python
# 修复后
CORS_ORIGINS = os.getenv("CORS_ORIGINS", "http://localhost:3001").split(",")
#+END_SRC

*** P2-12：SSE 直连后端

#+BEGIN_SRC python
# 当前：前端直接连接后端 SSE 端口
# 问题：生产环境需要 Nginx 反代，前端代码要改
#+END_SRC

修复方案：SSE 请求走 Vite 代理 / Nginx 反代，前端不硬编码后端地址。

#+BEGIN_SRC python
# 修复后：前端统一走 /api 前缀
const eventSource = new EventSource(`/api/sse/workflow/${workflow_id}`)
# Nginx 配置：
# location /api/sse/ { proxy_pass http://backend:8000/sse/; proxy_set_header Connection ''; }
#+END_SRC