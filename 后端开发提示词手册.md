# 多智能体小红书发布平台 - 后端开发提示词手册

| 字段 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 创建日期 | 2026-07-24 |
| 适用工具 | Codex + GLM-4.7-Flash |
| 范围 | 仅后端（FastAPI + LangGraph + Harness + MCP + Recovery） |
| 前端 | 用户自行用其他工具开发，本手册不含前端 |
| 用途 | 分阶段给Codex/GLM的执行提示词，按顺序逐条粘贴执行 |

---

## 0. 使用方式

**严格按 Phase 0 → Phase 8 顺序执行**，每个 Phase 完成并自检通过后再进入下一个。不可跳跃。

每个 Phase 的提示词包含 5 部分：
1. **任务目标**：本阶段做什么
2. **文档引用**：必须读哪些文档的哪些章节
3. **红线提醒**：本阶段相关红线
4. **执行指令**：具体生成什么代码
5. **自检清单**：完成后逐项确认

**强制要求**：
- 每次执行前，Codex 必须先读《开发红线手册.md》全文
- 生成代码必须 100% 遵守红线，违反时停止并提示
- 不确定的实现细节，**先问用户，不要自行猜测**
- 每个文件生成后立即用 `python -m py_compile` 或 `ruff check` 自检语法

---

## Phase 0: 项目骨架初始化

### 任务目标
初始化 backend 目录结构、依赖管理、基础配置文件。

### 文档引用
- 《技术架构设计文档.md》第9章「目录结构」
- 《开发红线手册.md》第1章「技术栈红线」
- 《PRD.md》D7、D12、D13

### 红线提醒
- 后端必须 FastAPI (Python 3.11+)
- 必须用 LangGraph，禁止 langchain AgentExecutor
- Harness 必须是子包结构，禁止单文件 `harness.py`
- DB 必须 PostgreSQL
- 禁止引入未在红线清单的库

### 执行指令（粘贴给 Codex）

```
请先阅读以下文档全文：
1. d:\My_Project\多智能体小红书发布平台\开发红线手册.md
2. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md（重点第9章目录结构）
3. d:\My_Project\多智能体小红书发布平台\PRD.md（重点 D7/D12/D13 决策）

任务：初始化后端项目骨架。

要求：
1. 在 d:\My_Project\多智能体小红书发布平台\backend\ 下创建目录结构
2. 严格按《技术架构设计文档》第9章的目录树创建，包括：
   - app/api/routers/ (auth, workflow, sse, review, rollback, recovery, account)
   - app/api/schemas/
   - app/services/ (workflow, rollback, review)
   - app/agents/core/ (harness子包: runtime/executor/recovery/observer)
   - app/agents/skills/ (xhs_search, vl_analyze, generate_image, mcp/)
   - app/agents/configs/
   - app/agents/prompts/
   - app/agents/schemas/
   - app/agents/graph.py
   - app/account/
   - app/crypto/
   - app/db/ (models.py, session.py)
   - app/config.py
   - tests/
3. 创建 pyproject.toml，依赖包括：
   - fastapi, uvicorn[standard]
   - langgraph, langgraph-checkpoint-postgres, langchain-core
   - pydantic, pydantic-settings
   - sqlalchemy[asyncio], asyncpg, alembic
   - httpx (调LLM和MCP)
   - deepseek 官方SDK 或 openai SDK（DeepSeek兼容OpenAI协议）
   - dashscope (Qwen-VL 和 通义万相)
   - cryptography (AES加密Token)
   - ulid-py
   - structlog
   - pytest, pytest-asyncio, httpx (测试)
4. 创建 app/config.py：用 pydantic-settings 读取环境变量（DEEPSEEK_API_KEY, DASHSCOPE_API_KEY, DATABASE_URL, XHS_MCP_PLUGIN_URL, XHS_MCP_LOCAL_CMD, AES_SECRET_KEY 等）
5. 创建 .env.example 列出所有需要的环境变量
6. 创建 app/db/session.py：异步SQLAlchemy session工厂
7. 创建 app/main.py：FastAPI app骨架，含 CORS 中间件，路由 include 占位
8. 创建 README.md 简要说明启动方式（uvicorn app.main:app --reload）

严禁：
- 不要实现具体业务逻辑，本阶段只搭骨架
- 不要创建任何业务代码文件的内容，只创建空文件或带 docstring 的占位
- 不要安装依赖（只写 pyproject.toml）

完成后输出：
- 目录树验证
- 每个文件是否存在
- 用 ruff check . 自检语法（允许空文件警告）
```

### 自检清单
- [ ] 目录结构与《技术架构设计文档》第9章完全一致
- [ ] harness 是子包（`agents/core/harness/`），不是单文件
- [ ] recovery 是子包（`agents/core/harness/recovery/`）
- [ ] pyproject.toml 含所有必需依赖
- [ ] config.py 用 pydantic-settings
- [ ] DB 用 async SQLAlchemy + asyncpg
- [ ] 无业务逻辑实现

---

## Phase 1: 数据模型层

### 任务目标
实现 SQLAlchemy 数据模型，对应《技术架构设计文档》第8章。

### 文档引用
- 《技术架构设计文档.md》第8章「数据模型」
- 《PRD.md》D5（多账号1:N）、D11（不存原图）、D6（checkpoint持久化）
- 《开发红线手册.md》第7章「数据模型红线」

### 红线提醒
- User:Account 必须 1:N（即使MVP只用1个账号）
- 禁止存储小红书爆款原图
- 禁止用 MemorySaver，必须 PostgresSaver
- Token 必须 AES 加密存储

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md 第8章「数据模型」
2. d:\My_Project\多智能体小红书发布平台\PRD.md D5/D6/D11
3. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第7章

任务：实现 app/db/models.py，包含以下表：

1. users
   - id (UUID主键)
   - email (唯一)
   - password_hash
   - created_at, updated_at

2. xhs_accounts (D5: User 1:N Account)
   - id (UUID主键)
   - user_id (FK -> users.id, 非空)
   - xhs_user_id (小红书用户ID)
   - xhs_nickname
   - xhs_avatar_url
   - session_data_encrypted (Text, AES加密的完整Session JSON)
   - refresh_token_encrypted (Text, AES加密)
   - token_expires_at (DateTime)
   - login_method (Enum: 'plugin' | 'session_refresh' | 'qrcode')  # D1分层登录
   - status (Enum: 'active' | 'expired' | 'banned')
   - last_used_at
   - created_at, updated_at
   - 索引: (user_id), (xhs_user_id)

3. workflows
   - id (UUID主键)
   - user_id (FK -> users.id)
   - account_id (FK -> xhs_accounts.id)
   - topic (主题，如"科技类")
   - status (Enum: 'pending'|'running'|'awaiting_review'|'suspended'|'completed'|'failed'|'terminated')  # 对应节点状态机
   - current_node_id
   - suspended_until (DateTime, nullable, D15 30分钟挂起用)
   - suspension_reason (Text, nullable)
   - created_at, updated_at, completed_at

4. workflow_nodes
   - id (UUID主键)
   - workflow_id (FK)
   - node_type (Enum: 'search'|'analyze'|'image_gen'|'image_review'|'copywrite'|'audit'|'final_review'|'publish')
   - node_key (str, 如 'search', 'analyze')
   - status (Enum: 9种状态, 对应《前后端通信协议》第4章)
   - input_data (JSONB)
   - output_data (JSONB, 不含原图base64, D11)
   - error_message (Text, nullable)
   - crash_reason (str, nullable)
   - recovery_attempts (int, default 0)
   - started_at, completed_at
   - duration_ms (int, nullable)
   - token_usage (int, nullable)
   - model_used (str, nullable)

5. workflow_checkpoints (D6: LangGraph checkpoint持久化)
   - 注：LangGraph PostgresSaver 自动管理 checkpoints 表
   - 但我们需要一个业务层的 checkpoint 索引表，用于时光机UI
   - id (UUID主键)
   - workflow_id (FK)
   - node_id (FK -> workflow_nodes.id)
   - langgraph_thread_id (str)
   - langgraph_checkpoint_id (str)
   - snapshot_summary (Text, 人类可读摘要)
   - created_at

6. agent_traces (D16: 过程透明化, L3原始数据存DB)
   - id (UUID主键)
   - node_id (FK -> workflow_nodes.id)
   - event_type (str: 'tool_call_start'|'tool_call_end'|'progress_update'|'model_switched'|'agent_thinking'|'decision_made')
   - event_payload (JSONB)
   - raw_data (JSONB, nullable, L3原始数据)
   - created_at
   - 索引: (node_id, created_at)

7. pending_reviews (人工审核待办)
   - id (UUID主键)
   - workflow_id (FK)
   - node_id (FK)
   - review_type (Enum: 'image_review'|'final_review'|'structural_recovery')  # D15 结构性恢复也走这里
   - payload (JSONB, 含 supervisor建议/溯源链)
   - status (Enum: 'pending'|'approved'|'rejected'|'timeout')
   - user_decision (JSONB, nullable)
   - decided_at (DateTime, nullable)
   - expires_at (DateTime, D15 30分钟超时)
   - created_at

8. pending_suggestions (监督Agent建议, D4 建议权)
   - id (UUID主键)
   - workflow_id (FK)
   - node_id (FK)
   - severity (Enum: 'info'|'warning'|'error')
   - suggestion_type (Enum: 'technical'|'structural')  # D15 技术性/结构性
   - message (Text)
   - trace (JSONB, 溯源链 SuggestionTrace[])
   - proposed_action (JSONB)
   - status (Enum: 'pending'|'auto_executed'|'user_confirmed'|'user_rejected'|'timeout')
   - created_at, resolved_at

要求：
1. 用 SQLAlchemy 2.0 风格（Mapped, mapped_column）
2. 全部用异步兼容的类型（AsyncEnum, JSONB）
3. 用 ULID 字符串作为主键（不要用自增int）
4. 表名用复数蛇形命名
5. 外键必须有 ondelete='CASCADE'
6. 创建 app/crypto/token_crypto.py：用 cryptography 库实现 AES-GCM 加密 session_data 和 refresh_token
7. 创建 alembic.ini 和 alembic/ 目录，生成初始 migration

严禁：
- 不要在 users 表直接存小红书账号信息（必须 1:N）
- 不要存图片 base64 或原图URL到DB（D11）
- 不要用 MemorySaver 相关字段
- 不要明文存 Token

完成后：
- 输出 models.py 全文
- 输出 token_crypto.py 全文
- 运行 alembic revision --autogenerate -m "init schema" 验证 migration 能生成
- ruff check . 通过
```

### 自检清单
- [ ] 8 张表全部实现
- [ ] User:Account 是 1:N（account 表有 user_id 外键）
- [ ] 无图片 base64 字段
- [ ] Token 字段都是 `_encrypted` 后缀
- [ ] 节点状态枚举是 9 种
- [ ] 工作流状态枚举含 `suspended`
- [ ] alembic migration 可生成

---

## Phase 2: Harness 子包骨架

### 任务目标
实现 Harness 核心子包：runtime / executor / observer，不含 recovery（Phase 6 再做）。

### 文档引用
- 《技术架构设计文档.md》第4章「Harness 架构」
- 《开发红线手册.md》第2章「架构分层红线」
- 《PRD.md》D13、D14

### 红线提醒
- Harness 必须是子包结构
- 三层架构严格分离：Harness 不能引用 LangGraph
- 监督Agent是分散式守卫，不是中心节点
- Executor 当前只做 single_shot，不做 ReAct

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md 第4章「Harness 架构」（4.1-4.5）
2. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第2章
3. d:\My_Project\多智能体小红书发布平台\PRD.md D13/D14

任务：实现 Harness 子包骨架（不含 recovery，recovery 在 Phase 6）。

需要创建的文件：

1. app/agents/core/harness/__init__.py
   - 导出 AgentHarness, ExecutorBase, SingleShotExecutor, AgentMemory, Observer

2. app/agents/core/harness/runtime.py
   - class AgentHarness: Agent运行时入口
   - 字段: agent_id, role, llm, skills, memory, prompt_template, output_schema, hard_rules, soft_semantic(optional), observer, recovery_loop(optional, Phase 6填充)
   - async def run(self, input: dict, context: WorkflowContext) -> AgentOutput:
     * 步骤: 前置硬规则校验 → 选 executor 执行 → 后置硬规则校验(输出schema) → (可选)软语义仲裁 → 返回 AgentOutput
     * 失败时抛 NodeExecutionError(node_id, crash_reason, message)
   - async def _execute_with_skills(self, input, context) -> dict: 由 executor 调用

3. app/agents/core/harness/executor/__init__.py
4. app/agents/core/harness/executor/base.py
   - class ExecutorBase(ABC): 
     - abstract async def execute(self, harness, input, context) -> dict
5. app/agents/core/harness/executor/single_shot.py
   - class SingleShotExecutor(ExecutorBase):
     - 单轮调用: 拼 prompt → 调 LLM (支持流式 reasoning_content) → 调 Skills (如有) → 解析结构化输出
     - 必须支持 LLM 流式输出回调 (用于 D16 agent_thinking 事件)
     - 必须支持工具调用回调 (用于 D16 tool_call_start/end 事件)

6. app/agents/core/harness/memory.py
   - class AgentMemory: 任务内上下文 (D7)
   - set(key, value), get(key), to_dict()
   - 不做持久化，仅内存

7. app/agents/core/harness/observer.py
   - class Observer: D16 观测层
   - async def emit_trace(self, node_id, event_type, payload): 推送 trace 事件
   - async def emit_llm_stream(self, node_id, chunk): 推送 reasoning 流式
   - async def emit_tool_call_start(self, node_id, tool_name, inputs)
   - async def emit_tool_call_end(self, node_id, tool_name, success, summary)
   - async def emit_progress(self, node_id, current, total, label)
   - async def emit_model_switched(self, node_id, from_model, to_model)
   - async def emit_decision(self, node_id, decision_text)
   - 这些方法内部通过依赖注入的 callback 推送到 SSE 层 (本Phase先定义接口, 不接SSE)

8. app/agents/core/skills/base.py
   - class Skill(ABC): 
     - name: str
     - description: str
     - input_schema: Type[BaseModel]
     - output_schema: Type[BaseModel]
     - abstract async def execute(self, inputs) -> dict

9. app/agents/core/schemas.py
   - class AgentOutput(BaseModel): output: dict, quality_report: dict, token_usage: int, duration_ms: int, model_used: str
   - class WorkflowContext(BaseModel): workflow_id, node_id, user_id, account_id, topic, upstream_outputs: dict
   - class HardRule(ABC): abstract async def check_pre(self, input) -> bool; abstract async def check_post(self, output) -> bool
   - class NodeExecutionError(Exception): node_id, crash_reason, message
   - class RecoveryExhaustedError(Exception): node_id, attempts, last_error

要求：
1. 严格三层分离：harness 内不能 import langgraph
2. Observer 用依赖注入（构造时传入 callback 函数），不直接 import SSE
3. SingleShotExecutor 必须暴露流式回调接口
4. 所有 LLM 调用通过 adapters 层（Phase 3 实现），本Phase 只定义调用接口
5. 类型注解完整，pydantic 模型校验

严禁：
- 不要在 harness 内 import langgraph 或 fastapi
- 不要实现 ReAct executor（MVP 不需要）
- 不要实现 recovery（Phase 6）
- 不要硬编码 LLM 调用（必须通过 adapter）

完成后：
- 输出每个文件全文
- ruff check . 通过
- mypy 严格模式通过（可选）
```

### 自检清单
- [ ] harness 是子包（含 `__init__.py`）
- [ ] runtime.py 不 import langgraph
- [ ] executor 是子包，含 base + single_shot
- [ ] observer.py 暴露 6 种 trace 事件方法
- [ ] SingleShotExecutor 支持流式回调
- [ ] AgentOutput/WorkflowContext/HardRule 用 pydantic
- [ ] 无 ReAct executor
- [ ] 无 recovery 代码

---

## Phase 3: 模型适配器 + Skills 层

### 任务目标
实现 LLM 适配器（DeepSeek/Qwen-VL/通义万相）+ 6 个 Skill（xhs_search/vl_analyze/generate_image + MCP 抽象）。

### 文档引用
- 《技术架构设计文档.md》第5章「Skills 层」、5.2「MCP 集成」、5.2.2「模型适配器」
- 《PRD.md》D2、D12、D17
- 《开发红线手册.md》第3章「MCP 集成红线」、第9章「模型路由红线」

### 红线提醒
- MCP 必须双模式（插件 + uv run）+ 降级
- 登录态分层：插件优先 → Session刷新 → 扫码兜底
- 文案Agent 用 R1，其他文本Agent 用 V3
- Qwen-VL 只做理解，不生图
- 通义万相/即梦 只生图，不理解
- 配图 base64 返回，不存原图

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md 第5章 + 5.2节
2. d:\My_Project\多智能体小红书发布平台\PRD.md D2/D12/D17
3. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第3章 + 第9章

任务：实现模型适配器 + Skills 层。

### Part A: 模型适配器

文件: app/agents/adapters/__init__.py, llm_base.py, deepseek.py, qwen_vl.py, image_gen.py

1. app/agents/adapters/llm_base.py
   - class BaseLLM(ABC):
     - model_name: str
     - abstract async def chat(self, messages, response_format=None, stream=False) -> Union[dict, AsyncIterator[dict]]
     - abstract async def stream_chat(self, messages, response_format=None) -> AsyncIterator[dict]
     - 流式 chunk 格式: {"content": str|None, "reasoning_content": str|None, "is_final": bool}

2. app/agents/adapters/deepseek.py
   - class DeepSeekAdapter(BaseLLM):
     - 支持 V3 和 R1 两个模型
     - 用 openai SDK（DeepSeek 兼容 OpenAI 协议）调 /chat/completions
     - 必须解析 R1 的 reasoning_content 字段（流式单独推送）
     - 必须支持 response_format={"type": "json_object"} 强制JSON输出
     - 必须统计 token_usage

3. app/agents/adapters/qwen_vl.py
   - class QwenVLAdapter(BaseLLM):
     - 用 dashscope SDK
     - 支持 image_input: str (base64 或本地路径)
     - 用于分析爆款配图，输出图片标签（暖色调/俯拍等）
     - 不用于生图（红线）

4. app/agents/adapters/image_gen.py
   - class ImageGenAdapter(ABC): abstract async def generate(self, prompt, size, count) -> list[bytes]
   - class WanxAdapter(ImageGenAdapter): 通义万相
   - class JimengAdapter(ImageGenAdapter): 即梦
   - 默认用 WanxAdapter，可配置切换
   - 返回 PNG bytes，由调用方决定存哪

### Part B: Skills 层

1. app/agents/skills/base.py（已在 Phase 2 创建，此处确认）

2. app/agents/skills/xhs_search.py
   - class XHSSearchSkill(Skill):
     - 封装小红书爆款搜索
     - 通过 MCP Client Manager 调用（不直接调 Playwright）
     - 输入: keyword, limit, account_id
     - 输出: list[NoteSummary]，每条含 note_id, title, summary, likes, comments, images_base64[]
     - 风控策略硬编码（D17）: 单账号每日≤20次, 间隔30-60秒, 验证码3次切手动
     - summarize_result() 方法: 返回 "找到N条笔记，平均点赞X" 用于 D16 trace

3. app/agents/skills/vl_analyze.py
   - class VLAnalyzeSkill(Skill):
     - 调用 QwenVLAdapter 分析单张图片
     - 输入: image_base64
     - 输出: tags[] (如 "暖色调", "俯拍", "手冲咖啡")
     - 不存储原图（D11），分析完即释放

4. app/agents/skills/generate_image.py
   - class GenerateImageSkill(Skill):
     - 调用 ImageGenAdapter 生图
     - 输入: prompt, count, size
     - 输出: list[image_base64]
     - 支持切换 Wanx/Jimeng

### Part C: MCP Client Manager（D17 双模式）

1. app/agents/skills/mcp/__init__.py
2. app/agents/skills/mcp/base.py
   - class XHSMCPInterface(ABC):
     - abstract async def search_notes(self, keyword, limit) -> list[NoteSummary]
     - abstract async def fetch_post_detail(self, note_id) -> NoteDetail
     - abstract async def publish_note(self, content, images) -> PublishResult
     - abstract async def check_login_status(self) -> LoginStatus
     - abstract async def get_qrcode(self) -> bytes  # 扫码兜底用
3. app/agents/skills/mcp/plugin_client.py
   - class PluginMCPClient(XHSMCPInterface):
     - 通过 SSE/HTTP 调用浏览器插件 MCP（方式A，生产首选）
     - 健康检查: ping 接口
4. app/agents/skills/mcp/local_client.py
   - class LocalMCPClient(XHSMCPInterface):
     - 通过 stdio 启动 mcp_fetch_xhs_post（方式B，开发/兜底）
     - 用 subprocess + JSON-RPC
5. app/agents/skills/mcp/manager.py
   - class MCPClientManager:
     - 维护 plugin_client 和 local_client 两个实例
     - health_check() 定期检查 plugin 是否在线
     - async def search_notes(self, keyword, limit):
       * 优先调 plugin_client，失败降级 local_client
       * 降级时推送 search_degraded 事件（通过 observer callback）
     - 同理 fetch_post_detail / publish_note
     - 登录态分层（D1调整）: 
       - 优先用 plugin（复用浏览器登录态，无需扫码）
       - plugin 不可用 → 检查 xhs_accounts.session_data_encrypted 是否有效 → 自动刷新
       - 都不行 → 调 get_qrcode 触发扫码登录

要求：
1. 适配器层禁止业务逻辑，只做 API 封装
2. Skills 层通过依赖注入拿 adapter 和 mcp_client
3. MCPClientManager 是单例，启动时初始化
4. 所有外部调用必须有超时（httpx timeout=30s）
5. 风控参数（每日上限/间隔）做成可配置

严禁：
- 不要在 Skill 内直接调 Playwright
- 不要在 Skill 内存图片到 DB
- 不要让 LLM 适配器返回原始 SDK 对象（必须转成 dict）
- 不要混用 V3 和 R1（适配器层不区分，由 Agent 配置决定）
- QwenVLAdapter 不能有 generate_image 方法

完成后：
- 输出每个文件全文
- 写一个 tests/test_mcp_manager.py mock 测试降级逻辑
- ruff check . 通过
```

### 自检清单
- [ ] DeepSeek adapter 解析 reasoning_content
- [ ] QwenVL adapter 不含生图方法
- [ ] ImageGen adapter 支持 Wanx/Jimeng 切换
- [ ] XHSSearchSkill 通过 MCPClientManager，不直接调 Playwright
- [ ] MCPClientManager 实现双模式降级
- [ ] 登录态三层优先级正确（插件→Session→扫码）
- [ ] 风控参数可配置
- [ ] 无图片持久化到 DB

---

## Phase 4: Agent 配置 + LangGraph 编排

### 任务目标
实现 6 个 Agent 的 YAML 配置 + Prompt + LangGraph StateGraph 编排。

### 文档引用
- 《技术架构设计文档.md》第4.6章「YAML 配置」、第6章「LangGraph 编排」
- 《PRD.md》D3、D6、D13、D14
- 《前后端通信协议.md》第4章「节点状态码」
- 《开发红线手册.md》第2.3章「监督Agent分散式守卫」

### 红线提醒
- 每个 Agent 必须有独立 YAML（含 recovery 节，虽然 Phase 6 才填策略）
- LangGraph 节点不能直接调 MCP，必须通过 Harness → Skills
- 路由必须硬编码在 conditional_edges，不让 LLM 决定
- 回退用 LangGraph 原生 checkpoint（D6），不自维护 stale
- 节点状态机 9 种状态必须实现

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md 第4.6章 + 第6章
2. d:\My_Project\多智能体小红书发布平台\前后端通信协议.md 第4章
3. d:\My_Project\多智能体小红书发布平台\PRD.md D3/D6/D13/D14
4. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第2.3章

任务：实现 LangGraph 编排 + 6个Agent配置。

### Part A: WorkflowState 定义

文件: app/agents/graph.py

```python
from typing import TypedDict, Literal, Annotated
from langgraph.graph import StateGraph, END
from langgraph.checkpoint.postgres import PostgresSaver
from langgraph.graph.message import add_messages

class NodeStatus(str, Enum):
    PENDING = "pending"
    RUNNING = "running"
    AWAITING_REVIEW = "awaiting_review"
    PASSED = "passed"
    REJECTED = "rejected"
    ERROR = "error"
    SUSPENDED = "suspended"
    COMPLETED = "completed"
    TERMINATED = "terminated"

class WorkflowState(TypedDict):
    workflow_id: str
    user_id: str
    account_id: str
    topic: str
    current_node: str
    node_statuses: dict[str, NodeStatus]   # node_key -> status
    node_outputs: dict[str, dict]          # node_key -> output_data
    node_errors: dict[str, dict]           # node_key -> error info
    recovery_attempts: dict[str, int]      # node_key -> attempts count
    pending_reviews: list[dict]            # 当前待办
    pending_suggestions: list[dict]        # 监督建议
    suspended_until: str | None            # ISO datetime
    # D6: 不维护 stale，回退用 LangGraph 原生 checkpoint
```

### Part B: 6个Agent配置 YAML

在 app/agents/configs/ 创建:

1. search_agent.yaml
   - agent_id: search
   - role: 搜索小红书爆款
   - model: deepseek-v3  # D12
   - skills: [xhs_search]
   - executor: single_shot
   - hard_rules:
     - search_result_non_empty
     - rate_limit_check
   - soft_semantic: null  # 搜索不走软语义
   - recovery:  # Phase 6 填充策略
     max_attempts: 3
     strategies: []

2. analyze_agent.yaml
   - model: deepseek-v3
   - skills: [vl_analyze]
   - soft_semantic:
     enabled: true
     quality_check_prompt: "..."  # 检查爆款因子是否真的符合主题调性
   - recovery: {max_attempts: 3, strategies: []}

3. image_gen_agent.yaml
   - model: null  # 不调LLM，纯工具
   - skills: [generate_image]
   - soft_semantic: null
   - recovery: {max_attempts: 3, strategies: []}

4. copywrite_agent.yaml
   - model: deepseek-r1  # D12 文案用R1
   - skills: []
   - soft_semantic:
     enabled: true
     quality_check_prompt: "..."  # 文案是否空洞/调性
   - recovery: {max_attempts: 3, strategies: []}

5. audit_agent.yaml
   - model: deepseek-v3
   - skills: []
   - soft_semantic:
     enabled: true
     quality_check_prompt: "..."  # 合规性/敏感词
   - recovery: {max_attempts: 3, strategies: []}

6. publish_agent.yaml
   - model: null  # 纯工具调用
   - skills: [xhs_publish]  # 通过 MCP Client Manager
   - soft_semantic: null
   - recovery: {max_attempts: 3, strategies: []}

### Part C: 6个Agent的Prompt模板

在 app/agents/prompts/ 创建 6 个 .md 文件:
- search.md: 根据主题生成搜索关键词
- analyze.md: 分析爆款因子，结合 VL 标签
- image_gen.md: 生成图片 prompt（基于分析结果）
- copywrite.md: 编写小红书文案（标题/正文/标签）
- audit.md: 审核文案合规性
- publish.md: 格式化发布内容

每个 prompt 用 jinja2 模板语法，支持变量替换 ({{topic}}, {{analyzed_factors}} 等)

### Part D: LangGraph 图编排

文件: app/agents/graph.py

实现 build_workflow_graph(checkpointer: PostgresSaver) -> CompiledGraph:

节点:
1. search_node: 调 SearchHarness.run
2. analyze_node: 调 AnalyzeHarness.run + 后置 quality_check_node (D14 分散式守卫)
3. image_gen_node: 调 ImageGenHarness.run
4. image_review_node: interrupt_before（人工审核图片）
5. copywrite_node: 调 CopywriteHarness.run + 后置 quality_check_node
6. audit_node: 调 AuditHarness.run + 后置 quality_check_node
7. final_review_node: interrupt_before（终审）
8. publish_node: 调 PublishHarness.run

边:
- search -> analyze
- analyze -> quality_check_analyze -> (pass) image_gen / (fail) suggestion_node -> 等用户决策
- image_gen -> image_review (interrupt_before)
- image_review -> copywrite (用户通过) / image_gen (用户重生成)
- copywrite -> quality_check_copywrite -> audit
- audit -> quality_check_audit -> final_review (interrupt_before)
- final_review -> publish (用户通过) / rollback_to_X (用户打回)
- publish -> END

回退实现（D6 LangGraph原生）:
- 用户调 POST /api/workflow/{id}/rollback {target_node}
- 后端从 checkpoint 恢复，下游节点 status 重置为 PENDING
- 旧 output 丢弃（D6）

要求：
1. 节点函数签名: async def node_fn(state: WorkflowState) -> dict  # 返回 state 增量
2. quality_check_node 是独立的软语义节点，用 V3 判断
3. interrupt_before 必须用 langgraph 原生
4. 图编译时必须传入 PostgresSaver
5. checkpoint 持久化到 PostgreSQL

严禁：
- 不要在节点内直接调 MCP/LLM（必须通过 Harness）
- 不要让 LLM 决定路由（conditional_edges 硬编码）
- 不要自维护 stale 状态（D6 用原生）
- 不要用 MemorySaver
- 不要漏掉 quality_check_node（D14 分散式守卫）

完成后：
- 输出 graph.py 全文
- 输出 6 个 YAML 全文
- 输出 6 个 prompt.md 全文
- 写 tests/test_graph.py 验证图结构正确（节点数/边数）
- ruff check . 通过
```

### 自检清单
- [ ] WorkflowState 含 9 种状态枚举
- [ ] 6 个 YAML 都有 recovery 节（即使策略为空）
- [ ] copywrite_agent 用 R1，其他用 V3
- [ ] analyze/copywrite/audit 有 soft_semantic
- [ ] search/image_gen/publish 无 soft_semantic
- [ ] 图含 8 个业务节点 + 3 个 quality_check 节点
- [ ] interrupt_before 在 image_review 和 final_review
- [ ] 路由硬编码，无 LLM 决定
- [ ] 用 PostgresSaver
- [ ] 无 stale 状态字段

---

## Phase 5: FastAPI 路由 + SSE 推送

### 任务目标
实现 HTTP API 路由 + SSE endpoint + 27 种事件推送。

### 文档引用
- 《前后端通信协议.md》全文（重点第5/6/7/8/9章）
- 《开发红线手册.md》第5章「通信红线」
- 《PRD.md》D9

### 红线提醒
- 必须 SSE + HTTP POST 双通道，禁 WebSocket
- 27 种事件必须全部实现推送
- 每个事件必须有 event_id（`evt_` + ULID）
- 必须支持 Last-Event-ID 续传
- 15 秒心跳
- 前端操作走 HTTP POST，不用 SSE 反向通信

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\前后端通信协议.md 全文
2. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第5章

任务：实现 FastAPI 路由 + SSE 推送层。

### Part A: SSE 事件总线

文件: app/services/sse_bus.py

class SSEEventBus:
  - 单例
  - 维护 workflow_id -> set[asyncio.Queue] 的订阅关系
  - async def publish(workflow_id, event_type, payload):
    * 生成 event_id = "evt_" + ULID
    * 构造 SSEEvent{event_id, type, payload, timestamp}
    * 推送到该 workflow 的所有订阅队列
    * 同时持久化到 DB (用于 Last-Event-ID 续传)
  - async def subscribe(workflow_id, last_event_id=None) -> AsyncIterator[str]:
    * 如果 last_event_id 提供，先从 DB 查该 ID 之后的事件补发
    * 然后进入实时订阅模式
    * 每 15 秒推送心跳 ": heartbeat\n\n"
    * yield 格式: "id: {event_id}\nevent: {type}\ndata: {json}\n\n"
  - subscribe/unsubscribe 管理

### Part B: API Schemas

文件: app/api/schemas/ 下创建:
- workflow.py: StartWorkflowRequest, WorkflowResponse, RollbackRequest, PauseRequest
- review.py: ReviewActionRequest (action: pass/reject/regenerate/edit, payload)
- suggestion.py: SuggestionConfirmRequest (suggestion_id, decision: confirm/reject)
- recovery.py: ManualResumeRequest (workflow_id)
- account.py: LoginRequest, AccountResponse
- common.py: StandardResponse[T], ErrorResponse(code, message, detail)

### Part C: API Routers

1. app/api/routers/auth.py
   - POST /api/auth/register
   - POST /api/auth/login
   - POST /api/auth/logout

2. app/api/routers/account.py  (D1 分层登录)
   - GET /api/accounts - 列出当前用户的小红书账号
   - POST /api/accounts/check-plugin - 检测浏览器插件是否在线
   - POST /api/accounts/qrcode - 获取扫码登录二维码（兜底）
   - POST /api/accounts/refresh-session - 主动刷新Session
   - DELETE /api/accounts/{id} - 解绑账号

3. app/api/routers/workflow.py
   - POST /api/workflows - 启动工作流（含 topic + account_id）
   - GET /api/workflows/{id} - 查工作流状态
   - GET /api/workflows/{id}/nodes/{node_id} - 查节点详情（含 input/output）
   - GET /api/workflows/{id}/nodes/{node_id}/trace - D16 L3 原始数据查询
   - POST /api/workflows/{id}/pause - 暂停（D9 软暂停，节点边界生效）
   - POST /api/workflows/{id}/resume - 恢复
   - POST /api/workflows/{id}/rollback - D6 回退到指定 checkpoint

4. app/api/routers/sse.py
   - GET /api/sse/workflow/{workflow_id}
     * Accept: text/event-stream
     * 读 Last-Event-ID header
     * 调 SSEEventBus.subscribe
     * 返回 StreamingResponse

5. app/api/routers/review.py
   - POST /api/workflows/{id}/reviews/{review_id} - 处理审核（action: pass/reject/regenerate/edit）
   - GET /api/workflows/{id}/reviews/pending - 列出待办

6. app/api/routers/rollback.py  (D6 + D15)
   - POST /api/workflows/{id}/suggestions/{suggestion_id}/confirm - 确认监督建议
   - POST /api/workflows/{id}/suggestions/{suggestion_id}/reject - 拒绝

7. app/api/routers/recovery.py  (D15)
   - POST /api/workflows/{id}/resume-from-suspension - 用户主动恢复挂起的任务

8. app/api/routers/config.py  (可选, MVP可省)
   - GET /api/agents/configs - 列出所有 Agent 配置
   - PUT /api/agents/configs/{agent_id} - 更新配置

### Part D: 27种事件全部接入

在节点执行过程中（通过 Observer 的 callback）推送:
- 工作流级: workflow_started, workflow_snapshot, workflow_completed, workflow_error, workflow_suspended, workflow_resumed, workflow_paused
- 节点级: node_started, node_status_changed, node_completed, node_error
- 流式: stream_chunk, image_generated
- 审核: review_required, review_processed
- 监督: supervisor_suggestion, recovery_attempt, recovery_exhausted, circuit_open
- 检查点: checkpoint_saved
- D16 过程: tool_call_start, tool_call_end, progress_update, model_switched, agent_thinking, decision_made
- D17 系统: search_degraded

Observer 的 emit_* 方法 → SSEEventBus.publish

要求：
1. 所有 SSE 事件 schema 必须严格匹配《前后端通信协议》第5章
2. event_id 用 ULID，全局唯一
3. Last-Event-ID 续传必须实现
4. 心跳 15 秒
5. HTTP POST 返回 StandardResponse[T] 包装
6. 错误返回 ErrorResponse，code 必须是协议第9章定义的枚举
7. 所有路由用 async def

严禁：
- 不要用 WebSocket
- 不要前端轮询
- 不要漏推事件
- 不要让 event_id 重复
- 不要在 SSE 内反向接收前端消息（前端操作走 HTTP POST）

完成后：
- 输出 sse_bus.py 全文
- 输出所有 router 文件
- 输出所有 schema 文件
- 写 tests/test_sse.py 验证事件推送 + Last-Event-ID 续传
- ruff check . 通过
```

### 自检清单
- [ ] SSEEventBus 实现单例 + 订阅/发布
- [ ] event_id = "evt_" + ULID
- [ ] 15 秒心跳
- [ ] Last-Event-ID 续传
- [ ] 27 种事件全部接入 Observer
- [ ] 8 个 router 文件
- [ ] HTTP POST 用 StandardResponse 包装
- [ ] 错误码用协议第9章枚举
- [ ] D1 分层登录路由完整
- [ ] D6 rollback 端点存在
- [ ] D15 resume-from-suspension 端点存在

---

## Phase 6: Recovery 子系统

### 任务目标
实现 Recovery Loop + 策略库 + 退避 + 熔断 + 监督Agent决策（D15）。

### 文档引用
- 《技术架构设计文档.md》第4.4章「Recovery 子系统」、第6章「监督Agent」
- 《PRD.md》D15
- 《开发红线手册.md》第4章「Recovery 红线」

### 红线提醒
- max_attempts=3 + 智能退避 + 熔断
- 技术性恢复自动执行，结构性恢复用户拍板
- 30 分钟超时挂起
- Recovery 策略必须配置化（YAML）
- 监督Agent 是分散式守卫，不是中心节点

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md 第4.4章 + 第6章
2. d:\My_Project\多智能体小红书发布平台\PRD.md D15
3. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第4章

任务：实现 Recovery 子系统。

### Part A: 退避策略 + 熔断器

1. app/agents/core/harness/recovery/backoff.py
   - class BackoffPolicy:
     - base_delay: float = 1.0
     - max_delay: float = 30.0
     - jitter: bool = True
     - def compute_delay(self, attempt: int) -> float:
       * delay = min(base_delay * (2 ** attempt), max_delay)
       * if jitter: delay *= random.uniform(0.5, 1.5)
       * return delay

2. app/agents/core/harness/recovery/circuit_breaker.py
   - class CircuitBreaker:
     - states: CLOSED / OPEN / HALF_OPEN
     - failure_threshold: int = 5
     - recovery_timeout: int = 60  # 秒
     - sliding_window: 60秒
     - record_failure(), record_success(), can_execute()
     - OPEN 状态时拒绝执行，recovery_timeout 后转 HALF_OPEN 试探
     - 推送 circuit_open 事件（通过 observer）

### Part B: Recovery 策略库

3. app/agents/core/harness/recovery/strategies.py
   - class RecoveryStrategy(ABC):
     - type: str
     - abstract async def adjust_input(self, input: dict, last_error: Exception) -> dict
   - class RetryStrategy(RecoveryStrategy): 原样重试
   - class BroadenKeywordStrategy(RecoveryStrategy): 拓宽搜索关键词
   - class ReduceInputStrategy(RecoveryStrategy): 减少输入数据量
   - class SimplifyPromptStrategy(RecoveryStrategy): 简化prompt
   - class SwitchModelStrategy(RecoveryStrategy): 切换模型（R1→V3降级）
   - class RefreshTokenStrategy(RecoveryStrategy): 刷新小红书Token
   - 策略工厂: from_config(strategy_config) -> RecoveryStrategy

### Part C: Recovery Loop

4. app/agents/core/harness/recovery/loop.py
   - class RecoveryLoop:
     - max_attempts: int = 3
     - backoff: BackoffPolicy
     - circuit_breaker: CircuitBreaker
     - strategies: list[RecoveryStrategy]
     - async def execute_with_recovery(self, harness, input, context) -> AgentOutput:
       * for attempt, strategy in enumerate(strategies):
         - if not circuit_breaker.can_execute(): raise CircuitOpenError
         - try:
           adjusted_input = await strategy.adjust_input(input, last_error)
           await asyncio.sleep(backoff.compute_delay(attempt))
           result = await harness._execute_with_skills(adjusted_input, context)
           circuit_breaker.record_success()
           return result
         - except Exception as e:
           circuit_breaker.record_failure()
           await observer.emit_trace(...)  # 推送 recovery_attempt 事件
           last_error = e
           continue
       * 全部失败 → raise RecoveryExhaustedError
       * 触发 supervisor 介入

### Part D: 监督 Agent 恢复决策

5. app/agents/core/harness/recovery/supervisor.py
   - class SupervisorRecovery:
     - async def decide(self, crashed_node, crash_reason, state) -> RecoveryAction:
       * 用 V3（D12）
       * 注入: crash_reason, 当前 workflow state, 各节点状态摘要
       * 输出结构化: {action: "technical_auto"|"structural_user"|"terminate", target_node?, hint?, reason}
       * crash_reason 映射:
         - "timeout" / "empty_result" / "http_5xx" → 技术性（自动切模型/刷新Token）
         - "quality_check_failed" / "input_insufficient" → 结构性（建议回退到上游节点）
     - 技术性恢复: 自动执行（不打扰用户）
     - 结构性恢复: 创建 pending_suggestion 记录 + 推送 supervisor_suggestion 事件 + 启动30分钟计时器
     - 30分钟超时: 推送 workflow_suspended 事件 + 释放资源 + 写 suspended_until

6. app/services/recovery_service.py
   - class RecoveryService:
     - async def handle_recovery_exhausted(self, workflow_id, node_id, error):
       * 调 SupervisorRecovery.decide
       * 技术性: 自动执行 → 推送 node_started 重跑
       * 结构性: 创建 pending_suggestion → 推送 supervisor_suggestion → 启动30min计时
       * 超时: 推送 workflow_suspended
     - async def handle_user_confirm_suggestion(suggestion_id, decision):
       * 用户确认 → 触发 LangGraph rollback
       * 用户拒绝 → terminate
     - async def resume_suspended_workflow(workflow_id):
       * 用户主动恢复 → 检查依赖 → 重新拉起

### Part E: 更新 Agent YAML

在 Phase 4 的 6 个 YAML 中填充 strategies:
- search_agent: [retry, broaden_keyword, switch_model(fallback=v3)]
- analyze_agent: [retry, reduce_input, switch_model]
- image_gen_agent: [retry, switch_model(fallback=jimeng)]  # wanx 失败切 jimeng
- copywrite_agent: [retry, simplify_prompt, switch_model(fallback=v3)]  # R1失败切V3
- audit_agent: [retry, switch_model]
- publish_agent: [retry, refresh_token, switch_model]

### Part F: 30分钟超时挂起

- 用 asyncio.create_task 启动计时器
- 计时器到点: 推送 workflow_suspended, 释放 worker
- 用户调 POST /api/workflows/{id}/resume-from-suspension: 取消计时器, 重新拉起
- 定时拉起: 可选（MVP用 APScheduler，每天定时检查 suspended_until 已过期的任务）

要求：
1. RecoveryLoop 必须有 max_attempts 上限
2. 熔断器三态完整
3. 退避带 jitter
4. 监督决策用 V3，不用 R1
5. 30分钟计时器必须可取消
6. 技术性恢复不打扰用户
7. 结构性恢复必须经用户确认

严禁：
- 无限重试
- 让 LLM 决定重试次数
- 结构性恢复不经过用户确认
- 用 R1 做监督决策

完成后：
- 输出所有文件全文
- 写 tests/test_recovery_loop.py: 测试3次重试后 raise RecoveryExhaustedError
- 写 tests/test_circuit_breaker.py: 测试 CLOSED→OPEN→HALF_OPEN 流转
- 写 tests/test_supervisor.py: mock LLM 测试技术性/结构性分类
- ruff check . 通过
```

### 自检清单
- [ ] BackoffPolicy 含 jitter
- [ ] CircuitBreaker 三态完整
- [ ] 6 种 RecoveryStrategy 实现
- [ ] RecoveryLoop max_attempts=3
- [ ] RecoveryLoop 耗尽后触发 SupervisorRecovery
- [ ] SupervisorRecovery 用 V3
- [ ] 技术性恢复自动执行
- [ ] 结构性恢复创建 pending_suggestion
- [ ] 30分钟计时器可取消
- [ ] 6 个 YAML 填充了 strategies
- [ ] 测试覆盖3个核心场景

---

## Phase 7: 集成 + 跑通最小闭环

### 任务目标
串联所有模块，跑通 search → analyze 最小闭环（mock MCP + mock LLM）。

### 文档引用
- 全部文档
- 《开发红线手册.md》第13章「测试红线」

### 红线提醒
- 测试用 mock，禁用真实小红书账号
- 测试不依赖外部网络

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第13章
2. 回顾前6个Phase的所有代码

任务：集成测试 + 跑通最小闭环。

### Part A: 串联 main.py

更新 app/main.py:
- 初始化 FastAPI app
- 注册所有 router
- 启动时初始化: DB engine, MCPClientManager (单例), SSEEventBus (单例)
- lifespan: 启动时连接DB, 关闭时清理资源

### Part B: WorkflowService 串联

app/services/workflow.py:
- class WorkflowService:
  - async def start_workflow(self, user_id, account_id, topic) -> workflow_id:
    * 创建 workflow 记录
    * 构建 LangGraph (用 PostgresSaver)
    * 启动异步执行 (asyncio.create_task)
    * 推送 workflow_started 事件
  - async def execute_graph(self, workflow_id):
    * 跑 graph.astream
    * 每个节点事件 → SSEEventBus.publish
  - async def pause/resume/rollback

### Part C: Mock 测试

tests/mocks/:
- mock_deepseek.py: 返回固定的 LLM 响应（含 reasoning_content 模拟）
- mock_qwen_vl.py: 返回固定图片标签
- mock_mcp.py: 返回固定的小红书笔记数据（10条mock笔记）
- mock_image_gen.py: 返回 1x1 PNG

tests/test_minimal_flow.py:
- 测试用例: 启动工作流(topic="科技类") → search节点执行 → analyze节点执行 → 工作流结束
- 全程用 mock，不连真实服务
- 验证:
  1. workflow_completed 事件被推送
  2. search/analyze 节点 status 都变为 completed
  3. node_outputs 含正确数据
  4. agent_traces 表有 D16 trace 记录
  5. SSEEventBus 推送了至少: workflow_started, node_started(search), tool_call_start, tool_call_end, node_completed, node_started(analyze), node_completed, workflow_completed

### Part D: 端到端手动验证

写一个 scripts/manual_test.py:
- 启动一个临时 FastAPI server
- 用 httpx 调 POST /api/workflows 启动工作流
- 用 httpx 调 GET /api/sse/workflow/{id} 订阅 SSE
- 打印所有收到的事件
- 验证 27 种事件中至少 8 种被推送

要求：
1. 所有测试用 mock，不连真实小红书/LLM
2. 测试可独立运行: pytest tests/ -v
3. main.py 启动后 /docs 可访问

严禁：
- 用真实小红书账号测试
- 测试依赖外网

完成后：
- 运行 pytest tests/ -v，全部通过
- 运行 uvicorn app.main:app，启动无报错
- 输出测试报告
```

### 自检清单
- [ ] main.py 注册所有 router
- [ ] MCPClientManager 单例初始化
- [ ] SSEEventBus 单例初始化
- [ ] WorkflowService 串联 graph + SSE
- [ ] mock 4 个外部依赖
- [ ] test_minimal_flow.py 全部通过
- [ ] 至少验证 8 种事件推送
- [ ] /docs 可访问
- [ ] 无真实外部依赖

---

## Phase 8: 监督 Agent 软语义节点 + 收尾

### 任务目标
实现 3 个 quality_check_node（analyze/copywrite/audit 后置软语义判断）+ 全局自检。

### 文档引用
- 《技术架构设计文档.md》第6章「监督Agent」
- 《PRD.md》D3、D14
- 《开发红线手册.md》第2.3章

### 红线提醒
- 软语义用 V3，不用 R1
- 软语义只判质量，不做路由决策
- 路由由 conditional_edges 硬编码

### 执行指令

```
请阅读：
1. d:\My_Project\多智能体小红书发布平台\技术架构设计文档.md 第6章
2. d:\My_Project\多智能体小红书发布平台\PRD.md D3/D14
3. d:\My_Project\多智能体小红书发布平台\开发红线手册.md 第2.3章

任务：实现监督Agent软语义节点 + 全局收尾。

### Part A: 软语义节点

在 graph.py 中实现:

1. quality_check_analyze_node (analyze 后置):
   - 输入: analyze 节点的 output（爆款因子列表）
   - 用 V3 判断: "这些爆款因子是否真的符合{{topic}}类调性？"
   - 输出: {quality_pass: bool, reason: str}
   - 如果 fail: 推送 supervisor_suggestion 事件 + 创建 pending_suggestion
   - 路由: pass → image_gen; fail → 等用户决策（interrupt）

2. quality_check_copywrite_node (copywrite 后置):
   - 判断文案是否空洞/调性不符/字数不达标
   - 同上模式

3. quality_check_audit_node (audit 后置):
   - 判断审核是否到位（双重保险）
   - 同上模式

### Part B: 软语义 prompt

app/agents/prompts/:
- quality_check_analyze.md
- quality_check_copywrite.md
- quality_check_audit.md

每个 prompt 用 V3，输入上下文 + 判断标准，输出 JSON: {quality_pass, reason, suggestions[]}

### Part C: 全局红线自检脚本

scripts/redline_check.py:
- 扫描所有 .py 文件
- 检查:
  1. harness 内不 import langgraph
  2. 没有用 WebSocket
  3. 没有用 MemorySaver
  4. copywrite_agent 用 R1
  5. 监督Agent用 V3
  6. 无明文 Token 存储
  7. User:Account 是 1:N
- 输出报告

### Part D: 启动文档

更新 backend/README.md:
- 环境变量说明
- 启动命令
- 测试命令
- 已知限制

要求：
1. 软语义节点用 V3
2. 路由硬编码在 conditional_edges
3. redline_check.py 可独立运行
4. 所有 Phase 0-7 的代码无明显遗漏

完成后：
- 运行 python scripts/redline_check.py，全部通过
- 运行 pytest tests/ -v，全部通过
- 输出最终报告
```

### 自检清单
- [ ] 3 个 quality_check_node 实现
- [ ] 软语义用 V3
- [ ] 路由硬编码
- [ ] redline_check.py 通过
- [ ] 所有测试通过
- [ ] README.md 完整

---

## 附录 A: 每次执行前的标准 preflight

每次给 Codex 粘贴提示词前，先粘贴这段 preflight：

```
[Preflight]
请确认以下事项：
1. 你已阅读 d:\My_Project\多智能体小红书发布平台\开发红线手册.md 全文
2. 你理解所有红线是不可违背的
3. 生成代码前你会对照第8章自检清单
4. 遇到不确定的实现细节你会停下来问，不会自行猜测
5. 你不会引入红线清单外的框架/库
6. 你严格遵守三层架构分离（LangGraph / Harness / Skills）

如果以上都确认，回复"已确认"，然后开始执行任务。
```

---

## 附录 B: 红线违反处理协议

如果 Codex 生成的代码违反红线：

1. 立即停止当前文件生成
2. 指出违反的红线编号（如"违反红线 2.3：监督Agent不能是中心节点"）
3. 给出正确做法
4. 重新生成符合红线的代码
5. 不允许以"更简单""性能更好""用户体验更好"为由违反红线

---

## 附录 C: 文档权威性

开发过程中：

| 场景 | 权威文档 |
|---|---|
| 产品需求/功能定义 | PRD.md |
| 架构设计/Harness/MCP/Recovery | 技术架构设计文档.md |
| 事件schema/接口定义/错误码 | 前后端通信协议.md |
| 视觉规范（前端用） | 前端样式规范.md |
| 开发硬约束 | 开发红线手册.md |

代码与文档冲突时，**以文档为准**，更新代码而非文档。如需更新文档，需用户确认。

---

**手册结束。按 Phase 0-8 顺序执行，每个 Phase 完成自检后再进入下一个。**
