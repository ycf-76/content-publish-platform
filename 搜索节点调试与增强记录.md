# 搜索节点 Agent 调试与增强记录

> 记录时间：2026-07-29
> 涉及模块：前端 WorkbenchView / workflow store、后端 workflow service / agents / skills

---

## 一、问题现象

1. 前端搜索长时间转圈圈，无结果返回
2. 修复 SSE 订阅后，结果有数据但图片渲染失败
3. 搜索结果质量差：混入几颗星的低热度笔记，部分字段为空

---

## 二、根因定位过程

### 2.1 后端 SSE 事件流验证

编写 `backend/debug_sse_full.py` 调试脚本，模拟前端完整流程（建 SSE session → 启动工作流 → 订阅 SSE），打印所有事件。

**结论**：后端 25 个 SSE 事件全部正确发送，包括 `node_completed` 事件携带完整 `results` 数据。后端正常。

### 2.2 前端 SSE 订阅未建立（根因 1）

后端返回 `StandardResponse` 包装格式：

```json
{"success": true, "data": {"workflow_id": "xxx", ...}, "message": ""}
```

axios 拦截器返回 `response.data`（HTTP body），但前端 store 直接把它当作 `WorkflowResponse` 使用，导致 `currentWorkflow.value.workflow_id` = `undefined`，**SSE 订阅从未被调用**。

### 2.3 LLM 精简字段（根因 2）

调试日志显示 `node_completed` 的 `results` 只有 `note_id/title/url/likes`，丢失了 `cover_img/summary/author` 等字段。LLM 在最终输出 JSON 时自行精简了字段。

### 2.4 小红书图片 referer 限制（根因 3）

小红书图片 CDN 有 referer 校验，浏览器直接 `<img src="https://sns-img.xhscdn.com/...">` 会被拒绝加载（403）。

### 2.5 搜索结果质量差（根因 4）

`XhsSearchSkill` 只做字段清洗，不过滤：
- 无效项（无 note_id / 无标题 / 无封面图）
- 低热度笔记（likes < 100）
- 未按热度排序

---

## 三、修复方案

### 3.1 修复 StandardResponse 提取

**文件**：`frontend/src/stores/workflow.ts`

```typescript
// 修复前
const data: any = await workflowApi.start({ topic, account_id: accountId })
currentWorkflow.value = data as WorkflowResponse

// 修复后
const resp: any = await workflowApi.start({ topic, account_id: accountId })
const wfData = resp?.data ?? resp  // 提取 StandardResponse.data
currentWorkflow.value = wfData as WorkflowResponse
```

同时修复 `fetchWorkflowDetail` 的相同问题。

### 3.2 增强 Prompt 保留字段

**文件**：`backend/app/agents/harnesses/factory.py`

在 `_SEARCH_PROMPT` 末尾添加明确指令：

```
重要：output.results 必须完整保留 xhs_search 工具返回的每条笔记的所有字段，
包括 note_id、title、summary、likes、comments、url、cover_img、author，
直接原样复制工具返回的结果列表，不要精简或丢弃任何字段（尤其 cover_img 必须保留）。
```

### 3.3 新增图片代理路由

**文件**：`backend/app/api/routers/proxy.py`（新建）

```python
@router.get("/image")
async def proxy_image(request: Request) -> StreamingResponse:
    """代理图片请求，解决小红书 CDN referer 限制。"""
    url = request.query_params.get("url")
    # 安全校验：只代理白名单域名
    parsed = urlparse(url)
    if parsed.hostname not in _ALLOWED_HOSTS:
        raise HTTPException(status_code=403, detail=f"domain not allowed")
    
    async with httpx.AsyncClient(timeout=15.0, follow_redirects=True) as client:
        resp = await client.get(url, headers={
            "Referer": "https://www.xiaohongshu.com/",
            "User-Agent": "Mozilla/5.0 ...",
        })
    return StreamingResponse(iter([resp.content]), media_type=content_type, ...)
```

**注册路由**：`backend/app/main.py`

```python
from app.api.routers import ..., proxy, ...
app.include_router(proxy.router)
```

### 3.4 前端图片通过代理加载

**文件**：`frontend/src/views/WorkbenchView.vue`

```typescript
// 修复前
const coverHtml = cover
  ? '<img src="' + cover + '" alt="' + title + '" loading="lazy" />'
  : ...

// 修复后
const coverSrc = cover ? '/api/proxy/image?url=' + encodeURIComponent(cover) : ''
const coverHtml = coverSrc
  ? '<img src="' + coverSrc + '" alt="' + title + '" loading="lazy" />'
  : ...
```

### 3.5 增强 XhsSearchSkill（核心）

**文件**：`backend/app/agents/skills/xhs_search.py`

#### 新增参数

```python
class XhsSearchInput(BaseModel):
    keyword: str = Field(...)
    limit: int = Field(default=20, ge=1, le=100)
    account_id: str = Field(default="")
    min_likes: int = Field(default=100, ge=0, description="最低点赞数阈值")
    require_cover: bool = Field(default=True, description="是否要求封面图非空")
```

#### 增强执行逻辑

```python
async def execute(self, inputs):
    in_ = XhsSearchInput.model_validate(inputs)
    client = self._get_xhs_client()
    
    # 1. 多抓候选池（limit*2），避免过滤后数量不足
    raw_results = await client.search_notes(in_.keyword, limit=in_.limit * 2)
    
    # 2. 字段清洗
    cleaned = [self._normalize(r) for r in raw_results]
    
    # 3. 过滤无效项（无 note_id / 无标题 / 无封面图）
    cleaned = [r for r in cleaned if self._is_valid(r, in_.require_cover)]
    
    # 4. 过滤低热度（likes < min_likes）
    cleaned = [r for r in cleaned if r["likes"] >= in_.min_likes]
    
    # 5. 按点赞数降序排序（热门优先）
    cleaned.sort(key=lambda r: r["likes"], reverse=True)
    
    # 6. 截断到 limit
    final_results = cleaned[:in_.limit]
    
    return {
        "results": final_results,
        "summary": ...,
        "count": len(final_results),
        "filter_stats": {
            "raw_count": len(raw_results),
            "dropped_invalid": dropped_invalid,
            "dropped_low_likes": dropped_low_likes,
            "min_likes": in_.min_likes,
        },
    }
```

#### 有效性判断

```python
@staticmethod
def _is_valid(note: dict, require_cover: bool) -> bool:
    """过滤掉无 note_id / 无标题 / 无封面图的无效项。"""
    if not note.get("note_id"):
        return False
    if not note.get("title"):
        return False
    if require_cover and not note.get("cover_img"):
        return False
    return True
```

### 3.6 更新 Prompt 传递新参数

**文件**：`backend/app/agents/harnesses/factory.py`

```
策略：
1. 调用 xhs_search 工具搜索，keyword 参数必须填上面的真实主题文字，
   limit=20，min_likes=100（只保留点赞数≥100的热门笔记），require_cover=true。
2. 工具会自动过滤无效/低热度结果并按点赞数降序排序，直接使用返回的 results，不要二次筛选。
```

---

## 四、验证结果

### 修复前

```
首条 likes=541
混杂低热度内容（部分 likes=0）
部分笔记无 cover_img
图片渲染失败（403）
```

### 修复后

```
[4] decision_made: "调用 xhs_search 搜索夏日穿搭，limit=20，min_likes=100，require_cover=true"
[5] tool_call_start: {"keyword": "夏日穿搭", "limit": 20, "min_likes": 100, "require_cover": true}
[9] decision_made: "已获取结果，共15条有效笔记"
[10] node_completed: 首条 likes=9374，次条 likes=7002，字段完整含 cover_img
图片通过 /api/proxy/image 代理加载成功
```

---

## 五、搜索节点 Agent 工作原理

### 5.1 整体架构（分层视图）

```
┌─────────────────────────────────────────────────────────────┐
│  前端 WorkbenchView.vue                                     │
│    startSearchFlow() → workflowStore.startWorkflow()        │
│         ↓ HTTP POST                  ↑ SSE 事件订阅          │
└────────┼───────────────────────────────────┬────────────────┘
         │                                   │
┌────────▼───────────┐              ┌────────┴────────────────┐
│ API Router         │              │ SSE Router              │
│ POST /api/workflows│              │ GET /api/sse/workflow/:id│
└────────┬───────────┘              └────────▲────────────────┘
         │                                   │
┌────────▼───────────────────────────────────┴────────────────┐
│ WorkflowService.start_workflow()                            │
│   ① 写 DB（Workflow + 8个 WorkflowNode）                     │
│   ② 发 workflow_started 事件                                 │
│   ③ asyncio.create_task(_execute_graph_safely)  ← 不阻塞    │
└────────┬────────────────────────────────────────────────────┘
         │
┌────────▼────────────────────────────────────────────────────┐
│ LangGraph 编排层  graph.astream(state)                       │
│   search → analyze → quality_check → image_gen → ...        │
│   每个节点 yield state delta → 发 workflow_snapshot 事件     │
└────────┬────────────────────────────────────────────────────┘
         │
┌────────▼────────────────────────────────────────────────────┐
│ search_node(state)  ← graph.py                              │
│   ① emit node_started / node_status_changed                 │
│   ② _run_node_harness() → AgentHarness.run()                │
│   ③ emit node_completed(output)                             │
└────────┬────────────────────────────────────────────────────┘
         │
┌────────▼────────────────────────────────────────────────────┐
│ AgentHarness.run()  ← runtime.py                            │
│   pre-rules → executor.execute() → post-rules → AgentOutput │
└────────┬────────────────────────────────────────────────────┘
         │
┌────────▼────────────────────────────────────────────────────┐
│ LoopExecutor  ← ReAct 风格循环（最多4轮）                    │
│                                                             │
│  ┌─→ LLM 思考（DeepSeek）                                    │
│  │     ↓ 返回 JSON: {thought, tool_calls, final}            │
│  │   解析 JSON                                               │
│  │     ├─ final=true → 输出最终结果，退出循环                 │
│  │     └─ tool_calls → 逐个分发到 Skill                      │
│  │           ↓                                               │
│  │     Skill.execute() → 返回 observation                    │
│  │           ↓                                               │
│  │     拼回对话: [observation] {json}                        │
│  └───┘ (下一轮 LLM 拿到 observation 继续思考)                 │
└────────┬────────────────────────────────────────────────────┘
         │
┌────────▼────────────────────────────────────────────────────┐
│ XhsSearchSkill.execute()                                    │
│   ① MCP 层抓取（limit*2 候选池）                              │
│   ② _normalize() 字段清洗                                    │
│   ③ _is_valid() 过滤无效项（无note_id/title/cover）          │
│   ④ 过滤低热度（likes < min_likes=100）                      │
│   ⑤ 按 likes 降序排序                                        │
│   ⑥ 截断到 limit                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 逐层讲解

#### 第 1 层：前端发起

`WorkbenchView.vue` 的 `startSearchFlow`：
1. 从输入框拿关键词（如"夏日穿搭"）
2. 注册 `watch` 监听 `workflowStore.nodes`，等 `search` 节点状态变 `completed` 就调 `completeSearch(results)`
3. 调 `workflowStore.startWorkflow(keyword, accountId)`

`workflow.ts`：
- 先 `ensureSseSession()` 拿 `sse_token` cookie
- `POST /api/workflows` 启动工作流，从 `StandardResponse.data` 提取 `workflow_id`
- 用 `workflow_id` 建立 SSE 订阅

#### 第 2 层：后端 API + 异步启动

`WorkflowService.start_workflow()`：
1. 创建 `Workflow` 记录 + 8 个 `WorkflowNode`（search/analyze/image_gen/...）
2. 发 `workflow_started` SSE 事件
3. **关键**：`asyncio.create_task(_execute_graph_safely())` 后台异步执行，HTTP 立即返回 `workflow_id`（不阻塞）

后台 task 用**独立 db session**（请求 session 在请求结束就 close 了），并 try/except 包裹防静默失败。

#### 第 3 层：LangGraph 流式编排

```python
async for chunk in graph.astream(state, {"recursion_limit": 50}):
    for node_key, delta in chunk.items():
        await sse_bus.publish(workflow_id, "workflow_snapshot", {...})
```

LangGraph 按**硬编码路由**依次执行节点：
```
search → analyze → quality_check_analyze 
  →(pass) image_gen → image_review(interrupt) → copywrite 
  → quality_check_copywrite → audit → quality_check_audit 
  →(pass) final_review(interrupt) → publish
```

路由是 `conditional_edges` 硬编码，**不让 LLM 决定走哪条路**（红线约束）。

#### 第 4 层：search_node 节点函数

三段式：`node_started` → `harness 执行` → `node_completed`。harness 失败时走 `fallback_output`，**单点失败不阻断工作流**。

#### 第 5 层：Harness 装配

```python
AgentHarness(
    agent_id="search",
    role="xhs_search_agent",
    llm=get_deepseek_llm(),          # DeepSeek V3
    skills=[XhsSearchSkill(), GlobSkill(), GrepSkill(), BashSkill()],
    memory=AgentMemory(),
    prompt_template=_SEARCH_PROMPT,
    observer=_make_observer(workflow_id),  # SSE 事件桥
    executor=LoopExecutor(max_iterations=4),  # 最多4轮
)
```

Observer 的 `emit_callback` 把 trace 事件转成 SSE 推送。

#### 第 6 层：LoopExecutor ReAct 循环（核心）

**初始 messages**：
```python
[
    {"role": "system", "content": "You are an autonomous agent. Reply in STRICT JSON..."},
    {"role": "user", "content": _SEARCH_PROMPT.format(topic=..., account_id=...)},
]
```

**每轮迭代**：
1. `emit_progress` — 推 `progress_update` 事件（如 `current=1, total=4`）
2. 调 LLM — DeepSeek 流式返回 JSON
   - 如果有 `reasoning_content`（深度思考），推 `agent_thinking` 事件
3. 解析 JSON — 容忍 markdown fence
4. 判断 `final`：
   - `final=true` → 取 `output` 字段，break 退出
   - `final=false` → 继续
5. 处理 `tool_calls`：
   - `emit_tool_call_start`
   - `permission_gate.require()` 权限门控（被拒时把错误回喂 LLM）
   - `skill.execute(arguments)` 调用 Skill
   - `emit_tool_call_end`（含成功/失败 + 摘要）
   - 把 result 作为 `[observation] {json}` 拼回 messages
6. 下一轮 — LLM 拿到 observation 继续思考

**退出条件**：`final=true` / 跑满 4 轮 / LLM 没调工具也没说 final

#### 第 7 层：XhsSearchSkill（数据抓取 + 过滤）

MCP 层是**双模架构**：插件 MCP（主） → 本地 Playwright worker（兜底）。失败时推 `search_degraded` 事件通知前端。

### 5.3 一次完整执行的时序（从调试日志）

```
[1] node_started          ← search_node 进入
[2] node_status_changed   ← status=running
[3] progress_update       ← LoopExecutor 第1轮开始 (1/4)
[4] decision_made         ← LLM thought: "调用 xhs_search 搜索夏日穿搭"
[5] tool_call_start       ← 开始调 xhs_search
[6] search_degraded       ← 插件 MCP 失败，降级到本地 Playwright
[7] tool_call_end         ← xhs_search 返回，success=true
[8] progress_update       ← 第2轮 (2/4)
[9] decision_made         ← LLM: "已获取结果，直接输出"
[10] node_completed       ← final output, 含 results
[11] workflow_snapshot    ← state delta（search=completed）
[12] node_started         ← 下一个节点 analyze 开始
...
[25] workflow_completed   ← 全部节点跑完
```

### 5.4 关键设计约束（红线）

1. **节点不直接调 MCP/LLM** — 全部经 Harness，方便观察和回滚
2. **路由硬编码** — LLM 不能决定走哪个节点
3. **单点失败不阻断** — harness 失败走 `fallback_output`
4. **Observer 不 import SSE/FastAPI** — 依赖注入 `emit_callback`，解耦
5. **后台 task 独立 db session** — 不共享请求 session（避免 `IllegalStateChangeError`）
6. **权限门控** — 每次 Skill 调用前 `permission_gate.require()`，被拒时错误回喂 LLM 而非崩溃

---

## 六、修改文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `frontend/src/stores/workflow.ts` | 修改 | 修复 `StandardResponse.data` 提取（startWorkflow + fetchWorkflowDetail） |
| `frontend/src/views/WorkbenchView.vue` | 修改 | 图片通过 `/api/proxy/image` 代理加载 |
| `backend/app/agents/harnesses/factory.py` | 修改 | Prompt 增加字段保留指令 + 传递 min_likes/require_cover 参数 |
| `backend/app/agents/skills/xhs_search.py` | 修改 | 新增 min_likes/require_cover 参数，增强过滤+排序逻辑 |
| `backend/app/api/routers/proxy.py` | 新建 | 图片代理路由，解决小红书 CDN referer 限制 |
| `backend/app/main.py` | 修改 | 注册 proxy router |
| `backend/debug_sse_full.py` | 新建 | SSE 事件流调试脚本 |

---

## 七、经验沉淀

1. **StandardResponse 包装要统一处理** — 后端用 `{success, data, message}` 包装，前端 axios 拦截器只剥了 HTTP body，业务层还要再剥一层 `.data`。类似问题应全局排查。

2. **LLM 输出不可信，必须用 prompt 强约束** — LLM 会自行精简字段，光靠 prompt 不够，关键场景应在 Skill 层保证数据完整性。

3. **第三方图片需后端代理** — 小红书/微博等平台图片 CDN 有 referer 校验，浏览器直接加载会 403。白名单 + 后端代理是通用方案。

4. **搜索结果必须过滤+排序** — 原始搜索结果混有广告位/推荐位/低热度内容，必须按业务场景过滤（有效性 + 热度阈值）并排序。

5. **调试脚本要保留** — `debug_sse_full.py` 这类端到端调试脚本对后续问题定位很有价值，沉淀到项目里。

6. **SSE 事件流是验证后端的利器** — 前端转圈圈时，先用调试脚本直连后端 SSE，能快速定位是前端还是后端问题。

---

## 八、技术评审：结果评价与方案探讨

> 本节从「结果评价、符合预期程度、红线约束下的方案合理性、无红线时的替代方案」四个维度做技术评审。

### 8.1 结果评价

#### 8.1.1 做得好的部分

**1. 根因定位准确且分层**
- 用 `debug_sse_full.py` 直连后端 SSE 验证，先排除后端嫌疑，再回前端定位——这是正确的调试方法论，没有乱猜
- 4 个根因（StandardResponse 包装 / LLM 精简字段 / referer 限制 / 无过滤）是**链式依赖**关系，逐层暴露，没有遗漏

**2. 图片代理设计规范**
- 白名单域名校验（防 SSRF 滥用）
- 流式响应 + Cache-Control（性能可控）
- 独立路由文件，不污染业务 router

**3. 增强过滤的设计闭环完整**
- 多抓候选池（limit*2）→ 过滤无效 → 过滤低热度 → 排序 → 截断，是数据清洗的标准 pipeline
- 返回 `filter_stats` 统计字段，便于后续观测和调参

#### 8.1.2 值得商榷的部分

**1. min_likes=100 是经验值，缺数据支撑**
- 没有先跑一遍原始数据看 likes 分布（如 P50/P75/P90）就拍板 100
- 不同类目（穿搭 vs 知识 vs 美食）的热度基数差异大，硬编码阈值会误伤小众优质内容
- **更稳健**：用百分比阈值（如"保留 likes 排名前 30% 的笔记"）或按类目动态调整

**2. 过度依赖 prompt 约束 LLM 保留字段**
- 总结里也承认"LLM 输出不可信"，但修复方案主要靠 prompt 强约束
- 一旦换模型、换 prompt 版本，字段丢失问题可能复现
- **更稳健**：在 `search_node` 或 harness post-rules 层，用 skill 原始返回覆盖 LLM 输出的 `results` 字段（见下文方案 A）

**3. require_cover=true 可能误伤**
- 小红书有少量高质量视频笔记（无 cover_img 字段，但有 video 字段）
- 当前逻辑会把它们全过滤掉
- **更稳健**：`cover_img OR video_cover` 任一存在即视为有效

**4. 图片代理没做失败降级**
- 上游 CDN 偶发 502 时，前端会显示裂图
- **更稳健**：代理失败时返回 1x1 透明占位图或本地默认封面

### 8.2 符合预期程度

#### 8.2.1 业务预期 vs 实际效果

| 维度 | 预期 | 实际 | 符合度 |
|------|------|------|--------|
| 搜索返回热门内容 | 过滤低热度，按热度排序 | 首条 9374 赞，按降序 | ✅ 符合 |
| 图片可渲染 | 浏览器直接加载 | 经代理加载成功 | ✅ 符合 |
| 字段完整 | 含 cover_img/author 等 | LLM 输出字段完整 | ⚠️ 脆弱符合（依赖 prompt） |
| 无空数据 | 过滤无效项 | 15 条有效笔记 | ✅ 符合 |
| SSE 实时推送 | 前端实时收到事件 | 25 个事件全部收到 | ✅ 符合 |

**整体结论**：**业务功能符合预期，但工程质量是"脆弱符合"**——多个修复点依赖 prompt 约束和硬编码阈值，鲁棒性不足。

#### 8.2.2 工程质量的"脆弱点"

```
脆弱点 1: LLM 保留字段依赖 prompt（换模型就崩）
脆弱点 2: min_likes=100 硬编码（换类目就误伤）
脆弱点 3: require_cover=true 硬编码（视频笔记被误杀）
脆弱点 4: 图片代理无降级（CDN 抖动就裂图）
脆弱点 5: Skill 层过滤逻辑无单测（改一行就回归）
```

### 8.3 红线约束下的方案合理性

#### 8.3.1 红线对方案的塑造

回顾项目红线：
1. 节点不直接调 MCP/LLM，全部经 Harness
2. 路由硬编码，LLM 不能决定走哪
3. 单点失败不阻断
4. Observer 不 import SSE/FastAPI
5. 后台 task 独立 db session

**红线 1（必须经 Harness）** 的副作用：
- LLM 成了 `results` 字段的必经通道，导致字段精简问题
- 如果允许 `search_node` 直接调 skill 拿原始数据，字段完整性可在节点层保证
- **当前方案在红线内只能靠 prompt 约束，是合理的妥协**

**红线 3（单点失败不阻断）** 的体现：
- `fallback_output={"results": [], "count": 0}` 保证 harness 失败不崩
- 但空 results 会让下游 analyze 节点拿不到素材，工作流会"空转"
- **当前方案符合红线，但缺少"空结果"的显式处理**

#### 8.3.2 红线内可优化但未做的

**优化点 A：post-rules 层字段兜底**（红线内可做）

`runtime.py` 的 `post-rules` 是 harness 输出的后处理钩子。可以加一条规则：

```python
# 伪代码：在 search harness 的 post-rules 里
def _ensure_results_completeness(output, skill_cache):
    """如果 LLM 输出的 results 丢失字段，用 skill 原始返回补全。"""
    if not output.get("results"):
        return output
    # skill_cache 是 LoopExecutor 执行期间 skill 的原始返回
    raw_results = skill_cache.get("xhs_search", {}).get("results", [])
    if not raw_results:
        return output
    # 用 note_id 做 join，补全缺失字段
    raw_map = {r["note_id"]: r for r in raw_results}
    for item in output["results"]:
        nid = item.get("note_id")
        if nid in raw_map:
            for k, v in raw_map[nid].items():
                if k not in item or not item[k]:
                    item[k] = v
    return output
```

这样即使 LLM 精简字段，也能在 harness 层兜底。**不违反红线，比纯靠 prompt 鲁棒得多**。

**优化点 B：空结果的显式处理**（红线内可做）

`search_node` 失败或返回空时，当前直接走 `fallback_output`。可以加一个 `search_empty` 事件：

```python
if not output.get("results"):
    await sse_bus.publish(workflow_id, "search_empty", {
        "node_id": "search",
        "reason": "过滤后无结果",
        "suggestion": "建议降低 min_likes 或换关键词",
    })
```

前端收到后展示"未搜到符合条件的热门笔记，建议调整关键词"——比让用户看空列表友好。

### 8.4 无红线时的替代方案

假设没有"节点不直接调 MCP/LLM"这条红线，以下方案会更优：

#### 方案 A：节点层直接保证数据完整性（推荐）

**思路**：search_node 不依赖 LLM 输出 results，而是直接从 skill 原始返回拿数据。

```python
async def search_node(state):
    # 1. 调 skill 拿原始结果（不经 LLM）
    skill = XhsSearchSkill()
    raw_results = await skill.execute({
        "keyword": state["topic"],
        "limit": 20,
        "min_likes": 100,
    })
    
    # 2. LLM 只负责生成 summary 和趋势分析（不碰 results）
    harness = build_search_analysis_harness(workflow_id)
    analysis = await harness.run({"topic": ..., "raw_results": raw_results["results"]})
    
    # 3. 节点层组装最终输出，results 一定完整
    output = {
        "results": raw_results["results"],  # ← 原始数据，字段一定全
        "summary": analysis.output.get("summary", ""),
        "trend": analysis.output.get("trend", ""),  # LLM 负责的趋势分析
    }
    
    await emit_node_event(workflow_id, "search", "node_completed", output)
    return {...}
```

**优势**：
- `results` 字段完整性在节点层保证，与 LLM 行为解耦
- LLM 只做"分析"（擅长的事），不做"数据搬运"（容易出错的事）
- 换模型、换 prompt 都不会丢字段

**劣势**：
- 违反"节点不直接调 skill"红线（但这条红线本意是"便于观察和回滚"，可以通过给 skill 调用加 observer 解决）

#### 方案 B：双通道返回（LLM 输出 + 原始数据并行）

**思路**：让 LLM 输出分析结论，skill 原始数据走旁路上报，节点层合并。

```python
# LoopExecutor 期间，skill 的原始返回缓存到 memory
skill.execute(inputs) → memory.store("xhs_search_raw", result)

# 节点层合并
async def search_node(state):
    harness_output = await _run_node_harness(...)
    raw = harness_output.memory.load("xhs_search_raw")
    
    # 合并：LLM 的 summary + 原始 results
    final_output = {
        "results": raw["results"] if raw else harness_output.get("results", []),
        "summary": harness_output.get("summary", ""),
    }
```

**优势**：完全在现有架构内，不违反红线，比纯 prompt 鲁棒。

**劣势**：需要改 LoopExecutor 和 memory，改动面较大。

#### 方案 C：百分比热度阈值（替代硬编码 min_likes）

```python
class XhsSearchInput(BaseModel):
    ...
    # 用 top_percent 替代 min_likes
    top_percent: int = Field(default=30, ge=1, le=100, 
                              description="保留点赞数排名前 N% 的笔记")

async def execute(self, inputs):
    raw = await client.search_notes(...)
    cleaned = [self._normalize(r) for r in raw]
    cleaned = [r for r in cleaned if self._is_valid(r, ...)]
    
    # 动态阈值：按实际数据分布取 top 30%
    if cleaned:
        sorted_by_likes = sorted(cleaned, key=lambda r: r["likes"], reverse=True)
        cutoff = max(1, int(len(sorted_by_likes) * in_.top_percent / 100))
        final = sorted_by_likes[:cutoff]
    else:
        final = []
```

**优势**：自适应不同类目的热度基数，穿搭类和知识类都能合理过滤。

**劣势**：数据量小时（如只抓到 5 条）百分比意义不大，需配合绝对阈值兜底。

#### 方案 D：LLM 做趋势分析 + 规则做过滤（分工最优）

**思路**：承认 LLM 和规则各有擅长。

```
LLM 擅长：理解"夏日穿搭"这个主题下哪些子话题是趋势、哪些笔记有参考价值
规则擅长：过滤无效数据、按数值排序、保证字段完整

分工：
- 规则层（skill）：抓取 + 清洗 + 过滤 + 排序 → 拿到 top 20 热门笔记
- LLM 层（harness）：拿到 top 20 后，分析"这 20 条反映了什么趋势、哪些值得参考"
- 节点层：合并规则结果 + LLM 分析
```

这是**理想架构**，但需要重构 search_node 和 harness 的职责边界，改动较大。

### 8.5 总评

#### 8.5.1 评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 问题定位 | 9/10 | 根因链完整，调试方法规范 |
| 修复方案 | 7/10 | 红线内合理，但多个脆弱点未消除 |
| 工程质量 | 6/10 | 缺单测、硬编码、无降级 |
| 符合预期 | 8/10 | 业务功能达标，工程鲁棒性不足 |

#### 8.5.2 核心结论

**当前结果是"红线约束下的合理妥协"，业务符合预期，但工程质量存在 5 个脆弱点。**

如果允许重构，**方案 B（双通道返回）+ 方案 C（百分比阈值）+ 方案 D（LLM 负责分析）的组合**是最优解：

1. 用方案 B 在 harness 层兜底字段完整性（不违反红线，改动小）
2. 用方案 C 替代硬编码 min_likes（自适应类目）
3. 用方案 D 重新划分 LLM 和规则的职责（长期方向）

#### 8.5.3 改进建议

**短期建议（不改架构）**：
- 加 post-rules 字段兜底（方案 A 的简化版）
- min_likes 改为可配置（从 config 读取，不硬编码）
- 图片代理加失败降级（返回占位图）
- 给 `_is_valid` 和过滤逻辑补单测

**长期建议（下个迭代）**：
- 按 方案 D 重构 search_node，让 LLM 做趋势分析而非数据搬运
- 引入 likes 分布监控，用真实数据校准阈值
