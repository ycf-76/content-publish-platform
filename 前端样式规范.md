# 多智能体小红书发布平台 - 前端样式规范

| 字段 | 内容 |
|---|---|
| 文档版本 | v1.1 |
| 创建日期 | 2026-07-24 |
| 关联文档 | PRD.md / 前后端通信协议.md / 开发红线手册.md |
| 技术栈 | React 18 + TypeScript + Zustand + Tailwind CSS + React Flow |
| 用途 | 前端开发的视觉契约，设计师brief基础（框架无关，Vue/React通用） |

---

## 1. 设计原则

### 1.1 核心气质：克制专业，掌控为先

本产品不是消费级应用，是**专业工具**。用户是内容运营者，他们要的是"看得见、控得住"，不是"花里胡哨"。设计原则如下：

| 原则 | 含义 | 反面案例（不做） |
|---|---|---|
| **掌控感** | 每个状态都有明确视觉反馈，用户随时知道发生了什么 | 黑盒等待、loading转圈无信息 |
| **安全感** | 操作可撤销、错误可追溯、不会误操作 | 不可逆按钮、无确认的危险操作 |
| **克制** | 反馈强度匹配事件重要性，不滥用动效 | 每次点击都弹动画、toast满天飞 |
| **去AI化** | 避免典型AI生成的塑料感（渐变彩虹、emoji图标、居中hero） | 渐变背景、emoji当icon、对称居中布局 |
| **信息密度** | 专业工具容忍高密度，不浪费屏幕空间 | 大留白、卡片巨大、一屏只放两件事 |

### 1.2 去AI化的具体执行

避免以下"AI生成味"特征：

| AI味特征 | 本项目做法 |
|---|---|
| 紫粉蓝彩虹渐变 | 单一冷调蓝，仅在关键CTA用极轻微渐变 |
| Emoji当图标 | 全部用 Lucide Icons（线性图标） |
| 居中hero + 大标题 + 副标题 + 双按钮 | 工具型布局，左主右辅，无hero |
| 圆角全部16px+ | 圆角分级：4px/8px/12px，不滥用大圆角 |
| 卡片悬浮 + 巨大阴影 | 阴影极浅，层级靠分隔线和背景色差表达 |
| 毛玻璃 backdrop-blur | 仅在浮层（气泡/抽屉）用，主界面不用 |
| 打字机效果满天飞 | 仅文案流式输出用打字机，其他地方不做 |
| "Powered by AI"角标 | 不出现任何AI自吹自擂标识 |

### 1.3 视觉参考标杆

气质对标（不是抄样式，是抄气质）：
- **Linear**：信息密度、键盘优先、状态色克制
- **Vercel Dashboard**：冷调、几何、专业
- **Raycast**：深色优雅、节点感
- **Arc Browser**：空间感、侧栏组织

**不对标**：Notion（太纸感）、Stripe（太营销页）、任何SaaS落地页。

---

## 2. 色彩系统

### 2.1 主色：冷调蓝

采用**单一冷调蓝**作为品牌主色，全场景贯穿。不使用橙色（用户明确偏好）。蓝色传达"可靠、专业、冷静"，匹配工具型产品气质。

```
主色阶（HSL: 215° 90% 55% 为基准）

--blue-50:  #EFF4FF    背景tint，极浅蓝
--blue-100: #DBE6FE    悬浮态背景
--blue-200: #BFD3FE    边框hover
--blue-300: #93B4FD    禁用态
--blue-400: #608CFA    次要按钮
--blue-500: #3B6CF6    ★ 主色，primary按钮、链接、选中态
--blue-600: #2B52E0    primary按钮hover
--blue-700: #2342B8    pressed
--blue-800: #1F3891    深色背景文字
--blue-900: #1C2F6E    
```

**主色使用规则**：
- Primary按钮：blue-500 填充，hover blue-600
- 链接：blue-500 文字
- 选中态：blue-50 背景 + blue-700 文字 + blue-200 边框
- 焦点态：blue-200 描边（2px）+ blue-50 背景

### 2.2 中性色

```
亮色模式：
--bg-base:    #FFFFFF    页面底色
--bg-subtle:  #F7F8FA    次级背景（侧栏、卡片底）
--bg-elevated:#FFFFFF    卡片背景（靠阴影区分层级）
--border:     #E5E7EB    默认边框
--border-strong: #D1D5DB 强调边框

--text-primary:   #111827  主文字
--text-secondary: #4B5563  次文字
--text-tertiary:  #9CA3AF  辅助文字、placeholder
--text-disabled:  #D1D5DB  禁用文字
```

```
暗色模式：
--bg-base:    #0B0F1A    页面底色（不是纯黑，偏蓝黑）
--bg-subtle:  #111726    次级背景
--bg-elevated:#1A2233    卡片背景
--border:     #2A3245    默认边框
--border-strong: #3A4255 

--text-primary:   #F1F5F9
--text-secondary: #CBD5E1
--text-tertiary:  #64748B
--text-disabled:  #475569
```

### 2.3 语义色（节点状态色）

节点9种状态对应明确的视觉色，全场景统一。**这是整个产品最重要的色彩系统**，用户靠它一眼判断流程状态。

| 状态 | 主色 | 含义 | 亮色模式 | 暗色模式 |
|---|---|---|---|---|
| `pending` | 灰 | 待执行 | `#9CA3AF` | `#64748B` |
| `running` | 蓝 | 执行中（呼吸灯） | `#3B6CF6` | `#608CFA` |
| `awaiting_review` | 琥珀 | 待人工审核（小铃铛） | `#F59E0B` | `#FBBF24` |
| `passed` | 绿 | 通过 | `#10B981` | `#34D399` |
| `rejected` | 红 | 打回 | `#EF4444` | `#F87171` |
| `error` | 红 | 异常（抖动） | `#EF4444` | `#F87171` |
| `suspended` | 紫 | 挂起 | `#8B5CF6` | `#A78BFA` |
| `completed` | 蓝（深） | 全流程完成 | `#2B52E0` | `#3B6CF6` |
| `terminated` | 灰（深） | 终止 | `#4B5563` | `#64748B` |

**状态色使用规则**：
- 节点描边：状态色（2px）
- 节点图标：状态色填充
- 节点背景：状态色的 8% 透明度 tint
- `error` 节点附加抖动动效（见6.3）
- `running` 节点附加呼吸灯动效（见6.2）
- `awaiting_review` 节点附加小铃铛角标 + 轻微脉动

### 2.4 严重性色（监督建议severity）

| severity | 色 | 用途 |
|---|---|---|
| `low` | 蓝 | 建议性提示 |
| `medium` | 琥珀 | 需关注 |
| `high` | 红 | 必须处理 |

### 2.5 色彩使用禁令

- ❌ 不使用橙色（用户偏好）
- ❌ 不使用彩虹渐变
- ❌ 不使用高饱和粉色/紫色作为主视觉
- ❌ 状态色不混用（红=错误/打回，不用于装饰）
- ❌ 同屏不超过3种语义色同时出现（避免视觉混乱）

---

## 3. 字体系统

### 3.1 字体族

```css
--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI",
             "PingFang SC", "Microsoft YaHei", "Helvetica Neue",
             Arial, sans-serif;
--font-mono: "SF Mono", "JetBrains Mono", "Fira Code",
             Consolas, monospace;
--font-display: var(--font-sans);  /* 不用衬线体，保持现代感 */
```

### 3.2 字号阶梯（rem，基准16px）

| Token | 字号 | 行高 | 字重 | 用途 |
|---|---|---|---|---|
| `text-xs` | 0.75rem (12px) | 1.5 | 400 | 角标、辅助信息、时间戳 |
| `text-sm` | 0.875rem (14px) | 1.5 | 400 | 次文字、表格、按钮内文字 |
| `text-base` | 1rem (16px) | 1.6 | 400 | 正文（默认） |
| `text-md` | 1.125rem (18px) | 1.5 | 500 | 卡片标题、节点名称 |
| `text-lg` | 1.25rem (20px) | 1.4 | 600 | 区块标题 |
| `text-xl` | 1.5rem (24px) | 1.3 | 600 | 页面主标题 |
| `text-2xl` | 1.875rem (30px) | 1.2 | 700 | 仅登录页/空状态 |

**字号使用规则**：
- 默认正文 14px（不是16px），工具型产品提高信息密度
- 节点名称 16px medium
- 不使用超过 24px 的字号（除登录页）
- 数字（耗时、token、计数）用 mono 字体 + tabular-nums

### 3.3 字重

仅用 3 档：
- `400` regular：正文
- `500` medium：标题、按钮、节点名
- `600` semibold：页面主标题、强调

**不用** 700/800/900，避免粗暴感。

---

## 4. 间距与圆角

### 4.1 间距系统（4px基准）

```
--space-1:  4px
--space-2:  8px
--space-3:  12px
--space-4:  16px     ★ 默认内边距
--space-5:  20px
--space-6:  24px     ★ 区块间距
--space-8:  32px
--space-10: 40px
--space-12: 48px
--space-16: 64px     大区块间距
```

**间距使用规则**：
- 组件内元素间距：8px/12px
- 卡片内边距：16px
- 区块间距：24px
- 页面外边距：32px（大屏）/ 16px（小屏）
- 流程图节点间距：64px（水平）/ 48px（垂直）

### 4.2 圆角系统（分级，不滥用大圆角）

```
--radius-none: 0
--radius-sm:   4px     输入框、小标签、按钮
--radius-md:   8px     卡片、下拉菜单、节点
--radius-lg:   12px    大卡片、对话框、抽屉
--radius-full: 9999px  胶囊、圆点、角标
```

**圆角使用规则**：
- 按钮、输入框、tag：4px（克制）
- 卡片、节点：8px
- 对话框、抽屉、审核卡片：12px
- 状态圆点、角标、计数器：full
- **不使用 16px+ 大圆角**（避免软糯感，保持专业）

### 4.3 尺寸规范

| 元素 | 高度 |
|---|---|
| 小按钮 | 28px |
| 默认按钮 | 32px |
| 大按钮（主CTA） | 40px |
| 输入框 | 32px |
| 节点（极简态） | 48px（胶囊） |
| 节点（展开态） | 自适应，最小 120px |
| 顶栏 | 56px |
| 侧栏 | 280px（可折叠至 64px） |
| 待办计数器 | 24px 圆形 |

---

## 5. 阴影与层级

### 5.1 阴影系统（极浅，靠背景色差表达层级）

```
亮色模式：
--shadow-xs:  0 1px 2px rgba(17, 24, 39, 0.04);              边框感
--shadow-sm:  0 1px 3px rgba(17, 24, 39, 0.06);              卡片默认
--shadow-md:  0 4px 12px rgba(17, 24, 39, 0.08);             悬浮卡片
--shadow-lg:  0 8px 24px rgba(17, 24, 39, 0.10);             对话框、抽屉
--shadow-focus: 0 0 0 3px rgba(59, 108, 246, 0.15);          焦点环

暗色模式：
--shadow-xs:  0 1px 2px rgba(0, 0, 0, 0.3);
--shadow-sm:  0 1px 3px rgba(0, 0, 0, 0.4);
--shadow-md:  0 4px 12px rgba(0, 0, 0, 0.5);
--shadow-lg:  0 8px 24px rgba(0, 0, 0, 0.6);
--shadow-focus: 0 0 0 3px rgba(96, 140, 250, 0.25);
```

**层级表达优先级**（从弱到强）：
1. 背景色差（bg-base vs bg-subtle）
2. 分隔线（border 1px）
3. 阴影 xs/sm
4. 阴影 md/lg

**不滥用阴影**：默认卡片用 sm，仅悬浮态升 md。避免"卡片飘起来"的塑料感。

### 5.2 z-index 层级

```
--z-base:        0
--z-dropdown:    100
--z-sticky:      200    顶栏、待办计数器
--z-node-active: 300    当前运行节点（流程图内）
--z-popover:     400    气泡、tooltip
--z-drawer:      500    侧边抽屉
--z-modal:       600    对话框
--z-toast:       700    toast（最高）
```

---

## 6. 动效规范

### 6.1 动效原则：克制、有目的

| 原则 | 做法 |
|---|---|
| 动效服务于信息 | 每个动效都传达状态变化，不做纯装饰 |
| 时长克制 | 微交互 150-200ms，状态切换 250-300ms，不超过 400ms |
| 缓动自然 | 默认 `ease-out`，进入用 `cubic-bezier(0.16, 1, 0.3, 1)` |
| 不滥用弹簧 | 仅拖拽、回弹场景用 spring，其他用线性缓动 |

### 6.2 缓动函数

```css
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in:  cubic-bezier(0.4, 0, 1, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
--ease-spring: cubic-bezier(0.16, 1, 0.3, 1);  /* 进入动效 */
```

### 6.3 节点状态动效

| 状态 | 动效 | 实现 |
|---|---|---|
| `running` | 呼吸灯（描边明暗循环） | `animation: breathe 2s ease-in-out infinite` |
| `error` | 水平抖动一次 | `animation: shake 0.4s ease-out` |
| `awaiting_review` | 小铃铛轻微脉动 | `animation: pulse 2.5s ease-in-out infinite` |
| `passed` | 无动效（静态绿色对勾） | — |
| `suspended` | 缓慢闪烁（紫） | `animation: blink-slow 3s ease-in-out infinite` |

```css
@keyframes breathe {
  0%, 100% { box-shadow: 0 0 0 0 rgba(59, 108, 246, 0.4); }
  50% { box-shadow: 0 0 0 6px rgba(59, 108, 246, 0); }
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4px); }
  75% { transform: translateX(4px); }
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}
```

### 6.4 交互动效

| 交互 | 动效 |
|---|---|
| 按钮hover | 背景色 150ms 渐变，无位移 |
| 按钮press | 轻微缩放 `scale(0.98)` 100ms |
| 卡片hover | 阴影 sm→md，200ms，无位移 |
| 节点hover | 边框色变化 150ms |
| 抽屉打开 | 从右滑入 250ms ease-spring |
| 对话框 | 淡入+轻微上移 200ms |
| toast | 从顶部滑入 200ms，3秒后淡出 |
| 流式文案 | 逐字渲染，光标闪烁 |

### 6.5 动效禁令

- ❌ 不做页面切换的滑动动效（专业工具不卡顿）
- ❌ 不做卡片入场的瀑布流动效
- ❌ 不做hover时元素大幅位移
- ❌ 不做loading旋转动画（用骨架屏或状态文字）
- ❌ 不做视差滚动

---

## 7. 关键组件样式规范

### 7.1 流程图节点

#### 7.1.1 极简态（默认，胶囊形）

```
┌──────────────────────────────┐
│  ● 搜索爆款          2.3s    │   高度 48px
└──────────────────────────────┘
 ↑                              ↑
 状态圆点(8px)                  耗时(mono, tertiary)
```

- 形状：胶囊（radius-full）
- 尺寸：高 48px，宽自适应（min 160px）
- 内容：状态圆点 + 节点名（16px medium）+ 耗时/角标
- 边框：1px 状态色 50% 透明度
- 背景：bg-elevated

#### 7.1.2 展开态（运行中/待审核/出错）

```
┌────────────────────────────────────┐
│ ● 搜索爆款                  running │
│────────────────────────────────────│
│ 关键词: AI编程                       │
│ 模型: deepseek-v3                   │
│ 策略: 首次执行                       │
│ ──────────────────────────────      │
│ [展开详情]                          │
└────────────────────────────────────┘
```

- 形状：圆角矩形（radius-md 8px）
- 尺寸：宽 280px，高自适应
- 顶部状态条（4px 高，状态色）
- 阴影：md
- z-index：300（高于其他节点）

#### 7.1.3 待审核态（特殊高亮）

```
┌────────────────────────────────────┐
│ 🔔 图片审核          awaiting       │  ← 顶部琥珀色条
│────────────────────────────────────│
│ ┌──────┐ ┌──────┐ ┌──────┐         │
│ │ img1 │ │ img2 │ │ img3 │         │  ← 图片缩略图
│ └──────┘ └──────┘ └──────┘         │
│                                    │
│ [采纳AI生成]  [上传替换]  [重新生成] │
└────────────────────────────────────┘
```

> 注：🔔 用 Lucide 的 bell 图标，不用 emoji

- 顶部条：琥珀色 4px
- 节点边框：琥珀色 2px + 脉动
- 操作按钮：水平排列，primary 用 blue-500

### 7.2 待办计数器（顶栏）

```
┌──────────────────────────┐
│ 待办 ●3                   │  ← 24px 圆形角标，琥珀色
└──────────────────────────┘
```

- 位置：顶栏右侧
- 角标：24px 圆形，琥珀色背景，白色数字
- 0 时：不显示角标，文字灰色
- 点击：展开下拉待办列表

#### 待办下拉列表

```
┌──────────────────────────────────────┐
│ 待办中心 (3)                          │
│──────────────────────────────────────│
│ ● 图片审核              待处理  just now│
│   科技类 / 3张图                      │
│──────────────────────────────────────│
│ ● AI建议回退            待处理  2min   │
│   分析节点 → 建议回退搜索              │
│   ⏱ 29:45 剩余                        │  ← 30分钟倒计时
│──────────────────────────────────────│
│ ● 终审                  待处理  5min   │
│   文案已生成，等待您确认发布            │
└──────────────────────────────────────┘
```

- 宽度：360px
- 阴影：lg
- 每项可点击 → 滚动定位到流程图对应节点 + 节点高亮闪烁2次

### 7.3 监督建议气泡（节点旁）

```
                  ┌─────────────────────────────────┐
                  │ ⚠ AI建议 · 中等严重性            │
                  │─────────────────────────────────│
                  │ 分析节点的爆款因子提取不够具体，  │
                  │ 视觉标签"暖色调"过于笼统。       │
                  │                                 │
                  │ 溯源链:                          │
                  │  搜索 → 分析 (贡献: 输入笔记少)  │
                  │                                 │
                  │ 建议回退到: 搜索节点             │
                  │                                 │
                  │ [确认回退]  [忽略建议]           │
                  │ ⏱ 29:45 剩余                    │
                  └─────────────────────────────────┘
                          ▲
                          │
                    ┌─────┴──────┐
                    │  分析节点   │
                    └────────────┘
```

- 形状：圆角矩形（radius-lg 12px）
- 宽度：320px
- 阴影：lg
- 边框：severity 色 1px
- 顶部severity色条 3px
- 带小箭头指向节点
- 倒计时：mono 字体，红色（剩余<5分钟时）

### 7.4 时光机滑块（顶栏下方）

```
┌──────────────────────────────────────────────────────────┐
│ ◄  │ ●────●────●────●────●────○────○────○────○────►  ► │
│    │ 搜索  分析  图生  图审  文案  审核  终审  发布      │
│    │                                                          │
│    │ 当前: 文案 ✓                              [从此处继续]  │
└──────────────────────────────────────────────────────────┘
```

- 高度：56px
- 刻度：已完成节点实心圆点（blue-500），未完成空心圆点（border）
- 当前位置：稍大圆点 + 蓝色竖线
- 拖动时：右侧数据面板实时回显该检查点内容（半透明覆盖）
- "从此处继续"按钮：拖到非当前位置时出现，primary 按钮

### 7.5 审核卡片（图片审核展开态）

```
┌──────────────────────────────────────────────────┐
│ 图片审核                              [×]         │
│──────────────────────────────────────────────────│
│                                                  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐           │
│  │         │  │         │  │         │           │
│  │  AI生成 │  │  AI生成 │  │  AI生成 │           │
│  │   图1   │  │   图2   │  │   图3   │           │
│  │         │  │         │  │         │           │
│  └─────────┘  └─────────┘  └─────────┘           │
│  prompt: 暖色调俯拍...                            │
│                                                  │
│ ────────────────────────────────────────────      │
│  [拖拽上传替换图片]                                │
│  ┌────────────────────────────────┐              │
│  │     虚线区域，拖拽到此          │              │
│  └────────────────────────────────┘              │
│                                                  │
│ ────────────────────────────────────────────      │
│  [采纳全部AI生成]      [重新生成(改prompt)]       │
└──────────────────────────────────────────────────┘
```

- 形态：侧边抽屉（从右滑入，宽 480px）
- 图片网格：3列，gap 12px
- 上传区：虚线边框，drag-over 时变实线 blue-500
- 按钮：采纳=primary，重新生成=secondary

### 7.6 终审卡片

```
┌──────────────────────────────────────────────────┐
│ 终审 · 发布前最后确认                  [×]        │
│──────────────────────────────────────────────────│
│                                                  │
│ 标题                                              │
│ ┌────────────────────────────────────────────┐  │
│ │ 今天用AI写了3个小红书爆款，结果...          │  │
│ └────────────────────────────────────────────┘  │
│                                                  │
│ 正文（流式生成中...）                             │
│ ┌────────────────────────────────────────────┐  │
│ │ 最近AI编程很火，我试了三个工具...           │  │
│ │ 1. Cursor: ...                              │  │
│ │ 2. Trae: ...|                               │  │
│ │ （光标闪烁中）                                │  │
│ └────────────────────────────────────────────┘  │
│                                                  │
│ 图片                                              │
│ [img1] [img2] [img3]                             │
│                                                  │
│ 标签                                              │
│ [AI编程] [效率工具] [+]                          │
│                                                  │
│ ────────────────────────────────────────────     │
│ 审核报告: ✓ 调性匹配  ✓ 无违规  ⚠ 标签偏少        │
│                                                  │
│ [手动编辑后发布]  [打回到指定节点]  [确认发布]    │
└──────────────────────────────────────────────────┘
```

- 形态：全屏对话框（max-width 720px，居中）
- 编辑：所有字段可直接点击编辑
- 审核报告：来自 `audit` 节点的 quality_report
- 主CTA：确认发布（primary blue）

### 7.7 配置中心（YAML转表单）

```
┌──────────────────────────────────────────────────┐
│ 搜索Agent 配置                    [AI解读] [保存] │
│──────────────────────────────────────────────────│
│                                                  │
│ 基础配置                                          │
│ 模型      [DeepSeek-V3 ▾]                        │
│ 温度      [─────●──] 0.3                         │
│                                                  │
│ 恢复策略 (Recovery)                               │
│ 最大重试次数  [3]                                │
│ 退避基准延迟  [1.0] 秒                           │
│ 熔断阈值      [5] 次                             │
│                                                  │
│ 策略链（按顺序执行）                              │
│ ┌────────────────────────────────────────────┐  │
│ │ 1. [原样重试          ▾]            [×]    │  │
│ │ 2. [拓宽关键词        ▾]            [×]    │  │
│ │ 3. [切换备用模型 ▾] → [deepseek-v3 ▾] [×] │  │
│ │                              [+ 添加策略]  │  │
│ └────────────────────────────────────────────┘  │
│                                                  │
│ ────────────────────────────────────────────     │
│ [AI解读] 点击后展开:                              │
│ "当前设置下，如果搜索超时，系统会先原样重试，      │
│  再拓宽关键词，最后切换到V3模型。失败3次后才会     │
│  找你帮忙。连续失败5次会触发熔断保护。"           │
└──────────────────────────────────────────────────┘
```

- 形态：卡片表单
- 策略链：可视化规则生成器，每行一个策略，可拖拽排序
- AI解读：折叠区，点击展开，用灰色背景区分

### 7.8 挂起状态横幅

```
┌──────────────────────────────────────────────────────────┐
│ ⏸ 任务已挂起 · 30分钟未响应结构性恢复                      │
│ 待决策节点: 分析节点 · 下次自动检查: 14:32               │
│                                          [立即恢复]       │
└──────────────────────────────────────────────────────────┘
```

- 位置：顶栏下方，全宽
- 背景：purple-50（亮色）/ purple-900 30%（暗色）
- 文字：purple-800 / purple-200
- 图标：Lucide pause-circle
- 按钮：primary，恢复任务

### 7.9 登录页（特殊处理）

用户偏好：登录页要有动态小人。这是全站唯一允许"活泼"的页面。

```
┌──────────────────────────────────────────────────────┐
│                                                      │
│              [左侧: 动态小人插画区]                   │
│                                                      │
│              小人坐在电脑前，                          │
│              屏幕上是流程图节点，                      │
│              小人会偶尔转头、敲键盘、                  │
│              节点会亮起又熄灭                          │
│                                                      │
│              (Lottie 或 SVG 动画)                     │
│                                                      │
│      ┌─────────────────────────────────┐             │
│      │                                 │             │
│      │      多智能体小红书发布平台      │             │
│      │                                 │             │
│      │   [扫码登录小红书]              │             │
│      │                                 │             │
│      │   二维码区域（扫码后变对勾）     │             │
│      │                                 │             │
│      └─────────────────────────────────┘             │
│                                                      │
└──────────────────────────────────────────────────────┘
```

- 布局：左右分栏（大屏）或上下（小屏）
- 动态小人：Lottie动画或SVG+CSS，配色用主蓝色系
- 登录卡片：居中，圆角12px，阴影lg
- 扫码成功：二维码区域变绿色对勾 + 小人举手庆祝动画

### 7.10 执行详情面板（D16 过程透明化）

这是把"Agent黑盒"变"玻璃盒"的核心组件。用户点击任意节点，右侧滑出此面板，实时展示Agent内部的思考、工具调用、决策过程。

#### 7.10.1 面板整体形态

```
                                                                    ┌─────────────────────────────────┐
                                                                    │ 分析节点 · 执行详情       [×]    │
                                                                    │─────────────────────────────────│
                                                                    │ 耗时 12.3s · Token 2,450 · R1   │
                                                                    │─────────────────────────────────│
                                                                    │                                 │
                                                                    │ [时间线区域，可滚动]              │
                                                                    │                                 │
                                                                    │─────────────────────────────────│
                                                                    │ [展开原始数据]  [查看prompt]     │
                                                                    └─────────────────────────────────┘
```

- 形态：右侧抽屉（从右滑入，宽 420px，与审核抽屉同位但不同时打开）
- 阴影：lg
- 背景：bg-elevated
- 顶部节点信息条：节点名 + 耗时 + Token + 模型（mono字体）
- 中部时间线：可滚动，虚拟列表（>50条时）
- 底部操作区：L3原始数据入口

#### 7.10.2 时间线条目样式

每条trace事件渲染为一个时间线条目，按 `timestamp` 纵向排列：

```
┌──────────────────────────────────────────────────────────┐
│ 14:23:01  🔍  调用小红书搜索：关键词=AI编程               │  ← L1 工具调用开始（蓝）
│ 14:23:03  ✓  找到10条笔记，平均点赞2.3k                  │  ← L1 工具返回（绿）
│ 14:23:04  💭  思考中...                          [展开 ▾]  │  ← L2 思考（灰，可折叠）
│ 14:23:06  🎯  决定采用清单体结构                          │  ← L2 决策（紫）
│ 14:23:07  📊  分析图片 3/10  ──────●──────                │  ← L1 进度（蓝条）
│ 14:23:08  🔄  模型切换：R1 → V3（过载降级）               │  ← L1 模型切换（蓝）
│ 14:23:10  ⚠  分析失败：输入不足                           │  ← 异常（红）
└──────────────────────────────────────────────────────────┘
```

**条目结构**：
- 时间戳：`text-xs` mono tertiary，固定宽度对齐
- 图标：16px Lucide，颜色随事件类型
- 内容：`text-sm` primary，可换行
- 右侧：可选的操作（如进度条、展开按钮）

#### 7.10.3 事件类型视觉映射

| 事件类型 | 图标(Lucide) | 颜色 | 背景 tint | 说明 |
|---|---|---|---|---|
| `tool_call_start` | search | blue-500 | blue-50 | 工具调用开始 |
| `tool_call_end` (success) | check | green-500 | green-50 | 工具成功返回 |
| `tool_call_end` (fail) | x | red-500 | red-50 | 工具失败 |
| `progress_update` | loader | blue-500 | — | 进度条形态，非图标 |
| `model_switched` | refresh-cw | blue-500 | blue-50 | 模型切换 |
| `agent_thinking` | brain | gray-400 | — | 思考，可折叠 |
| `decision_made` | target | purple-500 | purple-50 | 决策点 |
| 节点错误 | alert-triangle | red-500 | red-50 | 异常 |

**图标尺寸**：16px，与时间戳对齐。

#### 7.10.4 思考过程折叠区（agent_thinking）

L2 事件默认折叠，用户点击展开。形态类似 ChatGPT o1 的 thinking 展开：

```
折叠态：
┌──────────────────────────────────────────────────────────┐
│ 14:23:04  💭  思考中（已生成 234 字）            [展开 ▾]  │
└──────────────────────────────────────────────────────────┘

展开态：
┌──────────────────────────────────────────────────────────┐
│ 14:23:04  💭  思考过程                          [收起 ▴]  │
│──────────────────────────────────────────────────────────│
│ │ 用户要科技类爆款，我需要对比标题和配图规律...           │  ← 左侧竖线引导
│ │ 首先看搜索结果，10条笔记中...                           │
│ │ 对比发现前3条都用了清单体...                            │
│ │（光标闪烁，流式渲染中）                                  │
│ │                                                         │
└──────────────────────────────────────────────────────────┘
```

**样式细节**：
- 折叠态：灰色小字"思考中（已生成 N 字）"，右侧展开按钮
- 展开态：左侧 2px gray-300 竖线，内容 `text-sm` gray-600
- 流式光标：1px 宽 blue-500 竖线，`blink 1s step-end infinite`
- 完成后：保留完整内容，可再次展开查看
- **concise模式下**：折叠态仍显示（让用户知道在思考），但不可展开（或展开提示"切换详细模式查看"）

#### 7.10.5 进度条样式（progress_update）

```
┌──────────────────────────────────────────────────────────┐
│ 14:23:07  分析图片  3/10                                  │
│ ──────●───────────────────────────  30%                   │
└──────────────────────────────────────────────────────────┘
```

- 标签：`text-sm` primary
- 进度条：高 4px，背景 gray-200，填充 blue-500
- 进度圆点：8px blue-500
- 百分比：`text-xs` mono tertiary 右对齐
- 同时在流程图节点上显示简化角标"3/10"（gray-500 `text-xs`）

#### 7.10.6 决策条目样式（decision_made）

```
┌──────────────────────────────────────────────────────────┐
│ 14:23:06  🎯  决定采用清单体结构              [理由] hover │
└──────────────────────────────────────────────────────────┘
```

- 图标 target + purple-500
- 文字：decision_summary（`text-sm` primary，medium 字重）
- 悬浮 tooltip：显示 rationale + alternatives_considered
- tooltip 形态：暗色背景，白字，圆角 4px，最大宽度 280px

#### 7.10.7 L3 原始数据折叠区

面板底部的"展开原始数据"按钮，点击后调 HTTP 接口加载，以折叠 JSON 树展示：

```
┌──────────────────────────────────────────────────────────┐
│ [展开原始数据]  [查看prompt]                              │
│──────────────────────────────────────────────────────────│
│ ▾ tool_call: xhs_search (1.2s)                           │
│   ▾ inputs                                                │
│     keyword: "AI编程"                                     │
│     limit: 10                                             │
│   ▾ output                                                │
│     total: 10                                             │
│     notes: [...]                                          │
│ ▾ llm_call: deepseek-r1 (3.4s, 1234 tokens)              │
│   ▾ prompt                                                │
│     "你是一个小红书分析专家..."                            │
│   ▾ reasoning_content                                     │
│     "用户要科技类爆款..."                                  │
│   ▾ response                                              │
│     {...}                                                 │
└──────────────────────────────────────────────────────────┘
```

- 默认折叠，点击展开
- JSON 树：等宽字体，可折叠节点
- 颜色：key 用 blue-700，string 用 green-700，number 用 purple-700
- 长内容：超出 200px 高度时内部滚动

#### 7.10.8 粒度模式切换

顶部导航栏放一个粒度切换开关：

```
┌──────────────────────────┐
│ 显示模式  [简洁 │ 详细]   │  ← toggle 开关
└──────────────────────────┘
```

- 形态：segmented control，2档
- 简洁（默认）：仅渲染L1事件，思考条目显示但不可展开
- 详细：L1+L2全渲染，思考可展开流式
- 切换即时生效，不重连SSE（事件本来就在接收，只是渲染与否）
- 用户偏好持久化到 localStorage

#### 7.10.9 动效规范

| 场景 | 动效 |
|---|---|
| 面板打开 | 从右滑入 250ms ease-spring |
| 新条目加入 | 从下方淡入+轻微上移 150ms |
| 思考流式 | 逐字渲染，光标闪烁 1s |
| 进度条更新 | 宽度变化 200ms ease-out |
| 折叠/展开 | 高度过渡 200ms ease-out |
| 条目hover | 背景色变 gray-50 100ms |

**禁令**：
- 不做条目入场的瀑布流动效
- 不做进度条的弹性动效
- 不做思考过程的打字机音效

---

## 8. 暗色模式

### 8.1 实现策略

- 通过 `data-theme="dark"` 属性切换
- 所有颜色用 CSS变量，切换时仅改变量值
- 暗色模式默认底色 `#0B0F1A`（蓝黑，不是纯黑）

### 8.2 暗色模式特殊处理

| 元素 | 亮色 | 暗色调整 |
|---|---|---|
| 阴影 | 灰色 | 纯黑 + 更高透明度 |
| 卡片 | 白底 | bg-elevated `#1A2233` |
| 边框 | 浅灰 | 深灰 `#2A3245` |
| 状态色tint背景 | 8%透明 | 12%透明（暗色下需更明显） |
| 流程图连线 | 浅灰 | 中灰 `#3A4255` |
| 文案流式光标 | 黑色竖线 | 白色竖线 |

### 8.3 默认主题

默认跟随系统 `prefers-color-scheme`，用户可在设置中手动切换并持久化。

---

## 9. 响应式断点

```
--breakpoint-sm: 640px    手机
--breakpoint-md: 768px    平板
--breakpoint-lg: 1024px   小屏笔记本
--breakpoint-xl: 1280px   标准桌面（★ 设计基准）
--breakpoint-2xl: 1536px  大屏
```

### 9.1 各断点布局

| 断点 | 流程图区 | 侧栏 | 待办 |
|---|---|---|---|
| < 768px | 不支持（提示用电脑） | — | — |
| 768-1024 | 单列，节点纵向排列 | 底部抽屉 | 下拉 |
| 1024-1280 | 流程图主区 + 右侧详情面板（折叠） | 左侧固定 | 下拉 |
| ≥ 1280 | 流程图主区 + 右侧详情面板（展开） | 左侧固定 | 下拉 |
| ≥ 1536 | 三栏：侧栏 + 流程图 + 详情面板常驻 | 左侧固定 | 下拉 |

**MVP 仅适配 ≥ 1024px**，移动端显示"请使用电脑访问"提示页。

---

## 10. 图标规范

### 10.1 图标库

统一使用 **Lucide Icons**（线性图标，2px描边，圆润端点）。

### 10.2 节点图标映射

| 节点 | 图标 |
|---|---|
| search | search |
| analyze | scan-search |
| image_gen | image-plus |
| image_review | image-check |
| copywrite | pen-line |
| audit | shield-check |
| final_review | check-circle |
| publish | send |

### 10.3 状态图标

| 状态 | 图标 |
|---|---|
| pending | circle-dashed |
| running | loader (旋转，但用 breathe 替代旋转) |
| awaiting_review | bell |
| passed | check |
| rejected | x |
| error | alert-triangle |
| suspended | pause-circle |
| completed | party-popper（仅终态一次性） |
| terminated | ban |

### 10.4 图标尺寸

- 16px：行内图标、按钮内图标
- 20px：节点图标、卡片标题图标
- 24px：空状态图标、导航图标
- 32px：登录页大图标

---

## 11. 文案规范

### 11.1 语气

- **专业克制**：不用"亲""哦""呢"，不用感叹号
- **信息明确**：每个按钮、提示都说明"会发生什么"
- **不甩锅**：错误提示说清原因和下一步，不只是"出错了"

### 11.2 按钮文案

| 场景 | 文案 | 反例 |
|---|---|---|
| 确认发布 | "确认发布" | "GO" / "冲" |
| 回退 | "确认回退到搜索节点" | "回退" |
| 暂停 | "暂停运行" | "停下" |
| 忽略建议 | "忽略建议，继续" | "no" |
| 重新生成 | "重新生成图片" | "regenerate" |

### 11.3 错误提示

格式：`[发生了什么] + [为什么] + [怎么办]`

- ❌ "发布失败"
- ✅ "发布失败：小红书返回内容违规。请检查文案中的敏感词后重新发布"

### 11.4 状态文字

| 状态 | 显示文字 |
|---|---|
| running | "执行中..." |
| running (Recovery) | "重试中 (2/3) · 切换备用模型" |
| awaiting_review | "等待您审核" |
| suspended | "已挂起 · 等待恢复" |
| error | "执行失败 · 监督Agent处理中" |

---

## 12. 设计Token导出

所有变量以 CSS Custom Properties 导出，Tailwind 配置引用这些变量。

```css
:root {
  /* 色彩 */
  --color-blue-500: #3B6CF6;
  /* ... 完整色阶见第2章 */

  /* 字体 */
  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", ...;
  --font-mono: "SF Mono", "JetBrains Mono", ...;

  /* 间距 */
  --space-1: 4px;
  /* ... */

  /* 圆角 */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;

  /* 阴影 */
  --shadow-sm: 0 1px 3px rgba(17, 24, 39, 0.06);
  /* ... */

  /* 缓动 */
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-spring: cubic-bezier(0.16, 1, 0.3, 1);
}

[data-theme="dark"] {
  --bg-base: #0B0F1A;
  --bg-subtle: #111726;
  /* ... 暗色覆盖 */
}
```

Tailwind config 引用：
```js
// tailwind.config.js
export default {
  theme: {
    extend: {
      colors: {
        blue: {
          500: 'var(--color-blue-500)',
          // ...
        }
      },
      borderRadius: {
        sm: 'var(--radius-sm)',
        md: 'var(--radius-md)',
      }
    }
  }
}
```

---

## 13. 设计师Brief要点（供出图参考）

若交由设计师出高保真，核心约束：

1. **气质**：Linear / Vercel / Raycast 那一挂的冷调专业工具，不是Notion纸感，不是SaaS营销页
2. **主色**：冷调蓝 `#3B6CF6`，不用橙、不用紫粉渐变
3. **去AI化**：禁渐变彩虹、禁emoji当图标（用Lucide线性图标）、禁居中hero、禁超大圆角
4. **信息密度**：工具型，容忍高密度，默认正文14px
5. **圆角分级**：4/8/12，不滥用16+
6. **阴影极浅**：靠背景色差和分隔线表达层级
7. **动效克制**：150-300ms，仅服务于状态变化，不做装饰动效
8. **节点状态色**：9种状态有明确色彩映射（见2.3），这是产品的视觉灵魂
9. **登录页例外**：可活泼，要有动态小人（Lottie/SVG），配蓝色系
10. **必出页面**：登录页 / 工作流主界面（流程图+待办+时光机）/ 图片审核抽屉 / 终审对话框 / 配置中心 / 挂起横幅

---

**文档结束。本规范是前端样式实现的契约，开发严格按此实现。**
