# 多智能体小红书发布平台 - 开发红线手册

| 字段 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 创建日期 | 2026-07-24 |
| 适用工具 | Codex + GLM-4.7-Flash |
| 关联文档 | PRD.md / 技术架构设计文档.md / 前后端通信协议.md / 前端样式规范.md |
| 用途 | AI辅助开发的不可违背约束，任何代码生成必须遵守 |

---

## 0. 红线手册使用方式

本手册列出**不可违背的开发约束**。Codex/GLM-4.7-Flash 生成代码时：

1. **必须先读本手册**，确认每条红线
2. **生成代码前**，对照第2-7章检查是否触碰红线
3. **生成代码后**，对照第8章自检清单逐项确认
4. **遇到红线冲突**，停止生成并提示用户，不自行妥协

**红线 = 不可违背**。非红线部分可灵活实现，但红线部分必须100%遵守。

---

## 1. 技术栈红线（不可替换）

| 层 | 必须使用 | 禁止使用 | 理由 |
|---|---|---|---|
| 前端框架 | React 18 + TypeScript | Vue / Angular / JS（非TS） | D8决策，TS强制 |
| 构建工具 | Vite | Webpack / CRA | D8配套 |
| 前端样式 | Tailwind CSS + CSS变量 | Styled Components / CSS Modules | 样式规范第12章 |
| 前端状态 | Zustand | Redux / MobX / Pinia | D8决策 |
| 前端路由 | React Router v6 | Vue Router / 自研路由 | D8配套 |
| 流程图 | React Flow | Vue Flow / 自研Canvas / D3 | 原版更稳定 |
| 图标 | Lucide React | emoji / 其他图标库 | 去AI化规范 |
| 表单 | React Hook Form + Zod | Formik / 受控组件（复杂表单） | 登录/审核表单 |
| HTTP | Axios | fetch（封装不足） | 通信协议第8章 |
| 时间 | Day.js | moment.js | 轻量 |
| 后端框架 | FastAPI (Python 3.11+) | Django / Flask / Express | D7决策 |
| Agent编排 | LangGraph | 自研状态机 / langchain AgentExecutor | D13决策 |
| Agent运行时 | 自研Harness（子包结构） | langchain Agent / autogen | D13决策 |
| 文本LLM | DeepSeek-V3 / DeepSeek-R1 | OpenAI / Claude / 其他 | D12决策 |
| 多模态理解 | Qwen-VL | GPT-4o / 其他VL | D2决策 |
| 图片生成 | 通义万相 / 即梦 | DALL-E / Stable Diffusion | D2决策 |
| 数据库 | PostgreSQL | MySQL / SQLite（生产） | 技术栈 |
| 实时通信 | SSE + HTTP POST | WebSocket | 通信协议第1章 |

**绝对禁止**：
- ❌ 引入未在技术栈清单中的框架/库，除非用户明确同意
- ❌ 用 langchain AgentExecutor 替代自研Harness
- ❌ 用 Vue Flow 替代 React Flow（必须用原版）
- ❌ 用 JavaScript 替代 TypeScript（前端强制TS）
- ❌ 用 Redux 替代 Zustand（Zustand API更简洁，强制）
- ❌ 用 WebSocket 替代SSE
- ❌ 混用类组件和函数组件（必须用函数组件 + Hooks）

---

## 2. 架构分层红线（不可越层）

### 2.1 三层架构必须严格分离

```
Layer A: LangGraph 编排层   → 只管流程、状态、路由、checkpoint
Layer B: Harness 执行层     → 只管Agent运行时、Recovery、Skills调度
Layer C: Skills+MCP 能力层  → 只管具体API调用
```

**禁止**：
- ❌ LangGraph节点内直接调用MCP/HTTP（必须通过Harness→Skills）
- ❌ Harness内直接操作LangGraph的state（只能读input、写output）
- ❌ Skills层内引用LangGraph或做流程判断
- ❌ Agent配置（YAML）里写Python代码（只能是配置）

### 2.2 Harness必须是子包结构

```
agents/harness/
├── runtime.py          # 入口
├── executor/
│   └── single_shot.py  # 单轮执行器
├── recovery/           # Recovery子系统
│   ├── loop.py
│   ├── strategies.py
│   ├── backoff.py
│   ├── circuit_breaker.py
│   └── supervisor.py
└── observer.py         # 观测层
```

**禁止**：
- ❌ 把Harness写成单文件 `harness.py`
- ❌ 把Recovery逻辑硬编码进Runtime
- ❌ 把Executor和Recovery混在一起

### 2.3 监督Agent是分散式守卫，不是中心节点

**禁止**：
- ❌ 实现一个"中心化监督Agent节点"统筹所有决策
- ❌ 让LLM做流程路由决策（路由必须硬编码在conditional_edges）

**必须**：
- ✅ 硬规则在Harness内执行（<10ms，零Token）
- ✅ 软语义作为独立的quality_check_node（仅analyze/copywrite/audit）
- ✅ Recovery决策在recovery/supervisor.py（仅Recovery Loop耗尽时触发）

---

## 3. MCP集成红线（D17）

### 3.1 双模式必须实现

```
方式A: 浏览器插件MCP（xhs-mcp-server）→ 生产首选
方式B: uv run MCP（mcp_fetch_xhs_post）→ 开发/兜底
```

**禁止**：
- ❌ 只实现单模式（必须双模式+降级）
- ❌ 后端直接调用Playwright（必须通过MCP抽象）
- ❌ 把搜索/发布逻辑硬编码进FastAPI路由

### 3.2 必须通过MCP Client Manager统一入口

```python
# 正确
result = await mcp_client_manager.search_notes(keyword, limit)

# 错误
result = await plugin_client.search_notes(keyword, limit)  # 禁止直接调插件
result = await playwright_page.goto(...)                    # 禁止直接操作浏览器
```

### 3.3 登录态分层（D1调整后）

**优先级顺序（不可颠倒）**：
1. 浏览器插件登录态（首选，无需扫码）
2. 已存储Session自动刷新（次选）
3. 扫码登录（兜底）

**禁止**：
- ❌ 强制所有用户扫码（装了插件的用户应无感）
- ❌ 明文存储Token（必须AES加密）

---

## 4. Recovery红线（D15）

### 4.1 三层兜底必须完整

```
Layer1: Recovery Loop（max_attempts=3 + 智能退避 + 熔断）
Layer2: 监督Agent Recovery决策（技术性自动/结构性拍板）
Layer3: 30分钟超时挂起
```

**禁止**：
- ❌ 无限重试（必须有max_attempts上限）
- ❌ 让LLM做重试决策（重试策略是硬编码的）
- ❌ 结构性恢复不经过用户确认（建议权，用户拍板）
- ❌ 技术性恢复打扰用户（切模型/刷新Token自动执行）

### 4.2 Recovery策略必须配置化

每个Agent的YAML必须含 `recovery` 节：

```yaml
recovery:
  max_attempts: 3
  backoff:
    base_delay_seconds: 1.0
    max_delay_seconds: 30.0
  circuit_breaker:
    failure_threshold: 5
    recovery_timeout_seconds: 60
  strategies:
    - type: retry
    - type: broaden_keyword
    - type: switch_model
      fallback_model: deepseek-v3
```

**禁止**：
- ❌ 把Recovery策略硬编码进Python代码
- ❌ 不同Agent共用同一套策略（每个Agent显式声明）

---

## 5. 前后端通信红线

### 5.1 必须用SSE + HTTP POST双通道

**禁止**：
- ❌ 用WebSocket替代SSE
- ❌ 前端轮询后端获取状态
- ❌ HTTP POST返回流式数据（流式走SSE）

### 5.2 事件必须符合协议定义

所有SSE事件必须严格遵循《前后端通信协议》第5章的schema。

**禁止**：
- ❌ 自定义事件类型（必须用协议定义的27种）
- ❌ 事件payload字段名与schema不符
- ❌ 漏推关键事件（如node_error/review_required/supervisor_suggestion）

### 5.3 事件ID和断线重连

- 每个SSE事件必须有 `event_id`（`evt_` + ULID）
- 必须支持 `Last-Event-ID` 断线续传
- 必须有15秒心跳注释

**禁止**：
- ❌ 事件无ID（断线重连会丢事件）
- ❌ 用data字段塞非JSON字符串

---

## 6. 前端样式红线

### 6.1 色彩红线

**主色**：冷调蓝 `#3B6CF6`（blue-500）

**禁止**：
- ❌ 使用橙色作为主色或强调色（用户明确偏好）
- ❌ 使用紫粉彩虹渐变
- ❌ 使用emoji作为图标（必须用Lucide Icons）
- ❌ 同屏超过3种语义色同时出现

### 6.2 节点状态色必须遵守9色映射

| 状态 | 颜色 |
|---|---|
| pending | 灰 |
| running | 蓝（呼吸灯） |
| awaiting_review | 琥珀 |
| passed | 绿 |
| rejected | 红 |
| error | 红（抖动） |
| suspended | 紫（闪烁） |
| completed | 深蓝 |
| terminated | 深灰 |

**禁止**：
- ❌ 自定义节点状态色
- ❌ error节点不加抖动动效
- ❌ running节点不加呼吸灯

### 6.3 动效红线

- 微交互：150-200ms
- 状态切换：250-300ms
- 不超过400ms
- 默认缓动 `ease-out`，进入用 `cubic-bezier(0.16, 1, 0.3, 1)`

**禁止**：
- ❌ 超过400ms的动效
- ❌ 视差滚动、瀑布流入场、卡片大幅位移
- ❌ loading旋转动画（用骨架屏或状态文字）
- ❌ 打字机音效

### 6.4 去AI化红线

**禁止**：
- ❌ 居中hero + 大标题 + 副标题 + 双按钮布局
- ❌ 超大圆角（>12px）
- ❌ 重阴影（"卡片飘起来"的塑料感）
- ❌ 毛玻璃backdrop-blur（仅浮层可用）
- ❌ "Powered by AI"角标

### 6.5 字号红线

- 默认正文：14px（不是16px，工具型高密度）
- 不使用超过24px字号（除登录页）
- 数字用mono字体 + tabular-nums

---

## 7. 数据模型红线

### 7.1 多账号预留（D5）

User与Account必须1:N关联，即使MVP前端只暴露1个账号。

```sql
users (1) ──── (N) xhs_accounts
```

**禁止**：
- ❌ 把小红书账号信息直接存在users表
- ❌ 硬编码单账号（DB层面必须支持多账号）

### 7.2 爆款图处理（D11）

- 只分析不存储原图
- 只存VL输出的标签
- MCP返回的base64分析后立即删除临时文件

**禁止**：
- ❌ 在DB或文件系统存储小红书爆款原图
- ❌ 把图片base64持久化到DB

### 7.3 检查点存储

- LangGraph checkpoint必须持久化（PostgresSaver）
- 时光机功能依赖checkpoint

**禁止**：
- ❌ 用MemorySaver（内存，重启丢失）作为生产环境checkpoint存储

---

## 8. 代码生成自检清单

Codex/GLM-4.7-Flash 生成每段代码后，必须逐项确认：

### 8.1 架构层
- [ ] 是否严格分三层（LangGraph / Harness / Skills）？
- [ ] 是否有越层调用？
- [ ] Harness是否是子包结构？
- [ ] 监督Agent是否分散式守卫（非中心节点）？

### 8.2 MCP层
- [ ] 是否通过MCP Client Manager统一入口？
- [ ] 是否实现了双模式降级？
- [ ] 登录态是否分层（插件优先/Session次选/扫码兜底）？
- [ ] Token是否AES加密存储？

### 8.3 Recovery层
- [ ] 是否有max_attempts上限？
- [ ] Recovery策略是否在YAML配置？
- [ ] 结构性恢复是否需用户确认？
- [ ] 技术性恢复是否自动执行？
- [ ] 是否有30分钟超时挂起？

### 8.4 通信层
- [ ] 是否用SSE（非WebSocket）？
- [ ] 事件是否符合协议schema？
- [ ] 事件是否有event_id？
- [ ] 是否支持Last-Event-ID续传？
- [ ] 是否有15秒心跳？

### 8.5 前端层
- [ ] 是否用React 18 + TypeScript（函数组件+Hooks）？
- [ ] 是否用Zustand（非Redux/Pinia）？
- [ ] 主色是否冷调蓝（非橙色）？
- [ ] 图标是否用Lucide（非emoji）？
- [ ] 节点状态色是否9色映射？
- [ ] 动效是否≤400ms？
- [ ] 是否避免去AI化禁令项？

### 8.6 数据层
- [ ] User:Account是否1:N？
- [ ] 是否未存储爆款原图？
- [ ] checkpoint是否持久化到PostgreSQL？

---

## 9. 模型路由红线（D12）

不同Agent必须用对应模型，不可混用：

| Agent | 必须用 | 禁止用 |
|---|---|---|
| 搜索 | DeepSeek-V3 | R1（浪费成本） |
| 分析 | DeepSeek-V3 | R1（分析不需深度推理） |
| 文案 | DeepSeek-R1 | V3（质量优先） |
| 审核 | DeepSeek-V3 | R1（审核不需深度推理） |
| 监督软语义 | DeepSeek-V3 | R1（成本优先） |
| 图片理解 | Qwen-VL | 其他VL |
| 图片生成 | 通义万相/即梦 | DALL-E/SD |
| Recovery决策 | DeepSeek-V3 | R1 |

**禁止**：
- ❌ 所有Agent用同一个模型
- ❌ 文案Agent用V3（质量不达标）
- ❌ 监督Agent用R1（成本爆炸）

---

## 10. MVP边界红线（不可越界）

### 10.1 MVP必须做
- 固定线性工作流（搜索→分析→图生→图审→文案→审核→终审→发布）
- 6个Agent + 自研Harness + Recovery子系统
- MCP双模式 + 降级
- 可视化流程图 + 时光机 + 待办中心 + 执行详情面板
- SSE推送 + 27种事件
- 人工审核（图片审核 + 终审）
- 30分钟超时挂起

### 10.2 MVP坚决不做
- ❌ 可视化拖拽编排DAG（固定流程，不可拖拽）
- ❌ 多账号切换（DB预留，前端隐藏）
- ❌ 多工作流并发（单工作流）
- ❌ 跨任务记忆/向量库
- ❌ 在线修改Prompt（运营不调Prompt）
- ❌ 工作流指挥台聊天框（D18，延后）
- ❌ 数据看板Dashboard
- ❌ 移动端适配（仅≥1024px）

**禁止**：
- ❌ 以"提升体验"为由添加MVP边界外的功能
- ❌ 提前实现二期功能"预留接口"（YAGNI原则）

---

## 11. 文档引用红线

开发过程中，以下文档是**唯一权威来源**，代码必须与文档一致：

| 场景 | 权威文档 |
|---|---|
| 产品需求/功能定义 | PRD.md |
| 架构设计/Harness/MCP | 技术架构设计文档.md |
| 事件schema/接口定义 | 前后端通信协议.md |
| 色彩/字体/动效/组件 | 前端样式规范.md |

**禁止**：
- ❌ 代码与文档不一致时自行修改代码（应提示用户更新文档）
- ❌ 凭"常识"实现未在文档中定义的功能（应先问用户）
- ❌ 引用文档中不存在的决策编号

---

## 12. 错误处理红线

### 12.1 禁止吞异常

```python
# 禁止
try:
    result = await agent.run()
except:
    pass  # 吞异常

# 必须
try:
    result = await agent.run()
except SpecificError as e:
    logger.error(f"Agent执行失败: {e}", exc_info=True)
    await observer.log_error(agent.agent_id, e)
    raise  # 让Recovery子系统处理
```

### 12.2 禁止裸raise

```python
# 禁止
raise Exception("something wrong")

# 必须
raise NodeExecutionError(
    node_id=node_id,
    crash_reason="timeout",
    message="节点执行超时（>15秒）"
)
```

### 12.3 前端错误提示必须三段式

```
[发生了什么] + [为什么] + [怎么办]

✅ "发布失败：小红书返回内容违规。请检查文案中的敏感词后重新发布"
❌ "出错了"
```

---

## 13. 测试红线

### 13.1 必须测试
- Recovery Loop的三层兜底逻辑
- MCP双模式降级逻辑
- SSE断线重连
- 节点状态流转（9种状态）
- 回退后下游重跑

### 13.2 禁止
- ❌ 用真实小红书账号做自动化测试（用mock MCP）
- ❌ 测试用例依赖外部网络（用mock LLM/MCP）

---

## 14. 红线违反处理

如果Codex/GLM-4.7-Flash生成的代码违反红线：

1. **立即停止生成**
2. **指出违反的红线编号**（如"违反红线6.1：使用了橙色主色"）
3. **给出正确做法**
4. **重新生成符合红线的代码**

**不允许**：
- ❌ 以"更简单"为由违反红线
- ❌ 以"性能更好"为由违反红线
- ❌ 以"用户可能需要"为由添加MVP边界外功能

---

## 15. 红线速查卡（贴在开发环境）

```
┌─────────────────────────────────────────────────────┐
│                  开发红线速查卡                       │
├─────────────────────────────────────────────────────┤
│ 技术栈: React18+TS+Zustand+FastAPI+LangGraph+Harness  │
│ LLM: V3(默认)/R1(仅文案)/Qwen-VL(理解)/万相(生图)    │
│ 通信: SSE+POST（非WebSocket）                        │
│ 主色: #3B6CF6蓝（非橙）                              │
│ 图标: Lucide（非emoji）                              │
│ MCP: 双模式+降级（非单模式）                          │
│ Recovery: 3次+退避+熔断（非无限重试）                 │
│ 监督: 分散式守卫（非中心节点）                        │
│ 回退: LangGraph原生（非自维护stale）                 │
│ 登录: 插件优先→Session→扫码（非强制扫码）            │
│ 账号: DB 1:N（非硬编码单账号）                       │
│ 图片: 只分析不存原图                                 │
│ MVP: 不做聊天框/拖拽编排/多账号/多工作流              │
└─────────────────────────────────────────────────────┘
```

---

**手册结束。本手册是AI辅助开发的硬约束，任何代码生成必须100%遵守。**
